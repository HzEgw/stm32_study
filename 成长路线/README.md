# 成长路线 · 接续指南（**开新会话先读这个文件**）

> **用途**：对话上下文会满，届时记忆不再连续。这个文件就是"外置记忆"——
> 读它 + `00_8个月学习计划.md`，30 秒恢复到当前进度。
>
> **新会话第一句话照抄这句即可**：
> 「读 `D:\STM32F103C8_Workspace\成长路线\README.md`、`00_8个月学习计划.md` 与 `03_周主题日与学习指引.md`，
> 我是楚怡计划学生（学籍湖南农大／就读湖南机电职院），目标是 2027-03 挑战杯；
> 请按"当前进度"那一节继续，代码风格遵循第 4 节，每日节奏按 03 的主题日来。」

---

## 1. 我是谁 & 目标

- 楚怡计划，**本科学籍（湖南农业大学）+ 就读湖南机电职业技术学院**，大二；**绩点第一**；每周可投入约 **20 小时**（工作日 2h×5 + 周六/周日各 5h）
- 8 个月目标（2026-09-20 → 2027-05）：
  1. **挑战杯校赛（2027-03）** 通过 → 冲省赛；同时申报大创
  2. **一台 STM32F407 + micro-ROS 自研下位机**的雷达避障/巡检小车（在现有 FishBot 上改造）
  3. **公开作品包**：GitHub 仓库 + 可复现 README + 演示视频 + 技术笔记
- 定位：**电控 ↔ ROS2 之间的桥（micro-ROS 全栈）**
- 团队：我（电控/ROS2/集成）+ 机械 1 人（SolidWorks）+ 文档/PPT 1 人
- 短板：无导师、职院实验室不可用、git 零基础、深度学习零基础、缺 HAL/CubeMX 经验

## 2. 硬件现状

| 硬件 | 状态 |
|---|---|
| 鱼香ROS FishBot **二驱**雷达小车（下位机 **ESP32-S3 + micro-ROS**，板载 MPU6050） | **已有**。雷达 = **MiniV1（全新）**；轮径 **65 mm**、**轮距 172.75 mm**、减速比 **40.5**、编码器 **11 线（4 倍频，脉冲比 44）**；**370 带编码器减速电机**；电池 **12V/2800mAh**；另有 0.96" OLED、SR04 超声波、视觉模组 V2.0、雷达转接板（ESP8266 透传）、**树莓派安装底板**。**参数表见 `01_…清点表.md`，权威出处与移植映射见 `02_…官方固件参数.md`** |
| STM32F103C8T6 核心板 + ST-Link | **已有 —— 10 月中旬前的一切学习都基于它** |
| 平衡车套件（江协科技，标准库） | **09-29 前后（W2 末）购买**；到货后用 `06_Kit_Check` 验收 |
| 示波器 | 可用；**无逻辑分析仪**（建议买 ~50 元） |
| PC：i9-13980HX / 32G / **RTX 4080 Laptop 12G** + Ubuntu 22.04 双系统（ROS2 Humble） | 已有 |
| STM32F407 或 H743 + 车载计算（**树莓派优先**，底板已预留安装位） | **11 月（9000 元到账后）购买** |
| **12V→5V DC-DC 降压模块（≥3A）×2** | **必买**（11 月）：⚠️ 12V 电池不能直进开发板，会烧板，见清点表 4.1 |

## 3. 文件位置约定

| 内容 | 路径 |
|---|---|
| VS Code 多根工作区 | `D:\STM32F103C8_Workspace\STM32F103C8_Workspace.code-workspace` |
| 学习工程（标准库版 + 寄存器版，各自自带 `Libraries/`） | `01_PWM_Output(_Reg)`、`02_Input_Capture(_Reg)`、`03_Encoder(_Reg)`、`04_UART_Bluetooth(_Reg)`、**`05_I2C_MPU6050(_Reg)`** |
| **工具工程：套件到货验收（到货当天跑）** | `06_Kit_Check\`（一键自检：LED/按键/I2C/编码器/电机转向/电池） |
| ROS2 侧源码（须在 Linux 编译） | `ros2_ws_src\uart_bridge\` |
| C/C++ 语言笔记 | `学习C_C++语言补充\`（01 编译链接、02 static、03 volatile、04 易错点） |
| 成长路线/计划 | 本文件夹 `成长路线\` |
| **鱼香小车硬件清点表（参数/线序/实测步骤）** | `成长路线\01_FishBot硬件清点表.md` |
| **官方固件参数 & STM32 移植映射（权威出处 + 教程章节对照）** | `成长路线\02_FishBot官方固件参数与STM32移植映射.md` |
| **周主题日与学习指引（每日节奏 / ROS2 恢复期 / 打卡表）** | `成长路线\03_周主题日与学习指引.md` |
| Keil 库（不随工程） | `D:\Keil\ARM\PACK\Keil\STM32F1xx_DFP\2.2.0\`；CMSIS：`D:\Keil\ARM\PACK\ARM\CMSIS\5.0.1\CMSIS\Include` |
| VS Code 全局 IntelliSense | 用户设置里的 `C_Cpp.default.*`（已配好，指向上面两个 Keil 路径；标准库工程用 `USE_STDPERIPH_DRIVER,STM32F10X_MD`，寄存器工程只 define `STM32F10X_MD`） |

### 3.1 双系统（Windows + Ubuntu）文件布局 —— 重要

**铁律：ROS2 的编译工作空间必须放在 Linux 的 ext4 上，绝不在 NTFS 上 `colcon build`**
（NTFS 的权限位/大小写/软链接/性能都会出问题，git 仓库放 NTFS 也会报权限警告）。

| 内容 | 位置 | 说明 |
|---|---|---|
| STM32 工程 / Keil / 笔记 / 计划 | **Windows D:（现工作区）** | Keil 只能在 Windows |
| ROS2 **包源码** | 工作区里做"源码仓"（如 `ros2_ws_src\<包名>`） | 源码可跨系统，**编译产物不行** |
| ROS2 编译与运行 | **Linux `~/ros2_ws`（ext4）** | `git pull` → `colcon build` 都在这边 |
| 跨系统同步手段 | **git**（W1 建仓库） | Windows 提交 → Linux 拉取；`.gitignore` 排除 `build/ install/ log/` |
| 只想在 Linux 里"看"笔记 | 只读挂载 NTFS（`/etc/fstab` 加 `ntfs3` 或 `ntfs-3g`，带 `ro`） | 用来看，不要用来编译 |

Linux 端典型流程：
```bash
cd ~/ros2_ws/src
git clone <仓库地址>            # 或 git pull
cd ~/ros2_ws && colcon build --symlink-install
source install/setup.bash
```

### 3.2 语言约定

- **项目主语言：C++**（ROS2 侧用 `rclcpp`；MCU 侧是 C）—— 已确认，不为项目学 Python；
- **Python 仅作工具语言**：临时读串口、画曲线、批量处理 rosbag 等。需要时用现成脚本、改参数即可，**不作为学习目标**；
- micro-ROS 是 C/C++，与上面一致。

## 4. 代码风格约定（新代码必须遵守）

1. **每个主题出两份**：标准库版 + 寄存器版，各自独立工程（不共享源码）；
2. **注释标注寄存器与位**：如 `TIM3->CCR1 = ccr;` / `/* -> TIM3->CCR1 = ccr */`，并说明位含义；
3. 每个工程带 `README.md`：文件清单、Keil 建工程步骤、Include Paths、Define、引脚表、**验证方法（没有 LED/串口也能验证）**、常见问题、标准库↔寄存器对照表；
4. 中断只做"取数 + 置标志"，计算放主循环；中断共享变量加 `volatile`；模块内部数据/函数用 `static`；
5. 验证优先用：Keil Watch 变量 + 万用表/示波器 + 把信息编码到 PWM 输出；
6. 所有代码进 git（W1 建仓库），参数放配置文件，不硬编码；
7. 引脚约定：避开 SWD（PA13/PA14）与串口（PA9/PA10）。

## 5. 当前进度（**每次做完就更新这一节**）

| 周 | 时间 | 状态 | 产出 |
|---|---|---|---|
| W1 | **09-20 ~ 09-26** | 进行中 | **A 线**：✅ **本地仓库已建 + 首次提交 `3867136`**（752 文件 / 561 KB / 200 唯一对象，2026-09-19 20:23）；⏳ **待推到 GitHub 私有仓库 `stm32-ros2-lidar-car`**。鱼香小车参数已入账（`01` / `02`，官方值 40.5 / 44 / 172.75），剩"实读确认"。**R 线**：环境体检 + ROS2 命令肌肉记忆（`03` §4）。**制度**：主题日已启用（`03`） |
| W2 | 09-27 ~ 10-03 | 待开始 | F103 UART（中断+环形缓冲）+ 串口→ROS2 话题 |
| W3 | 10-04 ~ 10-10 | 待开始 | 平衡车验收（`06_Kit_Check`）；F103 I2C 读 MPU6050（双版本） |
| W4 | 10-11 ~ 10-17 | 待开始 | 平衡车直立环起步；数据上上位机画曲线 |

**下一步（未完成的第一件事）**：把本地仓库推到 GitHub 私有仓库 `stm32-ros2-lidar-car`
（`git remote add origin …` + `git push -u origin main`，或在 VS Code 点「发布分支」）。
**09-19（周六）晚热身已完成**：`git init` + 首次提交 **`3867136`**（752 文件）—— 不计入 KPI，但原排在 09-20 的第一件事**提前一天做完了**。

**⏳ 时间纪律（重要，写给自己看）**：**09-20（周日）= W1 第 1 天**，**W1 只交付两件事：git 上手 + GitHub 仓库**。
下面这些**现在都不做**（已经排好队写在文件里了，到周再动手）：

| 事情 | 排到哪一周 | 为什么不是现在 |
|---|---|---|
| **每日怎么过（主题日）** | **今天就开始**（`03_周主题日与学习指引.md`） | 它是"节奏"，与周次无关：周一/周三电控、周二/周四 ROS2、周五归档、周末攻坚+产出 |
| 读"本车实读参数"（`read_config`） | **W1 周末 / W2**（车在手边时顺手 30 分钟） | 不阻塞任何事：参数已有官方默认值可用（`02_…` §2.1） |
| 万用表量编码器供电、6P 线序、ECHO 分压 | **W3 接线之前**（唯一必须"动手前"做的） | 现在不接线，测了也用不上 |
| 教程第 13~16 章通读 | **W2 起，每周 1 章** | 一次读完会消化不良 |
| `05_I2C_MPU6050_Reg`、`06_Kit_Check` 两个工程 | **已提前写好** ✅（见第 3 节文件表） | 到 W3 直接用，不用等"临阵磨枪" |

**我这边（Cline）的准备工作已覆盖到 W4**：F103 全部双版本工程、ROS2 桥、参数文档、git 速查、Kit 验收工程都已就位；
**你需要动手的第一件事仍然是 git**（`学习C_C++语言补充\05_git速查与工作流.md`）。

**主线学习资源（已定位）**：《动手学ROS2》**第 16 章 "FishBot 控制系统搭建"**（16.7 编码器测速 / 16.8 脉冲校准 / 16.9 最大速度 / 16.11~16.14 运动学与里程计）
—— https://fishros.com/d2lros2/#/humble/chapt16/章节导读 （ESP32 版；你的任务是**用 STM32 重做**，见 `02_…` 第 7 节映射表）。

## 6. 上下文管理建议（省时间也省钱）

- **一个话题一个新会话**：写代码 / 做规划 / 问概念 分开，避免上下文互相污染；
- 每次开会话先让我读本文件 + 计划文件；有进展就先让我**更新第 5 节**；
- 重要结论必须落盘（进本文件夹或对应 README），**不要只留在对话里**；
- 长期资料放 D 盘，`%TEMP%` 里的临时脚本可随时删。
