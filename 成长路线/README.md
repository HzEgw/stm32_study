# 成长路线 · 接续指南（**开新会话先读这个文件**）

> **用途**：对话上下文会满，届时记忆不再连续。这个文件就是"外置记忆"——
> 读它 + `00_8个月学习计划.md`，30 秒恢复到当前进度。
>
> **新会话第一句话照抄这句即可**：
> 「读 `D:\STM32F103C8_Workspace\成长路线\README.md`、`00_8个月学习计划.md` 与 `03_周主题日与学习指引.md`，
> 我是楚怡计划学生（学籍湖南农大／就读湖南机电职院），目标是 2027-03 挑战杯；
> 请按"当前进度"那一节继续，代码风格遵循第 4 节，每日节奏按 03 的主题日来。」

---

## 1. 我是谁 & 目标

- 楚怡计划，**本科学籍（湖南农业大学）+ 就读湖南机电职业技术学院**，大二；**绩点第一**；每周 **基准 25 小时 / 冲刺 30 小时**（v1.11 按真实课表：周一/二晚 2.5h、周三/四下午 3h、周五 5h、周六 4h、周日 5h）
- 8 个月目标（2026-09-20 → 2027-05）：
  1. **挑战杯校赛（2027-03）** 通过 → 冲省赛；同时申报大创
  2. **一台 STM32F407 + micro-ROS 自研下位机**的雷达避障/巡检小车（在现有 FishBot 上改造）
  3. **公开作品包**：GitHub 仓库 + 可复现 README + 演示视频 + 技术笔记
  4. **RM（**P1 · 探路 + 队伍身份**，v1.24 起激活 · v1.30 加护栏）**：**不额外占主线时间**、**不改变 `S1→S11` 学习顺序**；
     🛡 **硬护栏（v1.30）**：**RM ≤5h/周（含例会、考核、私下练习）**、**禁止占用周一/周三电控日**；
     **任一月主线完成率 <80% → RM 降为"只参会"**；**2026-12-31 判据不变**（联系不到可用战队/学籍口径不认 → 退出 RM，回"个人作品 + 挑战杯 + micro-ROS"）；
     **唯一真会互相抢的只有"时间盒"和"注意力"** —— 因为两条线共用同一套肌肉（PWM / 编码器 / PID / UART / FreeRTOS）；
     **只在 RM 才需要的（CAN / 无刷·H 桥驱动 / 裁判协议）→ 单独排队，不插 A 线**（`12` §4）。
     > ⚠️ **小车是 P0、是唯一作品载体**（用户 2026-09-25 原话：**"RM 不一定能拿比赛，但是小车我必须要拿出手"**）。
- ❌ **蓝桥杯：已放弃（2026-09-23，见 `00` §16.8 v1.24）** —— 寒假 30h 已归主线
- 定位：**电控 ↔ ROS2 之间的桥（micro-ROS 全栈）**
- 团队：我（电控/ROS2/集成）+ 机械 1 人（SolidWorks）+ 文档/PPT 1 人
- 短板：无导师、职院实验室不可用、git 零基础、深度学习零基础、缺 HAL/CubeMX 经验

## 2. 硬件现状

| 硬件 | 状态 |
|---|---|
| 鱼香ROS FishBot **二驱**雷达小车（下位机 **ESP32-S3 + micro-ROS**，板载 MPU6050） | **已有**。雷达 = **MiniV1（全新）**；轮径 **65 mm**、**轮距 172.75 mm**、减速比 **40.5**、编码器 **11 线（4 倍频，脉冲比 44）**；**370 带编码器减速电机**；电池 **12V/2800mAh**；另有 0.96" OLED、SR04 超声波、视觉模组 V2.0、雷达转接板（ESP8266 透传）、**树莓派安装底板**。**参数表见 `01_…清点表.md`，权威出处与移植映射见 `02_…官方固件参数.md`** |
| STM32F103C8T6 核心板 + ST-Link | 已有；**备用 F103 板 ×2 + F407VET6 立刻买**（v1.5：钱管够，**不再等 11 月**） |
| 平衡车套件（江协科技，标准库 · **套餐1 含遥控器**） | ✅ **已下单 2026-09-25 22:32**；⚠️ **到货即成品（无需组装）** → G0 = "**核对 + 量电压链 + `06_Kit_Check` + 反向拆解画引脚表**"（`11` §1.1）。📋 **官方参数已入账（`11` §1，下单页截图）**：主控 `F103C8T6` ｜ 驱动 **`TB6612FNG`** ｜ 姿态 `MPU6050` ｜ 无线 `NRF24L01+` ｜ 蓝牙 **`HC-04`**（UART = S6）｜ 稳压 **`AMS1117` 3.3V** ｜ 电机 **`25GA370`／减速比 9.27666／6V620RPM** ｜ **编码器 AB 正交 `11PPR`** ⇒ **推算 102 计数/圈（1 倍频）／408（4 倍频）** ｜ 电池 2×18650 1500mAh ｜ 遥控器 = `F103` + `NRF24L01+`(AT2401C)。⚠️ **"是否带编码器"已由参数表回答（有）** → G0 只剩**实物核对 + 查 A/B 引到 MCU 哪两个脚**；⚠️ **一圈计数 / 电压链仍必须实测**，**不许照抄鱼香车的 40.5 / 44 / 172.75**；**电池每月补电一次** |
| 示波器 | 可用；**逻辑分析仪（8ch 100MHz）+ USB 电流表 立刻买**（调 I2C/UART/编码器的刚需） |
| PC：i9-13980HX / 32G / **RTX 4080 Laptop 12G** + Ubuntu 22.04 双系统（ROS2 Humble） | 已有 |
| **F407 + 车载计算（树莓派 5 优先）** | **立刻买**（P0/P1，见 `00` §11）：F407 是 micro-ROS/RTOS 的载体；树莓派装在**已有底板**上（省转接件） |
| **12V→5V DC-DC 降压模块（≥3A）×3** + 电池 ×2 + 充电器 | **立刻买**：⚠️ 12V 电池不能直进开发板，会烧板，见清点表 4.1 |
| （可选）深度相机（D435i / Astra） | 预留 2000，**SLAM 阶段按触发条件再买**（见 `00` §11） |

## 3. 文件位置约定

| 内容 | 路径 |
|---|---|
| VS Code 多根工作区 | `D:\STM32F103C8_Workspace\STM32F103C8_Workspace.code-workspace` |
| 学习工程（标准库版 + 寄存器版，各自自带 `Libraries/`） | `01_PWM_Output(_Reg)`、`02_Input_Capture(_Reg)`、`03_Encoder(_Reg)`、`04_UART_Bluetooth(_Reg)`、**`05_I2C_MPU6050(_Reg)`** |
| **工具工程：套件到货验收（到货当天跑）** | `06_Kit_Check\`（一键自检：LED/按键/I2C/编码器/电机转向/电池） |
| ROS2 侧源码（须在 Linux 编译） | `ros2_ws_src\uart_bridge\` |
| C/C++ 语言笔记（**只收"语言本身"**） | `学习C_C++语言补充\`（01 编译链接、02 static、03 volatile、04 易错点、**05 智能指针/`bad_weak_ptr`**） |
| **工具与环境笔记**（git / apt / SSH / VS Code） | **`工具与环境\`**（01 git 速查与工作流） |
| **STM32 知识笔记** | **各工程自己的 `0X_工程名\README.md`**（11 份；知识点/引脚/验证/常见问题） |
| **ROS2 学习笔记**（Ubuntu 侧） | **`~/ros2_study_myself/学习笔记/`** |
| 成长路线/计划 | 本文件夹 `成长路线\` |
| **鱼香小车硬件清点表（参数/线序/实测步骤）** | `成长路线\01_FishBot硬件清点表.md` |
| **官方固件参数 & STM32 移植映射（权威出处 + 教程章节对照）** | `成长路线\02_FishBot官方固件参数与STM32移植映射.md` |
| **周主题日与学习指引（每日节奏 / ROS2 恢复期 / 打卡表 / **§9 节假日与校历对齐**）** | `成长路线\03_周主题日与学习指引.md` |
| **Ubuntu 侧开工清单（SSH + 国内镜像源 + 编译 uart_bridge + 报错表）** | `成长路线\04_Ubuntu侧开工清单.md` |
| **代码审查与能力盘点（读真源码 + 问题清单 + §0 权威版）** | `成长路线\05_代码审查与能力盘点.md` |
| **RM 考核失败复盘 + 工程精神整改（09-25 · 自省 / 欠账台账 / 周日复核勾）** | `成长路线\09_0925考核复盘与工程精神整改.md` |
| **今日/当周任务单（一次性，照做即可）** | `成长路线\06_0921电控日任务单.md` · `07_0922ROS2日任务单.md` · **`08_0924ROS2日②任务单.md`（当前）** |
| **每日项目制（学完即出一题：三档卡 D/W/P + 项目卡模板 + 收工 10 分钟 + 反模式）** | `成长路线\10_每日项目制.md` |
| **国庆项目卡（P-01 平衡车套装：到货核对 + G0~G2 里程碑与 S 台阶闸门 + 7 天时间盒 + 安全红线）** | `成长路线\11_国庆平衡车项目卡.md` |
| **计划一致性审计 + 按实际进度重订顺序（7 项检查清单 / 15 条问题 / A·B·C 三套顺序 / RM 护栏 / 提交策略）** | `成长路线\12_计划一致性审计与顺序修订.md` |
| **成果转化路线（P-02 卡：小车 → 专利/论文/大创/竞赛；推免加分口径原文 + 资产映射 + 「一鱼四吃」实验 + 倒推 2028-09-01 时间表 + 8 条风险）** | `成长路线\13_成果转化路线_专利论文大创竞赛.md` |
| Keil 库（不随工程） | `D:\Keil\ARM\PACK\Keil\STM32F1xx_DFP\2.2.0\`；CMSIS：`D:\Keil\ARM\PACK\ARM\CMSIS\5.0.1\CMSIS\Include` |
| VS Code 全局 IntelliSense | 用户设置里的 `C_Cpp.default.*`（已配好，指向上面两个 Keil 路径；标准库工程用 `USE_STDPERIPH_DRIVER,STM32F10X_MD`，寄存器工程只 define `STM32F10X_MD`） |

### 3.1 双系统（Windows + Ubuntu）文件布局 —— 重要

**铁律：ROS2 的编译工作空间必须放在 Linux 的 ext4 上，绝不在 NTFS 上 `colcon build`**
（NTFS 的权限位/大小写/软链接/性能都会出问题，git 仓库放 NTFS 也会报权限警告）。

| 内容 | 位置 | 说明 |
|---|---|---|
| STM32 工程 / Keil / 笔记 / 计划 | **Windows D:（现工作区）** | Keil 只能在 Windows |
| ROS2 **包源码** | 工作区里做"源码仓"（如 `ros2_ws_src\<包名>`） | 源码可跨系统，**编译产物不行** |
| ROS2 编译与运行 | **Linux `~/ros2_ws`（ext4）** | `git pull` → `colcon build` 都在这边 |
| 跨系统同步手段 | **git**（W1 建仓库） | Windows 提交 → Linux 拉取；`.gitignore` 排除 `build/ install/ log/` |
| 只想在 Linux 里"看"笔记 | 只读挂载 NTFS（`/etc/fstab` 加 `ntfs3` 或 `ntfs-3g`，带 `ro`） | 用来看，不要用来编译 |

**GitHub 仓库分工（3 个，2026-09-19 定）**：`stm32_study`（本工作区，含**自研** `ros2_ws_src/uart_bridge`）·
`ros2_study`（《动手学ROS2》**教程练习** chapt1~10，**非自研**，README 需注明出处）·
`stm32-ros2-lidar-car`（**比赛作品库**，W5 后建）。详见 `04_Ubuntu侧开工清单.md` §0.1。

Linux 端典型流程：
```bash
cd ~/ros2_ws/src
git clone <仓库地址>            # 或 git pull
cd ~/ros2_ws && colcon build --symlink-install
source install/setup.bash
```

### 3.2 语言约定

- **项目主语言：C++**（ROS2 侧用 `rclcpp`；MCU 侧是 C）—— 已确认，不为项目学 Python；
- **Python 仅作工具语言**：临时读串口、画曲线、批量处理 rosbag 等。需要时用现成脚本、改参数即可，**不作为学习目标**；
- micro-ROS 是 C/C++，与上面一致。

## 4. 代码风格约定（新代码必须遵守）

1. **每个主题出两份**：标准库版 + 寄存器版，各自独立工程（不共享源码）；
2. **注释标注寄存器与位**：如 `TIM3->CCR1 = ccr;` / `/* -> TIM3->CCR1 = ccr */`，并说明位含义；
3. 每个工程带 `README.md`：文件清单、Keil 建工程步骤、Include Paths、Define、引脚表、**验证方法（没有 LED/串口也能验证）**、常见问题、标准库↔寄存器对照表；
4. 中断只做"取数 + 置标志"，计算放主循环；中断共享变量加 `volatile`；模块内部数据/函数用 `static`；
5. 验证优先用：Keil Watch 变量 + 万用表/示波器 + 把信息编码到 PWM 输出；
6. 所有代码进 git（W1 建仓库），参数放配置文件，不硬编码；
7. 引脚约定：避开 SWD（PA13/PA14）与串口（PA9/PA10）。
8. **换行符**：仓库统一存 **LF**（根目录 `.gitattributes` 已配好），Windows 工作区照旧 CRLF ——
   **不用再手动设 `autocrlf`**；改过 `.gitattributes` 后跑一次 `git add --renormalize .` 即可。
   目的：Linux 侧 `colcon build` / `.sh` 脚本不会因 CRLF 报 `bad interpreter` 之类的怪错。
9. **笔记：AI 写全文，你负责验收**（**2026-09-25 v1.28 修订**；原第 9 条是"AI 留空白 + 本人补写"）：
   **正文结构 / 表格 / 图 / 寄存器注解 / 常见坑全部由 AI 写**，但每篇末尾**必须留 3 个手写口**：
   「**一句话复述**」「**我的坑**」「**我的疑问**」—— **AI 不许代填**。
   你的**验收三动作**（5 分钟/篇）：① **不看笔记复述一句话结论**；
   ② **指出至少 1 处**我不准 / 不够 / 你不认同的地方（说"全对"要同时说你**怎么验证的**）；
   ③ 用它**写一行代码**或在一个工程里**真的调用一次**。
   > 理由：分工可以变（笔记可以我写），但**"看懂一次 ≠ 学会"不会变**（`09` §2 #2 的证据）。
   > 所以验收必须从"读一遍觉得对"升级为**可核查的三个动作** —— 否则笔记只是我的，不是你的。
   > 配套：周日复盘 **50min**，其中**至少 20min 用来读笔记 + 填 3 个手写口**（见 `03` §1/§3/§6）。
10. **git 全部本人手敲**（2026-09-20 起生效）：AI 只提供命令 + **只读**的事后验证，
    **不代跑** `init / add / commit / push / branch / mv`。
    > 理由：简历与答辩里"我用了版本管理"要经得起追问 —— 只有手上敲过、踩过坑，才是你的。
11. **工程精神五条**（2026-09-25 新增，源自 RM 第一次考核失败）：
    ① **规格卡先行**（通道数 / 频率 / 引脚 / 判据 / 证据清单 —— 模板见 `05` §9.5）；
    ② **意图 ↔ 配置对照表**（一句意图对应哪一行配置/寄存器，`F12` 能指到）；
    ③ **`0 Error` 只是入场券**：每个工程收尾必须有**一条现象判据**（Watch 数值 / 波形 / 照片），否则不算完成；
    ④ **数值先算 / 先仿真再下结论**（`PSC/ARR` 的 `+1`、相位下标、量程 —— 与 `05` §9.2 C① 同源）；
    ⑤ **结构三件套**：主循环不阻塞 / 初始化职责明确 / 状态集中在一个变量里。
    > 完整版（六条动作 + 欠账台账 + 周日复核勾）见 **`成长路线\09_0925考核复盘与工程精神整改.md`**。
12. **每日项目制**（**2026-09-25 新增**：学完即出一题 —— 用户要求"每天学习之后都出一个项目给我完成"）：
    ① **每天学习结束后，由 AI 出一张「项目卡」**，**你独立做完并交一条现象判据**才算出卡闭环；
    ② 三档：**D（日卡，30~90 min）/ W（周卡，＝规格卡）/ P（窗口卡，国庆/寒假）**，**在途只允许 1 张**；
    ③ **收工前 10 分钟**加四个动作：写一行结论（含计算式）→ **跑一条判据** → 改台账 → 写"明天第一件事"；
    ④ **卡里不附实现代码**（防止绕开"结构 / 数值 / 判据"三层）；卡死 >45 min 才允许要提示，且必须记"卡在哪"；
    ⑤ **拦截条款**：一周只准开 **1 个新知识点**，且必须写"约束来源"（`00` §16.4 第 7 条）。
    > 完整版（三档定义 / 卡模板 / 反模式 7 条 / 8 条建议 / 台账）见 **`成长路线\10_每日项目制.md`**；
    > 第一张 `P` 卡 = **`成长路线\11_国庆平衡车项目卡.md`**（国庆 10-01~10-07）。

## 5. 当前进度（**每次做完就更新这一节**）

| 周 | 时间 | 状态 | 产出 |
|---|---|---|---|
| W1 | **09-20 ~ 09-26** | ✅ **已收工（W1 达标）** | **A 线**：✅ **本地仓库已建 + 首次提交 `484d3d0`**（752 文件 / 561 KB / 200 唯一对象；身份 `HzEgw <1765377619@qq.com>`，2026-09-19 20:23）；✅ **已推到 GitHub：`https://github.com/HzEgw/stm32_study`**（2026-09-19 20:34 首次推送；随后**改走 SSH 免密、不再依赖代理**，梯子可关。仓库**保持公开** —— 队员直接 clone 即可）。鱼香小车参数已入账（`01` / `02`，官方值 40.5 / 44 / 172.75），剩"实读确认"。**R 线**：环境体检（09-21 实测 `bash_history` 里有 **59 条 `ros2` 命令** → W1 的"命令肌肉记忆"已达成，09-22 直接做 W2 的**参数 / launch / bag**）。**制度**：主题日已启用（`03`）<br>✅ **09-21（周一 · 电控日①，~2h）**：**自己新建 Keil 工程 + 手写三路 PWM**（TIM2 **CH2/CH3/CH4 → PA1/PA2/PA3**）+ 工程 README（含示波器照片）+ **提交 `6039ee9`**（`stm32_study_myself\01_pwm_out_project_keil`；审读见 `05` §8：7 正面 / 2 P0 / 4 P1）；待修项（`PSC/ARR → 719/99` 等）顺延到 **09-23**；日更打卡见 `03` §6<br>✅ **09-25（中秋假 · 电控日）**：① **A 线** `w1_ic` **双定时器重写完成**（` -正确版`：TIM2_CH2/**PA1** 出 1 kHz → 跳线 **PA1↔PA6** → **TIM3_CH1** 从模式复位；LED 判据 **900~1100 Hz**；`0E/0W`；审读 `05` §8.10）；② ⚠️ **RM 第一次考核代码失败**（`stm32_study_myself\rm战队第一次考核\流水灯控制`：原版**能编译却运行时卡死在波浪灯** → 4 坑 / 23 warning；已留档 + 重构版 + 闭卷计划；审读 `05` §8.11）<br>　→ **自省落盘：`成长路线\09_0925考核复盘与工程精神整改.md`**（"工程精神"六条动作 / 欠账台账 / 周日复核勾），并新增 `README` §4 第 11 条、`00` §16.4 第 7 条；③ 提交 `b20084d2`；④ **22:32 下单「平衡车入门套件 · 套餐1（平衡车套件 + 遥控器）」**（截图 9 项清单）→ 归位为**国庆项目卡 P-01**（`11`）；⑤ 新增 **`10_每日项目制.md`**（每日出卡机制 / 笔记分工 v1.28 / 反模式 7 条 / 8 条建议）与 **`11_国庆平衡车项目卡.md`**（G0~G2 与 S 台阶闸门 / 7 天时间盒 / 安全红线）——**主题日与钟点一字未动**；⑥ **定 B 方案**（顺序重订：W2=S1 只清四路+5kHz / W3=S2 / W4=S3 / **W5=S6 UART 提前** / W6=S5 速度环 → `00` §14 已重写、`00` §13 步骤 1 改 W5~W8、`05` §9.5 范围修订），**四个开关全同意**；⑦ **平衡车确认为"成品、无需组装"** → `11` 新增 **§1.1 成品车专项**（反向拆解 + 禁止拿出厂 demo 当成果 + 电池月补电）；⑧ **一致性审计上线**：`12`（7 项检查 C1~C7 / 15 条问题 / 已修 9 处 / RM 护栏 / 提交策略），并新增 `10` §8 **反模式 #8**。<br>✅ **09-26（周六 · W1 最后一天 · RM 考核交付日）**：① **闭卷重做交卷并上板** —— `rm战队第一次考核\流水灯第二次尝试\`（`led_test.uvprojx`；5 处修复全对、全量 `0E/0W`、`Code 4044→4104`）＋ **3 条现象闭环**（按下即切 = 判定窗 **640 ms → 40 ms**／流水灯 **100 ms 一盏**／两模式主循环 ≈**50 Hz**）⇒ **`10` §7 `D-0926-01` 改判「已交」**（呼吸灯在第 1 个工程里本来就有，**2/2 效果齐**）；审读 `05` §8.12（8 子节）；② 🔴 **本人用示波器抓到新真问题 `P1-2`**（`PWM_Init()` 每 20 ms 重跑 ⇒ `mode 1` 频率瞬跳 **1.2 kHz**）⇒ **09-27 开工前 5 min 修**（4 行 `last_mode` 守卫；今天不修 = 疲劳裁决，**不阻塞任何结案**）；③ 🛠 **陪练模式生效**（AI 只给判据、不贴代码）＋ `05` §8.12.9 / §8.12.10 落盘；④ 🧾 **09-26 当天 12 次 `docs:` 提交**（`0c77d0c` 13:27 → `ab2c777` 14:11）**全部已推送**、**工程源文件零改动**；⑤ ⏳ **W1 未闭合（顺延）**：工程 `README` 三要素 / `09` §7.2 的 W1 三个数（周日手写）/ D-0926-02 实物照片·录屏 / `流水灯第二次尝试\` 65 文件入库 |
| W2 | **09-27 ~ 10-03**（🎑 09-27 假 · 🇨🇳 10-01~10-03 假） | 待开始 | **S1 PWM 输出（v1.7 修正版）**：`TIM3` **四路 5 kHz**（对齐官方电机双 PWM）→ `TIM1` **互补 + 死区**；**寄存器版由本人重写**（`01_PWM_Output_Reg` 只作对照答案）；规格卡 = `05` §9.5；平衡车套件到货 → **`11` G0**（`06_Kit_Check` 自检）<br>⚠️ **开关 ①（`12` §5）待定**：若采纳 B 方案 → 本格只清 **四路 + 5 kHz**，互补死区改性质 |
| W3 | **10-04 ~ 10-10**（🇨🇳 10-04~10-07 假 · 🏫 10-10 补班） | 待开始 | **S2 编码器 + 开环调速**：`03_Encoder(_Reg)` 编码器接口模式 + 用 PWM 驱动电机正反转；**国庆窗口执行 `11` 的 G0~G2**（组装 → 双电机开环 → 编码器测速）；**周末验收顺延到 10-11(日)** |
| W4 | **10-11 ~ 10-17** | 待开始 | **S3 输入捕获 + SR04**：`02_Input_Capture(_Reg)` PWM 输入模式（捕自己发的 PWM，频率/占空比）＋ SR04 实战（⚠️ ECHO 5V 需分压）；**B 线**：一页选题书 + 给老师第一封邮件 + 问一次报名流程（`05` §9.4 风险②） |

> 🔧 **2026-09-25 修订（重要 · 用户指出）**：上面 **W2~W4 三行已被重写**，与 `00` §14 的台阶（**W2=S1 / W3=S2 / W4=S3**）对齐。
> **原三行是 09-20"起点更正"之前的旧内容**（旧：W3 = ADC+I2C+平衡车验收、W4 = UART+直立环；**ADC=S4、I2C=S7、UART=S6，物理上不可能排在 W3/W4**）——
> 当时**只加了下面那段说明、没有重写行**，于是"说明说改了、行里还是旧的"，这处矛盾一直留到 09-25 被你自己发现。
> 审计过程与改法见 **`成长路线\12_计划一致性审计与顺序修订.md`** §0 / §2（#1/#2/#5/#11）；
> **我的教训已写成反模式**："**改顺序必须重写所有引用它的表行，只加注 = 没改**"（`10` §8 反模式 #8）。
>
> ⚠️ **2026-09-20 起点更正（原注保留）**：原判"PWM/输入捕获/编码器已手写双版本"是把 **AI 备好的参考工程**
> 当成了本人能力；实情是 **PWM 输出 / 输入捕获尚未吸收、ADC 未学**。
> 故 W2~W4 的电控日顺序已调整（详见 `00_8个月学习计划.md` §16.8 的 **v1.3** 变更日志）。

**W1 的 git 任务：✅ 已完成（2026-09-19 20:34）** 仓库：`https://github.com/HzEgw/stm32_study`
日常四连就够了：
```powershell
git status ; git add . ; git commit -m "feat: xxx" ; git push
```
✅ **本机网络结论（2026-09-19 定案）**：`github.com:443` 被墙，但 **22 端口（SSH）通** →
git 已改为 **SSH 免密**，**完全不依赖代理**：`origin = git@github.com:HzEgw/stm32_study.git`
**梯子可以关**（关了照样 `push`/`pull`）；只在你要访问其它墙外站点时才需要它。
日常四连（照旧，只是不再走代理）：
```powershell
git status ; git add . ; git commit -m "feat: xxx" ; git push
```
> 网络/密钥的排障细节见 `工具与环境\01_git速查与工作流.md` **§4.1**（443 被墙排障 + Windows 配 SSH 密钥的三个坑）。
> **仓库分工**：`stm32_study` = **学习工作区**（现在这个：练习工程 + 成长路线文档）；
> `stm32-ros2-lidar-car` = **比赛作品库**（W5 之后另建，只放 F407 固件 + ROS2 包，避免和练习代码混在一起）。
**09-19（周六）晚热身已完成**：`git init` + 首次提交 **`484d3d0`**（752 文件；身份已改为 `HzEgw <1765377619@qq.com>`）—— 不计入 KPI，但原排在 09-20 的第一件事**提前一天做完了**。

**⏳ 时间纪律（重要，写给自己看）**：**09-20（周日）= W1 第 1 天**，**W1 只交付两件事：git 上手 + GitHub 仓库**。
下面这些**现在都不做**（已经排好队写在文件里了，到周再动手）：

| 事情 | 排到哪一周 | 为什么不是现在 |
|---|---|---|
| **每日怎么过（主题日）** | **今天就开始**（`03_周主题日与学习指引.md`） | 它是"节奏"，与周次无关：周一/周三电控、周二/周四 ROS2、周五归档、周末攻坚+产出 |
| 读"本车实读参数"（`read_config`） | **W1 周末 / W2**（车在手边时顺手 30 分钟） | 不阻塞任何事：参数已有官方默认值可用（`02_…` §2.1） |
| 万用表量编码器供电、6P 线序、ECHO 分压 | **接线之前**（编码器＝W3/国庆 **G0**；SR04＝W4） | 现在不接线，测了也用不上 |
| 教程第 13~16 章通读 | **W2 起，每周 1 章** | 一次读完会消化不良 |
| `06_Kit_Check`（套件自检） | **到货当天**（= `11` G0） | 自检就是为了分清"硬件坏还是软件坏"，越早跑越省时间 |
| `05_I2C_MPU6050(_Reg)`（I2C 读 MPU6050） | **到 S7 才用**（约 W7，见 `12` §3.2） | ⚠️ **I2C = S7**，不是 W3 的内容（旧版写过"到 W3 直接用"→ 已修） |

**我这边（Cline）的准备工作已覆盖到 `S7`**（I2C 工程、`06_Kit_Check` 也已就位）：F103 双版本工程、ROS2 桥、参数文档、git 速查、套件验收工程；
⚠️ **但 2026-09-20 起：git 一律由本人执行**（Cline 只给命令 + 只读验证，不代跑）——见第 4 节第 10 条；
**你需要动手的第一件事仍然是 git**（`工具与环境\01_git速查与工作流.md`）。

**主线学习资源（已定位）**：《动手学ROS2》**第 16 章 "FishBot 控制系统搭建"**（16.7 编码器测速 / 16.8 脉冲校准 / 16.9 最大速度 / 16.11~16.14 运动学与里程计）
—— https://fishros.com/d2lros2/#/humble/chapt16/章节导读 （ESP32 版；你的任务是**用 STM32 重做**，见 `02_…` 第 7 节映射表）。

## 6. 上下文管理建议（省时间也省钱）

- **一个话题一个新会话**：写代码 / 做规划 / 问概念 分开，避免上下文互相污染；
- 每次开会话先让我读本文件 + 计划文件；有进展就先让我**更新第 5 节**；
- 重要结论必须落盘（进本文件夹或对应 README），**不要只留在对话里**；
- 长期资料放 D 盘，`%TEMP%` 里的临时脚本可随时删。
