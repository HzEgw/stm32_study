# 00 · 8 个月学习计划（v1.2）

> **起止**：**2026-09-20（周日）→ 2027-05**（覆盖 2027-03 挑战杯校赛、2027-05 省赛）
> **周制**：**每周从周日算起、到周六结束** —— W1 = **09-20(日) ~ 09-26(六)**，之后每周 +7 天
> **时间预算（v1.11 · 按真实课表重算）**：**基准 25 h/周**（周一 2.5 ｜ 周二 2.5 ｜ 周三 3 ｜ 周四 3 ｜ 周五 5 ｜ 周六 4 ｜ 周日 5）+ **冲刺档 30 h/周**；假期另算（中秋 3 天 ≈ 12h、国庆 7 天 ≈ 35h）
> 说明：**09-19（周六）晚只做"看一眼文件"**（读 README / 03 / 建目录），**不计入 KPI**；正式计时从 **09-20** 开始。
> **本文件是"活文档"**：每两周打卡一次，答案填在 §9 打卡表；每月初回看 §3 里程碑。

## 📚 配套文件（5 个文件的分工 —— 别混着看）

| 文件 | 管什么 | 什么时间看 |
|---|---|---|
| `README.md`（接续指南） | 换会话怎么快速接上、代码风格约定、进度表 | **开新会话第一件事** |
| **`00_8个月学习计划.md`（本文件）** | 8 个月到哪一步：里程碑 / 逐周计划 / 预算 / 风险 | 每周日复盘、每月初 |
| `01_FishBot硬件清点表.md` | 车上有什么、运动学参数、实测步骤、采购影响 | 涉及硬件时 |
| `02_FishBot官方固件参数与STM32移植映射.md` | 官方参数权威值、ROS2 接口、ESP32→STM32 映射表 | 写固件 / 对齐话题时 |
| **`03_周主题日与学习指引.md`** | **每天怎么过**：主题日、时间盒、ROS2 恢复期路线、打卡 | **每天开工前 10 分钟** |
| **`04_Ubuntu侧开工清单.md`** | **Linux 侧怎么开工**：SSH 密钥、国内镜像源（免梯子）、编译 `uart_bridge`、报错表 | **在 Ubuntu 上干活前** |
| **`05_代码审查与能力盘点.md`** | **代码审查 + 能力盘点**：读了你 8 个自建工程的源码，逐条结论 + 问题清单（P0/P1/P2）+ §0 权威版 + S 台阶调整 | **动手改代码前 / 每月盘点** |

---

## 目录

| 节 | 标题 | 用途 |
|---|---|---|
| §0 | 自我盘点 | 我知道什么、缺什么 |
| §1 | **起点与终点（一页速览）** | 只看一页就看这节 |
| §2 | 两条主线 + 一条恢复线 | 战略：只做这些 |
| §3 | 月度里程碑 | 每月要交什么 |
| §4 | **每周节律：主题日制度** | 每日节奏（详见 `03_…`） |
| §5 | 关键时间节点 | 盯死报名/比赛窗口 |
| §6 | 硬件预算（历史稿） | ⚠️ 已被 §11 取代 |
| §7 | Plan B | 校赛没过怎么办 |
| §8 | 确认状态（已确认 / 待办） | 未定事项清单 |
| §9 | 打卡表 | 每两周填一行 |
| §10 | 团队与协作 | git 流程、分工 |
| §11 | **预算分配（钱管够版）** | 钱怎么花 |
| §12 | 敲门砖计划 | 破"没导师"的局 |
| §13 | micro-ROS 专项路线 | 项目的技术骨架 |
| §14 | 前 6 周逐周计划（W1~W6） | 近期执行 |
| §15 | 鱼香小车情报 | 参考资料（已归档到 01/02） |
| §16 | 自审修订 + 不做清单 / DoD / 风险 / 变更日志 | 附录 |
| §17 | **8 个月后我能到什么程度（实话版基线）** | 每月初拿来照镜子 |


---

## 0. 自我盘点（2026-09-19 更新，据本人回答整理）

| 维度 | 现状 | 判定 |
|---|---|---|
| STM32 | 能不看教程写 GPIO / 定时器中断（有印象，需巩固）；**只用标准库**，没用过 HAL+CubeMX；**FreeRTOS 仅"了解"，没有实际写过**（`bash_history` 与 D 盘工程里都无痕迹）；NVIC 优先级（抢占/子优先级）清楚 | **⚠️ 2026-09-20 四轮更正（**v1.8 · 以本人自述为准**）：当前进度 = **江协科技教程刚学完第 6 章（PWM）**。**会**：GPIO / 按键 / OLED / **单路 PWM（`TIM2_CH1` 1kHz 呼吸灯，标准库，示波器做过）** / 定时器中断 + NVIC 优先级 / ETR 外部计数 / Keil 自建工程；**半成品**：第 5 章外部中断（`5-1` 没写完）；**未学过**：多通道 PWM、TIM1 互补死区、输入捕获、编码器接口模式、**ADC**、DMA、**UART**、**I2C**、SPI、FreeRTOS、HAL/CubeMX。<br>⚠️ 工作区 `01~06(_Reg)` 11 个工程是 **AI 参考工程**（含 I2C/UART 等你还没学的内容）→ **不代表你的水平**，是给你将来照着学的。<br>证据：`Desktop\keil_program\` 8 个自建工程 + 本人自述（2026-09-20）→ 详见 `成长路线\05_代码审查与能力盘点.md`。<br>⚠️ 工作区 `01~05(_Reg)` 双版本工程是 **AI 准备好的参考代码**（工程 README 原文："Keil 工程由你自己新建…代码已经写好"）→ **≠ 本人已掌握**。<br>✅ **2026-09-21 更新**：**多通道 PWM 已自己写出** —— `stm32_study_myself\01_pwm_out_project_keil`（自建 Keil 工程，TIM2 **CH2/CH3/CH4 → PA1/PA2/PA3** 三路；待修 PSC/ARR 与延时，见 `05` §8）；<br>仍缺：**TIM1 互补死区** / 输入捕获 / **ADC** / UART / I2C / SPI / DMA / **FreeRTOS** / HAL+CubeMX / CAN（可选） |
| 硬件动手 | 会基础焊接、能看简单原理图、用过示波器；**无逻辑分析仪** | 够用；**逻辑分析仪（8ch 100MHz）+ USB 电流表 立刻买**（v1.5：调 I2C/UART/编码器的刚需） |
| ROS2 | Ubuntu 22.04 双系统；C++ 为主；自定义 msg / 节点 / 话题 / launch / 参数**都会**；rviz2、rqt、tf 用过；**已跑通 ros2_control + Gazebo 双轮差速仿真**；1.5 个月没碰，需要恢复期；**git 已上手**（W1：`init/commit/push` + SSH 免密 + 分支/远程） | **强于同届大多数**：ros2_control 不是入门内容。<br>✅ **2026-09-20 核查通过（有硬证据）**：自定义接口看 `~/ros2_study/chapt4_ws/src/chapt4_interfaces/srv/*.srv`；仿真看 `~/fish_ws/src/fishbot-humble/.../fishbot_gazebo.urdf` + `gazebo.launch.py`；命令面看 `bash_history`（59 条 `ros2` + `rqt`/`rviz2`）。<br>⚠️ 但"会"应理解为"**照教程做过**"，**从零自写**留待 W2~W4 验证 |
| 深度学习 | 零基础；4080 从未用于学习 | 从零起步，用 4080 做小模型 |
| 已有硬件 | **雷达避障小车（B站鱼香ROS 款）**、F103C8T6 + ST-Link、可用示波器 | **关键**：已有雷达底盘 = 项目起点，不必从零攒车 |
| 时间 | **基准 25 h/周**（v1.11：周一 2.5 / 周二 2.5 / 周三 3 / 周四 3 / 周五 5 / 周六 4 / 周日 5）＝ **≈ 100 h/月**；冲刺 30 h/周 | **充裕**（8 个月 ≈ **800~1000 h**）；关键是**可持续**，不是单周爆发 |
| 团队 | 机械 1 人（SolidWorks）+ 文档/PPT 1 人 | 够组建 3 人小队 |
| 资金 | **钱管够**（v1.5）：P0/P1 **立刻买**（平衡车套件由 09-29 提前到 W2）；原"11 月 9000"按 §11 分配 | 够；**工具与安全件不再省**（省工具 = 花时间）；按 §11 优先级花 |
| 导师/场地 | **几乎没有导师**；职院实验室不让用；无背景；**绩点第一** | **最大短板 → 用"敲门砖"策略破局（见第 12 节）** |
| 定位 | 想当"电控 ↔ ROS2 的桥" | **判断正确**：micro-ROS 全栈是差异化最大的路径 |

### 关于"大一沉进 DDS 理论"这件事

**不是浪费，是押错了顺序**：DDS 是 ROS2 的地基，但你在大一先啃地基、却对"下位机"完全陌生，于是出现了"上面会、下面虚"的落差感。
从现在起改成这个顺序：**下位机能力（MCU/通信/控制）→ 桥（micro-ROS）→ 上层应用（SLAM/Nav2/视觉）**。
DDS 理论**不再深挖**，够用即可：QoS 三档（reliable/best_effort/transient_local）+ 发现机制概念 + 用 `ros2 topic hz/info` 观察一次就够。

---

## 1. 起点与终点（一页速览）

> 只记这一节也能活：**起点 → 终点 → 每周怎么走 → 三条铁律**。

| 项 | 内容 |
|---|---|
| **起点（2026-09）** | STM32F103 标准库+寄存器（⚠️ **2026-09-20 更正**：PWM 输出 / 输入捕获**尚未吸收**、**ADC 未学**；`01~05` 双版本工程是 AI 备好的**参考代码**、Keil 工程待自建）；ROS2 入门偏上（ros2_control+Gazebo 跑通过，需恢复期）；F103C8T6 + ST-Link + 示波器；**一辆鱼香 FishBot（ESP32-S3 + micro-ROS）**；PC 有 RTX 4080 Laptop |
| **终点（2027-05）** | ① 一台能跑的农业场景**巡检/避障小车**（下位机自研 STM32 + micro-ROS）② 挑战杯校赛通过 → 省赛 + 大创 ③ **公开作品包**（GitHub 仓库 + 可复现 README + 演示视频 + 技术长文） |
| **每周节奏** | 周一/周三 电控日 · 周二/周四 ROS2 日 · 周五 归档+联调日 · 周六 攻坚日 · 周日 产出日（**基准 25h**；钟点与目标小时见 `03` §1 / §10.3） |
| **铁律五条**（2026-09-20 扩） | ① 每个学习块**收尾必落盘**（文件/commit/截图）② **周日必复盘 50min**（三问 + 打卡表 + **读 AI 笔记补空白**）③ **不熬夜**（**23:00 前收工**；25h 基准／30h 冲刺）④ **AI 提炼的笔记只是草稿**：留空白 + 本人过一遍补写 ⑤ **git 一律本人手敲**（AI 不代跑） |

**起点（2026-09）**
- STM32F103 标准库 + 寄存器：⚠️ **2026-09-20 更正** —— `01_PWM_Output(_Reg)`、`02_Input_Capture(_Reg)`、`03_Encoder(_Reg)`、`05_I2C_MPU6050(_Reg)` 等**双版本工程是 AI 提供的参考代码**（工程 README 原文："代码已经写好，Keil 工程由你新建"），**本人尚未吸收** PWM 输出 / 输入捕获 / 编码器，**ADC 完全没学** → 列为 **W2~W3 电控日的首要任务**
- 计算机基础：PC 有 RTX 4080 Laptop（12GB），适合做视觉训练
- ROS2：入门级（待用下面的问题确认到哪一步）
- 硬件：F103C8T6 核心板 + ST-Link；无 F4/H7 板；平衡车套件待购

**终点（2027-05）—— 三个成果，缺一不可**
1. **一台能跑的原型**：F407/H743 + 激光雷达 + (可选)视觉 的"农业场景巡检/避障小车"，MCU 通过 micro-ROS 接入 ROS2；
2. **一份能拿出去的比赛材料**：挑战杯校赛（2027-03）通过 → 冲省赛；同时申报大创（2027-03~04）；
3. **一套公开作品包**：GitHub 仓库 + 可复现 README + 演示视频 + 5 篇技术笔记（这是独立于比赛的简历资产）。

---

## 2. 两条主线 + 一条恢复线（别贪多）

| 线 | 内容 | 为什么 |
|---|---|---|
| **A｜STM32 技术主线（台阶式 S1~S11，每 1~2 周一个可演示台阶）** | **S1~S7 在现有 F103 上打底**（PWM 输出 → 编码器+开环调速 → 输入捕获+SR04 → ADC → 速度环 PID → UART+环形缓冲 → I2C IMU/OLED）→ **S8 FreeRTOS 入门** → **S9 F407 + HAL/CubeMX + micro-ROS** → **S10 整车联调 + 对比实验** → S11 校赛打磨 | 这是"能打比赛、能进实习"的硬通货；**台阶式**保证"每两周有能演示的产出"（对上 `03` §1 的铁律）。<br>⚠️ **CAN/RM 状态（v1.24 更新）**：**RM 探路已启动** → **CAN 恢复为"必学"**，但**位置不变**（W11+，严格按 `S1→S11` 顺序，**不提前**）；本项目本身仍用**直流电机 + PWM** |
| **B｜成果主线** | 选题调研 → 找指导老师 → 组队 → 作品化（报告 + 实物 + 视频 + 数据）→ 报名参赛 | 技术再强，没有"作品载体"就进不了比赛 |
| **R｜ROS2 恢复线**<br>（W1~W4，**7h/周**） | 环境体检 → 命令/节点/包 → 参数 + launch + bag → 自定义接口 + TF → **Gazebo 回炉** | ⚠️ **你 1.5 个月没碰 ROS2**；不恢复就接不上 micro-ROS 主线（逐周安排见 `03_周主题日与学习指引.md` §4） |

> **两条主线 + 一条恢复线**：A 是主线，B 把 A 变成成绩，**R 把 ROS2 手感恢复到"能接项目"的状态**（W5 起并入 A）。
> 其余（RM 以外的项目、换方向、追新框架）一律暂缓；**蓝桥杯已放弃（v1.24）** —— 时间集中到 **RM 探路 + micro-ROS 小车** 两条线（不再有第三件事分心）。

---

## 3. 月度里程碑

| 月份 | 主线 A（技术） | 主线 B（成果） | 本月产出物（可演示/可提交） |
|---|---|---|---|
| 2026-10 | **F103 台阶 S1~S4 + S5 起步**：PWM 输出（TIM3 四路 + TIM1 互补死区）→ 编码器 + 开环调速 → 输入捕获（PWM 输入模式）+ SR04 → **ADC（电池电压）** → 速度环 PID 起步<br>⚠️ **本月不碰** UART / I2C / SPI / DMA / FreeRTOS / F4（见 §16.8 v1.5） | 选题调研（农业场景+机器人）；锁定 1 个题目；**联系指导老师**；拉 2 位队友 | 示波器波形图 + 编码器读数（1782/圈）+ SR04 测距 + ADC 电压读数 |
| 2026-11 | **S5 速度环收尾 + S6 UART（中断+环形缓冲）+ S7 I2C（MPU6050+OLED）**；B 线并行：用**现有 ESP32-S3** 跑通 `micro-ros-agent` 端到端 | 写立项/选题报告初稿；查 10 篇相关文献（技术调研表） | 转速跟随数据（曲线）+ 串口上报曲线 + 姿态角显示 + micro-ROS 节点数据 |
| 2026-12 | **S8 FreeRTOS 入门**（任务/队列/信号量 + **静态创建**）+ **S9 起步**（F407 + HAL/CubeMX + `micro_ros_stm32cubemx_utils`） | 申报书骨架；确定作品创新点表述 | F103 三任务并发 + F407 上 micro-ROS 最小节点 |
| 2027-01（寒假） | **S9 收尾 + S10**：micro-ROS 打通 F407 ↔ ROS2；雷达/IMU/电机接入；底盘装配；建图 → 自主避障；跑通"从 A 到 B" | 研究报告目录 + 实验设计（对比实验怎么设计） | 小车自动避障 demo v1（视频）+ 一个 launch 启动整车 |
| 2027-02 | **S10 收尾 + 对比实验**（官方固件 vs 自研固件）+ 视觉小模型（4080 训练/微调）集成 | 报告初稿 + 演示视频 + 答辩 PPT | 原型 v2 + 对比数据表 + 3 分钟演示视频 |
| 2027-03 | **S11 稳定性打磨** + 采集实验数据（重复性/成功率/耗时） | **挑战杯校赛报名/答辩**；大创申报 | 校赛材料提交 + 答辩 |
| 2027-04 | 省赛打磨；补充对比实验 | 省赛材料；论文/报告定稿 | 省赛参赛 |
| 2027-05 | **RM 线按判据执行（v1.24）**：**2026-12-31 前已联系到可用战队** → 按战队节奏参赛/备赛；**否则不做**（判据见 §16.8 v1.21 ③ + v1.24 ④） | 开源发布（仓库+README+笔记） | 复盘文档 + 公开作品包 |

---

## 4. 每周节律：主题日制度（**基准 25h/周 · 冲刺 30h**）

> **本节只给"骨架"；逐日细节（时间盒、ROS2 恢复期路线、打卡表、降级方案）在 → `03_周主题日与学习指引.md`。**

| 天 | 时长 | 主题日 | 主任务 |
|---|---|---|---|
| 周一 | **2.5h** | 🔧 电控日 ① | STM32 主线：写驱动 / 调硬件（**19:00-21:40**） |
| 周二 | **2.5h** | 🐢 ROS2 回炉日 | R 线：命令 → 节点 → 包（**19:00-21:45**） |
| 周三 | **3h** | 🔧 电控日 ② | STM32：读手册 → 写代码 → Watch/示波器验证（**14:00-17:00**，稳定档期） |
| 周四 | **3h** | 🐢 ROS2 动手日 | R 线：写/改包，与 MCU 数据对接（**14:30-17:30**） |
| 周五 | **5h** | 📦 归档 + 联调 | 上午 **08:30-11:30** 笔记/git/问题清单；下午 **14:00-16:00** 跨线联调 |
| 周六 | **4h** | ⚒️ 攻坚日 | **14:00-18:00** 整段不被打断：搭环境 / 联调 / 做实验 |
| 周日 | **5h** | 🎯 产出日 | **09:00-11:40 + 14:00-16:30**：2.5h ROS2 + 2.5h 联调 + **50min 复盘归档** |

**25h 分配**：STM32 **13h** ｜ ROS2 **7h**（W5 起降为 4~5h）｜ 归档 **2h** ｜ 联调/实验 **3h** + 复盘 **50min**（含读笔记补空白）。
>
> 📅 **课表实测（2026-09-20 解出，详见 `03` §10）**：**第 4-13 周（09-21~12-13）周五全天无课** ⭐⭐、**第 7 周起周六上午有课**、**第 8-13 周周三晚上有课**、**第 14-19 周周五下午有课**、**周日全天无课** → 主题日**不再固定"周一/周三晚"**，具体落点按 `03` §10.3 的"可用时段表"执行。

**铁律：每两周必须有一个"能演示给外人看"的小产出**（能跑的视频 / 能看的数据 / 能复现的 README 段落）。
没有产出就走不到 3 月校赛——这不是能力问题，是节奏问题。

---

## 5. 关键时间节点（提前标在日历上）

| 时间 | 事件 |
|---|---|
| **2026-09-20（周日）· 10-10（周六）** | **补班日（官方调休，要上课）** —— 这两天**不要排 5h 整段任务** |
| **2026-09-25 ~ 09-27** | **中秋假 3 天**（官方）→ 轻量任务 + 读笔记补空白（`03` §9.3） |
| **2026-10-01 ~ 10-07** | **国庆 7 天连休**（官方）→ ⭐ **开学后最大整段窗口**：S2 编码器 + 开环调速 + 平衡车套件组装（`03` §9.3） |
| 2026-10（W4） | **摸清挑战杯报名流程**（学院/校团委：报名时间、材料截止、是否需指导老师签字）← 新增，**别等到 3 月** |
| 2026-12 前后 | ~~RM 报名/规则发布~~（⚠️ **2026-09-20 降级为"可选"**：本项目用**直流电机 + PWM**，不需要 CAN；若你仍想打 RM，**再单独评估、不许占用主线 9h**） |
| 2026-12 ~ 2027-01 | **挑战杯报名窗口（很多学校在这段时间截止，务必确认）** |
| ⚠️ **2027-01-11 ~ 01-17（第 20 周）** | **期末考**（依据：**课表已解出**，课程排到第 19 周 01-04~01-10）→ 12 月只做 F103 侧小步，见 `03` §9.4 / §10.3 |
| **2027-01 中 ~ 02 月下** | **寒假（约 5~6 周无课）= 校赛前最大整段** → **F407 + micro-ROS 重头戏放这里**（`03` §9.4） |
| 2027-03 | **挑战杯校赛**（大挑是奇数年，2027 正好是大挑年） |
| 2027-03~05 | 大创申报（**以学校通知为准**，11 月就主动问教务/团委） |
| 2027-04~05 | 挑战杯省赛 / 湖南省职业院校技能大赛 / RM 分区赛 |
| 2027-07~08 | 全国大学生电子设计竞赛（若参加，通常 5~6 月报名） |

---

## 6. 硬件预算（约 1200~2000 元）—— ⚠️ 历史稿

> **本节是 v0.1 的初稿估算，已被 §11「9000 元预算分配」取代**（那份按"到账时间 + 优先级"重排过）。
> 留档原因：单件价格区间仍有参考价值（逻辑分析仪 30~60、雷达 300~400、电机+驱动 200~400 …）。

| 项目 | 估算 | 备注 |
|---|---|---|
| F407 或 H743 开发板 | 150~250 | RM 与实习主流；**优先买这个** |
| 激光雷达（LD19/LD06 等） | 300~400 | 避障/建图核心 |
| 电机 + 驱动（编码器直流减速电机 / M3508+C620） | 200~400 | 先用直流减速电机练手，再上 RM 电机 |
| 底盘 + 电池 + 充电器 | 200~300 | 电池**必须买新的** |
| IMU（MPU6050/ICM20602） | 30~80 | — |
| 8 通道逻辑分析仪 | 30~60 | 调 I2C/串口救命，性价比最高 |
| 杂项（杜邦线、铜柱、焊锡、备件） | 100~200 | — |

---

## 7. Plan B（校赛没过怎么办）—— 永远不要 all-in 一个比赛

| 备选抓手 | 说明 |
|---|---|
| 大创 / 挑战杯小挑 / 工创赛 | 用同一台小车换个"作品包装"就能报 |
| **湖南省职业院校技能大赛**（嵌入式/机器人方向赛项） | 职院体系最高含金量，学院通常有集训，**你的主场** |
| 世界技能大赛"移动机器人"项目（国内选拔） | 技术栈与 ROS/机器人高度相关（以当年赛项指南为准） |
| 全国大学生电子设计竞赛（专科组/本科组） | 你身边前辈正在打这个，可以蹭经验 |
| 开源作品 + 技术博客 | 独立于比赛：面试时"陌生人能复现你的仓库"最有说服力 |

---

## 8. 确认状态（2026-09-19 已更新）

**✅ 已确认**（明细见第 0 节「自我盘点」）

- **STM32**（⚠️ 2026-09-20 更正，详见 §0）：GPIO / 定时器中断**有印象、需巩固**；只用标准库（无 HAL/CubeMX）；**FreeRTOS 仅"了解"（无实际工程）**；**PWM 输出 / 输入捕获 / 编码器 / ADC 尚未掌握**；NVIC 优先级清楚；会基础焊接、看简单原理图、用过示波器（**逻辑分析仪立刻买**）
- **ROS2**：Ubuntu 22.04 双系统 + Humble；**C++ 为主**；自定义 msg / 节点 / 话题 / launch / 参数**（照教程做过）**；rviz2/rqt/tf 用过；跑过 ros2_control + Gazebo 差速仿真；**git 已上手**（W1 完成 `init/commit/push`+SSH+分支）；深度学习零基础
- **资源**：每周 **基准 25h / 冲刺 30h**（v1.11 按真实课表）；团队 3 人（我 + 机械 + 文档）；无指导老师、职院实验室不可用；预算 = **钱管够**（v1.5：P0/P1 立刻买，见 §11）
- **定位**：**电控 ↔ ROS2 的桥（micro-ROS 全栈）**
- **语言**：项目主语言 **C++**（ROS2 侧 rclcpp）；**Python 仅作工具**（临时脚本），不作为学习目标
- **文件布局**：ROS2 编译工作空间放 **Linux ext4（`~/ros2_ws`）**；Windows 侧只放源码/笔记；**用 git 同步**（见接续指南 3.1）

**⏳ 真正待办（已去重；只保留"必须有外部输入才能定"的事）**

| # | 待办事项 | 处理时间 | 影响 | 状态 |
|---|---|---|---|---|
| 1 | ~~雷达型号 / 减速比 / 编码器 PPR / 轮径~~ → **已解决**（雷达 MiniV1 全新；官方值 40.5 / 44 / 65 / 轮距 172.75），**只剩"上电实读确认"** | W1 周末 或 W2 | 运动学参数 | ✅ 文档已齐（`01` / `02`） |
| 2 | **是否开源**（仓库先私有，何时转公开） | W1 建仓库时定 | 选题书措辞、仓库权限 | ⏳ 待你决定 |
| 3 | 农大指导老师人选 + 回复 | W4 发邮件后 | 挑战杯报名资格 | ⏳ 待外部 |
| 4 | 职院技能大赛教练 / 校团委联系人 | W5 | Plan B 抓手 | ⏳ 待外部 |
| 5 | 车载计算选型（**树莓派 5/4 优先**，车顶板已预留安装位；备选 Jetson / 二手 NUC） | 12 月买之前 | 决定车能否独立跑 | ⏳ 已收敛到"树莓派优先" |
| 6 | 平衡车到货后的实际电机/编码器参数 | **W3 到货当天** | PID 参数标定 | ⏳ 待硬件（`06_Kit_Check` 已备好） |
| 7 | 编码器**供电电压**、6P **线序**、SR04 **ECHO 分压** | **W3 接线之前（必须）** | ⚠️ 接错会烧编码器 / 烧引脚 | ⏳ 待实测（`01` §5.B） |

**✅ 已查到（官方证据，见第 15 节，不必再清点）**

| 项目 | 结论 | 证据 |
|---|---|---|
| 下位机主控 | **ESP32-S3** | `esp32s3_pio_template`（FishBot 四驱模板工程，含 `platformio.ini`） |
| 下位机固件 | **micro-ROS 版本** | `fishbot_motion_control_microros` |
| 板载 IMU | **MPU6050** | `MPU6050_light`（FishBot 主控板 IMU 驱动模块） |
| 雷达接入方式 | 经 **雷达转接板** + 独立 ROS 驱动 | `fishbot-laser-control`、`fishbot-laser-driver` |
| 摄像头 | 有配套**无线摄像头** ROS2 驱动（实物是否带待看） | `fishbot_camera` |
| 官方课程覆盖 | 含 **MicroROS 章** 与 **实体机器人章**（硬件搭建/嵌入式通信/上位机建图导航） | `d2l-ros2` README 章节表 |

---

## 9. 打卡表（**双周粒度** —— 每两周填一行；**日/周粒度在 `03` §6**）

> 🔁 **跨表同步规则（v1.17 新增）**：每天的回填要**同时**写进 **`03` §6 的产出行**与**本表对应行的"本周产出"**（本表虽是双周粒度，但要**滚动维护**，别等两周再补）—— v1.16 就漏过一次：09-22 的产出只进了 `03` §6，没进本表，被你一眼看出。


| 周次 | 日期 | 本周产出（能演示/能提交的东西） | 卡住的问题 | 下一步 |
|---|---|---|---|---|
| W1-2 | 09-20 ~ 10-03 | 09-20（W1 第 1 天）：A 线 git ✅（09-19 提前完成）；手写 `w1_topic_demo` pub/sub 跑通（修 `%s` 乱码）；C/C++ 笔记 `06~11` 落盘 + ROS2 笔记 `01/02` + 工具笔记 `02`；**09-21（W1 第 2 天）：自己新建 Keil 工程 + 写出三路 PWM + README（含截图）+ git 提交 `6039ee9`**（`stm32_study_myself\01_pwm_out_project_keil`，TIM2 **CH2/CH3/CH4 → PA1/PA2/PA3**；审读见 `05` §8）；`5-1` 已在别的笔记本完成 ✅<br>**09-22（W1 第 3 天 · ROS2 日）**：✅ **手写 `w1_service_demo`（service server + client）** → 推 `ros2_study` 的 **`my-notes`** 分支（`f8091f6`，+554/−3）；✅ **学习笔记 `03`（publish 实体四层地址链）+ `04`（服务的三个"为什么" + 自我推理逐条判对错）**；✅ **定「双层笔记工作流」**（原始层自己写 / 成品层 AI 整理 / **复述才算认领**）；✅ 读 `rclcpp` 源码卡在 STL → `12_STL容器_…哈希表.md`（+222）+「语言补丁队列」（`stm32_study` 提交 `1ef1a14`）；⏳ `uart_bridge` 编译未做<br>**09-22 当晚续（时钟显示为"09-23 04:4x"，实为 20:4x —— 双系统把 Windows 时钟改了 +8h，见 v1.18）**：✅ 把 `delay_ms` 换成 **SysTick `System/Delay.c`**（编译组 + `IncludePath` 均接入）+ `main.c` 改 CH2 呼吸灯<br>**09-23（W1 第 4 天 · 今天下午）**：✅ **`PSC/ARR 720/100 → 719/99`（精确 1 kHz）**；✅ **13:24 Rebuild = `0 Error / 0 Warning`**（`.axf`/`.map` 刷新、`Code=2064 / ZI=1632`）；✅ **示波器实测 `Freq 1.00 kHz` / `Cyc1 997 µs`**（09-21 为 `1.01 ms` / 990 Hz → 误差 **−1.1% → −0.3%**，详见 `05` §8.8）；✅ **CH3/CH4 已对齐判据**（`25%/75%`，13:25）→ **13:25:30 复 Rebuild = `0/0`**；✅ **提交 `3c3340e`（13:32:33）已推送**；✅ **S1-a 凭证齐全**（两路同屏照：`1.00 kHz / 997 µs`，CH3=75% / CH4=25%）；✅ **今晚新开工 `w1_ic_测周法_project_keil`**（18:32-19:03，输入捕获·测周法）→ 审读 `05` §8.9~§8.9.9：思路对、**封存为"标志版"**（演示用"自测自"、单定时器只能自证 → **09-25 已完成双定时器重写，见下**）；**OLED 烧了**（现象改 Watch/示波器）；编译 `0E/1W`（当日未提交 → 次日留档为 `w1_ic_测周法_project_keil-错误版`）<br>**09-25（周五 · 文件时间戳 14:33~15:49）**：✅ **双定时器重写完成 → 归档为 `stm32_study_myself\w1_ic_测周法_project_keil -正确版`**（TIM2_**CH2/PA1** 出 1 kHz/20% → **跳线 PA1↔PA6** → **TIM3_CH1** 捕获 + `SlaveMode_Reset`；`IC_GetFreq() = 1000000U / CCR1`；**新加现象判据**：频率 ∈ (900,1100) Hz → **PA11 置高**；`Rebuild = 0 Error / 0 Warning`、`Code=2728 / ZI=1632`；目录含 **2 张照片**）＋ 原版**原样留档**（`-错误版`，含 09-23 18:54 那份 455 B 原始 README）＝ **错题本** → 审读 `05` **§8.10**<br>⏳ **仍缺 3 项（09-25 现状）**：① ` -正确版\README.md` **三要素**（接线表重点写 **PA1↔PA6 跳线** + 2 张照片各拍什么）② **两个目录都未提交**（`??` 状态）③ **一次 Watch 数值**（`TIM3->CCR1` 与 `IC_GetFreq()` → 定 `+1` 之争） | ✅ **`uart_bridge` 已于 09-24 编译成功**（`colcon build` 0 failed，见 `03` W1 行的 09-24 记录；Linux 侧产物不在本仓）；**仍待做**：W2 的参数 / launch / bag（**含参数回调**）；**09-22 的真卡点**：读 `rclcpp` 源码被 **STL 容器（`unordered_map`/`try_emplace`）** 挡住 → 已进「语言补丁队列」（`03` §4） | 🔧 09-21 电控日开 STM32（可直接按 W2 的 UART 环形缓冲开工）；**09-22 不必"补命令肌肉记忆"**（实测 `bash_history` 已有 59 条 `ros2` 命令：`run`×34、`topic`×15、`node`×6、`pkg`×6、`interface`×5、`doctor`×3 + `rqt` 节点树看过）→ 改做 **W2 的参数/launch/bag**（已完成一部分：service demo ✅）；**09-23（今天）14:00-17:00：Keil Rebuild + `PSC/ARR→719/99` + CH3/CH4 = 25%/75% 示波器两路同屏（S1-a）+ `git add -A` 提交**；**09-24（周四 · ROS2 日② · ~3h）：✅ 已完成** —— `colcon build` **0 failed** → **`socat` 假数据灌帧** → **`/mcu/frame` = `data: [0, 7]`、`/mcu/counter` = 7**（**无硬件**）；3 个现象已成笔记（**pts 编号每次变** / **`0x55` 写错对象→终端显示 `U`** / **`pgrep -x` 只认前 15 字符**）；**发现桥没有参数回调 → `ros2 param set port` 不生效** ⇒ 记为 W2 动手项；**下一步（09-25 起）**：① ` -正确版` README 三要素 ② **两个 IC 目录提交** ③ **W2（09-27 起）：`publish_period` + 参数回调 + launch + bag**<br>**09-26（W1 第 7 天 · 周六攻坚日 · RM 考核交付日）**：① ✅ **`流水灯第二次尝试\`（`led_test.uvprojx`）上板闭环** —— 5 处修复全对（全量 `0E/0W`、`Code 4044→4104`）＋ **3 条现象**（按下即切 = 判定窗 **640 ms → 40 ms**；流水灯 **100 ms/盏**；两模式主循环 ≈**50 Hz**）；② ✅ **`D-0926-01` 改判「已交」**（呼吸灯口径澄清：第 1 个工程本来就有 ⇒ **2/2 效果**）；③ 🔴 **本人示波器抓到新真问题 `P1-2`**（`PWM_Init()` 每 20 ms 重跑 ⇒ `mode 1` 频率瞬跳 **1.2 kHz**）⇒ **09-27 开工前 5 min 修**（4 行 `last_mode` 守卫）；④ 🛠 **陪练模式生效** ＋ `05` §8.12.9 / §8.12.10 落盘；⑤ ⏳ **W1 未闭合 4 项**：工程 `README` 三要素／`09` §7.2 W1 三个数／D-0926-02 实物照片／`流水灯第二次尝试\` 入库；**下一步（09-27 = W2 第 1 天）**：**先修 P1-2 → 再 `W-0927-01`（S1 四路 @5 kHz）** |
| W3-4 | 10-04 ~ 10-17 |  |  |  |
| W5-6 | 10-18 ~ 10-31 |  |  |  |
| W7-8 | 11-01 ~ 11-14 |  |  |  |
| W9-10 | 11-15 ~ 11-28 |  |  |  |
| W11-12 | 11-29 ~ 12-12 |  |  |  |
| W13-14 | 12-13 ~ 12-26 |  |  |  |
| W15-16 | 12-27 ~ 01-09 |  |  |  |
| W17-18 | 01-10 ~ 01-23 |  |  |  |
| W19-20 | 01-24 ~ 02-06 |  |  |  |
| W21-22 | 02-07 ~ 02-20 |  |  |  |
| W23-24 | 02-21 ~ 03-06 |  |  |  |
| W25-26 | 03-07 ~ 03-20 | 校赛 |  |  |
| W27+ | 03-21 ~ 05-31 | 省赛 / 大创 / 开源发布（**此阶段起改为每月一填**，行数按需续写） |  |  |

> 📅 **2026 秋法定假期（官方 · 国办发明电〔2025〕7 号）**：**中秋 09-25(五)~09-27(日) 3 天**；**国庆 10-01(四)~10-07(三) 7 天**；**补班：09-20(周日)、10-10(周六) 要上课/上班**。
> ⏱ **每周目标（v1.11）**：**基准 25 h**（冲刺 30 h）→ 复盘时把**实际小时数**填在打卡表该行末尾（例：`26.5h`；**日/周填 `03` §6，双周填本表**）。
> 连续两周实际 < 16h → **主动降回 20h 档**（周一/二 2h、周三/四 2h、周五 3h、周六 3h、周日 4h）；稳定 ≥ 90% 完成率 → 上冲刺档。
> → **影响**：W1 的 **09-25~09-26**、W2 的 **09-27 + 10-01~10-03**、W3 的 **10-04~10-07** 均为假期；**10-01~10-07（7 天连休）= 开学后最大整段窗口**（排期细则见 `03_周主题日与学习指引.md` §9）。


---

## 10. 团队与协作（**git 第 1 周必学，这是当前最大硬伤**）

| 角色 | 人 | 职责 |
|---|---|---|
| 你 | 电控 + ROS2 + 集成 | MCU 固件、micro-ROS、雷达/传感器接入、PID、系统联调 |
| 机械同学 | 1 人 | 结构设计（SolidWorks）、装配、3D 打印/铝件、底盘改造 |
| 文档同学 | 1 人 | 文献调研、报告/PPT、演示视频剪辑、技术笔记排版 |

**协作规则（第一周就要建立）**
1. **所有代码进 git**：一个项目一个仓库，`README.md` 写清怎么编译、怎么跑；
2. **每两周一次 30 分钟例会**：上周产出 / 本周目标 / 卡点；
3. 一个功能一个分支 → 自己发 PR → 合并（**习惯 PR 流程，比赛材料里可截图证明**）；
4. **参数进配置文件**（`config/*.yaml`），不要硬编码，换场地能快速重调；
5. 文档同学把每周笔记整理成"可对外发布"的版本 —— 直接变成挑战杯材料素材。

**git 学习清单（W1 用 4 天 × 1h 完成）**
`git init / add / commit / status / log / diff / branch / checkout / merge / remote / push / pull / clone`，
外加 GitHub：新建仓库、Issue、Pull Request、README、`.gitignore`（C / ROS2 / Python 模板）。

---

## 11. 预算分配（**钱管够版**：一次买对、买全、留冗余 —— 2026-09-20 v1.5 重排）

> **口径变化**：原计划"等 11 月 9000 到账再买" → 现在 **P0/P1 立刻买**（**早买 = 早省时间；工具缺失才是最大的时间成本**）。
> **原则**：① 工具一次到位；② 关键件买好不返工；③ 备件冗余；④ **不买用不上的能力**（M3508/CAN 已降级 → 不买）。

| 优先级 | 项目 | 预算 | 说明 |
|---|---|---|---|
| **P0 立刻** | **STM32F407VET6 开发板**（正点原子/野火，配 ST-Link V2.1 或 J-Link OB）+ **F103C8T6 备用板 ×2** | 500 | **不用等 11 月**；F407 是 micro-ROS/RTOS 的载体；备用板防烧 |
| **P0 立刻** | **逻辑分析仪 8ch（100MHz 档、带 I2C/SPI/UART 协议解析）** + USB 电流表 | 300 | 调 I2C/UART/编码器的刚需（比 50 元款省时间） |
| **P0 立刻** | **平衡车套件**（江协科技，标准库）| 150 | 原计划 09-29 买 → **提前到 W2**，与 S1/S2 同步 |
| **P0 立刻** | **USB-TTL ×2（CH340）** + 杜邦线/洞洞板/端子/热缩管/焊锡 散件包 | 150 | 串口调试与搭电路必备 |
| **P0 立刻** | **12V→5V DC-DC ≥3A ×3** + 保险丝座/船型开关/接线端子 | 150 | ⚠️ 12V 电池**绝不能直进开发板**（见 `01_…清点表.md` 4.1） |
| **P0 立刻** | **电池 ×2 + 充电器/平衡充** | 400 | 电池**必须买新的**；两块 = 实验不中断 |
| **P1 车能自跑** | **树莓派 5（8G）+ 散热壳 + NVMe 或高速卡** | 900 | 顶层板**已预留树莓派安装位**（省转接件） |
| **P1** | **USB 摄像头（或 CSI 摄像头）** | 150 | 视觉小模型的数据源（现有 ESP32 视觉模组留作对照） |
| **P1** | 电机/驱动冗余：**370 编码器电机 ×2 备用 + TB6612/DRV8871 ×2 + 更好车轮** | 250 | **不买 M3508**（不需 CAN，见 §16.8 v1.5） |
| **P2 按需** | 结构件（3D 打印/铝件/螺丝包）、亚克力备件、OLED/SR04 备件 | 400 | 机械同学用 |
| **P2 可选（SLAM 阶段再定）** | **深度相机（RealSense D435i / Orbbec Astra）** | 预留 2000 | ⭐ 钱管够就**预留**：等 11~12 月做 SLAM/Nav2 时若雷达不够再买（**触发条件写死，避免"买了不会用"**） |
| **不买** | ~~M3508+C620 + CAN 驱动板（~2000）~~、~~H743~~（F407 够用）、~~第二台雷达~~（MiniV1 全新够用） | 0 | 与主线无关 |
| 预留 | 备件/耗材（约 10~15%） | 1000 | 一定会用掉，别省 |
| **合计** | | **≈4,350（立刻买）+ 2,000（可选预留）+ 1,000（耗材）≈ 7,350** | 在 9000 内；若金额有变动，**先砍 P2 可选深度相机** |

> 原则不变：**先让"车能自己跑起来"，再谈升级**；但**工具与备件不再省**（省工具 = 花时间）。

---

## 12. 敲门砖计划（破"没导师 / 没实验室 / 没背景"的局）

你的劣势很实在：**职院实验室不开放、职院老师不熟本科赛事、农大那边你不认识人**。
但你有一个别人没有的组合：**绩点第一 + 每周 25 小时 + 能自己做出东西**。
所以策略是"**先有作品，再敲老师**"，而不是先找人。

**10 月底前必须产出"三件套"（这就是敲门砖）**
1. **一页选题书（PDF）**：题目 / 应用场景（果园·设施农业）/ 技术路线（STM32 + micro-ROS + 雷达 + 视觉）/ 创新点（一句话）/ 已有基础（git 仓库、仿真与实物视频）/ 需要的支持（只要"挂名指导 + 偶尔借场地"）；
2. **GitHub 仓库**：真实 commit 历史、README 能复现、有接线图；
3. **演示视频**：30 秒~3 分钟（**Gazebo 仿真 + 现有 FishBot 实物各一段**）。<br>⚠️ v1.5 时间线修正：10 月的实物段落用**现有 FishBot（官方固件）**拍；**自研固件的版本在 1 月（S9 打通后）补拍** —— 因为 10 月只到"外设台阶 S1~S4 跑通"，还没有自研整车。

**找谁（按成功率排序）**

| 目标 | 为什么可行 |
|---|---|
| 农大**工学院/机电方向的年轻讲师、副教授**（农机 / 智慧农业 / 图像处理 / 机器人方向） | 你的选题与他们的方向天然相关，他们也需要学生做项目 |
| 农大**校团委 / 创新创业学院**负责挑战杯的老师 | 他们关心"有没有学生愿意报"，你报名就是他们的业绩 |
| 职院这边的**技能大赛教练** | 熟技能大赛体系，能带你报职院赛项（不同赛道，互不冲突） |
| 往届挑战杯参与者 / 学长学姐 | 拿模板、避坑、了解答辩风格 |

**邮件要点**（一页、别写小作文）：我是谁（楚怡计划/学籍农大/绩点第一）→ 我在做什么（一句话 + 附 GitHub/视频）→ 想请您做什么（挑战杯指导，支持需求极少）→ 进度计划 → 落款。

**底线思维**：若春节前仍无老师 → 走"个人作品 + 大创申报"路线；比赛报得上就报，报不上就把**开源作品 + 技术长文**做成硬通货。

---

## 13. micro-ROS 专项路线（你要当的"桥"，这就是差异化）

| 步骤 | 时间 | 平台 | 内容 | 验收标准 |
|---|---|---|---|---|
| 0 | **W6（10 月底）** | 现有 FishBot（**ESP32-S3 官方固件**） | 跑通 `micro-ros-agent` 端到端，观察话题/QoS，读官方固件与文档 | 看到 micro-ROS 节点数据（**不依赖 F103 能力**） |
| 1 | **W5~W8**（⭐ v1.30：S6 UART 提前到 W5，故本步骤较原计划提前一周） | F103 + PC | **串口协议桥**：MCU 打包 → 上位机解析 → 发 ROS2 话题（**与 S6 UART 合并**，先理解"过桥"） | `ros2 topic echo` 收到 MCU 数据 |
| 2 | **W11~W12** | F103 | **FreeRTOS 入门**：任务/队列/信号量 + **静态创建**（内存纪律见 `学习C_C++语言补充\11`） | F103 上 3 任务并发 + 队列传数据 |
| 3 | **W13~W16（12 月中~1 月中）** | **F407 + HAL/CubeMX** | `micro_ros_stm32cubemx_utils` + agent：让 STM32 成为**原生 ROS2 节点** | MCU 发布/订阅成功 |
| 4 | **W17~W20（1 月下~2 月上）** | F407 + FishBot 硬件 | 雷达/IMU/电机全部接入 ROS2；做 **ESP32 官方固件 vs STM32 自研固件对比实验** | 一个 launch 启动整车 + 对比数据表 |
| 5 | 2027-02 | — | 长文《STM32 + micro-ROS 下位机移植与对比》+ 开源 | 文章 + 仓库 + 视频 |

> 说明：micro-ROS 在 **F103（20KB RAM）上非常勉强**，正式移植放在 **F407**；
> **10 月先用现成的 ESP32-S3 车体验（步骤 0）**，不用等 F4 到货 —— 但**先用手上的 F103 把台阶 S1~S7 走完**再上 F407（避免"两头都半途"）。
> ⚠️ **CAN 已降级为可选**（本项目用直流电机+PWM）→ **原"步骤 1a CAN 预研"删除**，省 3~4 周。
> ⚠️ **RM 探路已启动（v1.24：用户已决定"放下蓝桥杯、全力 RM + micro-ROS 小车"）** → **CAN 恢复为"必学"**（RM 电机全部走 CAN，是电控组入场券之一）；**但顺序不变** —— 它排在 **W11+** 的既有位置（见 §16.8 v1.21 ②(a) / v1.24 ③）。
> ⚠️ **期末考风险（2026-09-20 核 · 课表已解出）**：**课程排到第 19 周（2027-01-04~01-10）** → 期末考大概率落在 **第 20 周（2027-01-11~01-17）** → 与上表 **步骤 3（W13~W16 = 12 月中 ~ 1 月中，F407 + HAL + micro-ROS）的前半段重叠**。
> **预案**：12 月只做 F103 侧小步（FreeRTOS 入门 / 文书），**把 F407 + micro-ROS 整段挪到寒假**（无课，且正好是 2027-03 校赛前的冲刺段）。
> **待办**：查清校历期末考试周后回来改这张表（细则见 `03_周主题日与学习指引.md` §9.4）。



---

## 14. 前 6 周逐周计划（2026-09-20 → 2026-10-31）

> ⚠️ **重要修订（2026-09-20 v1.5，取代 09-19 版）**：**钱管够 → F407 立刻可买**，
> 但**顺序不变**：**W1~W8 全部基于现有的 STM32F103C8T6 + 鱼香 FishBot（ESP32-S3）+ PC**（先把台阶 S1~S7 走完），
> **F407 / HAL / CubeMX / micro-ROS 从 W13（12 月中）开始**；**FreeRTOS 从 W11 开始**。
> 台阶定义见 §2 的 A 线说明与 §13 步骤表。

| 周 | 主线 A（技术，全部 F103 平台） | 主线 B（成果/团队） | 周末验收产出 |
|---|---|---|---|
| **W1** 09-20~09-26<br>🏫 09-20 补班 · 🎑 09-25~09-26 假 | **git**（本地 + GitHub）；小车参数已入账（`01` / `02`），若车在手边可顺手读一次实读参数 | 建**学习工作区仓库 `stm32_study`**（⚠️ 建成时是**公开**库，建议改 Private）；把计划与笔记提交上去 —— **本地已建 + 首次提交 `484d3d0`（752 文件，09-19 20:23，✅ 提前完成；身份 `HzEgw`）**，**09-19 20:34 已 `git push` 成功**（6 次提交；仓库决定**保持公开**，队员可直接 clone）。**比赛作品库 `stm32-ros2-lidar-car` → W5 后另建**（只放 F407 固件 + ROS2 包） | ✅ **W1 实况（09-20~09-26 · 已收工）**：① **GitHub 仓库 + 6 次提交**（`484d3d0` 起）+ **清点表/参数已入账**（`01`/`02`）；② **自建 Keil 工程 + 三路 PWM**（`01_pwm_out_project_keil`；`PSC/ARR = 719/99` → 示波器 **`997 µs` / `1.00 kHz`**、CH3/CH4 = 75%/25% 两路同屏）；③ **`w1_ic_测周法` 双定时器重写**（跳线 PA1↔PA6 + 从模式复位；判据 **900~1100 Hz** ⇒ **S3 提前完成 ≈80%**）；④ **RM 第一次考核**：`流水灯控制`（3 效果，含呼吸灯）+ **闭卷重做 `流水灯第二次尝试`**（非阻塞 + 5 处修复；上板 3 现象；**`D-0926-01` = 已交**）；⑤ **R 线闭环**：`colcon build` 0 failed + `socat` 假数据 → `/mcu/frame`（**W2 的 launch / 参数回调 / bag 已预做一半**）；⑥ **制度 5 件套**：`03` 主题日 / `09` 工程精神五条 / `10` 每日项目制 / `11` 国庆 P 卡 / `12` 一致性审计；**KPI 16h/16h 达标**<br>⏳ **W1 未闭合（顺延）**：工程 `README` 三要素 / `09` §7.2 的 W1 三个数 / D-0926-02 实物照片 / `流水灯第二次尝试\` 入库（65 文件）/ **`P1-2`（09-27 开工前 5 min 修）** |
| **W2** 09-27~10-03<br>🎑 09-27 假 · 🇨🇳 10-01~10-03 假 | **S1 卡口（B 方案 · v1.30）**：**TIM3 四路 PWM @5 kHz** → 只清**欠账 ①②**（对齐官方电机双 PWM，四路独立占空比）；**③ 互补+死区、④ 寄存器版「改性质」**（③ → RM/无刷分支以后再补；④ → 换到 **W3 的 S2 工程上用寄存器版写**）；平衡车套件到货 → **`11` G0** | 与机械同学对需求 | 四路波形（至少两路不同占空比）+ `g_tim3_freq = 5000±3` + 能脱稿讲清 `PSC/ARR/CCR` |
| **W3** 10-04~10-10<br>🇨🇳 10-04~10-07 假 · 🏫 10-10 补班 | **S2 编码器 + 开环调速**：`03_Encoder`（标准库）+ **本人用寄存器版再写一遍**（`_Reg` 只作对照答案）+ 用 PWM 驱动电机正反转；**国庆窗口执行 `11` 的 G0~G2**（成品车：核对 → 量电压链 → `06_Kit_Check` → 双电机开环 → 编码器测速） | 文献调研表（10 篇） | 手转输出轮一圈 = **实测计数**；电机能正反转；转速与秒表手测 **±5%** 内 |
| **W4** 10-11~10-17<br>（假期后回归正常周） | **S3 输入捕获 + SR04**：`02_Input_Capture(_Reg)` PWM 输入模式（**捕自己发的 PWM，频率/占空比**）× SR04 超声波实战（⚠️ ECHO 5V 需分压）；**B 线**：**一页选题书 + 给农大老师发第一封邮件** + 联系校团委 | 收集老师回复 | SR04 测距（±1cm）+ 捕获读数与设定值一致 |
| **W5** 10-18~10-24 | **S6 UART + 环形缓冲（⭐ 提前 · B 方案）**：中断收发 + 环形缓冲（**复用算法补丁 ⑧**）→ **MCU 每 100 ms 发一帧（占空比/转速）到 PC 画曲线**；`04_UART_Bluetooth(_Reg)` 双版本 | 摸清报赛流程 | PC 端曲线能复现（截图）+ 帧格式与 `README` 一致 |
| **W6** 10-25~10-31 | **S5 速度环 PID（用 W3 的编码器）**：先开环阶跃 → 再加 P/I，**必须有上位机曲线**；B 线：**用现有 FishBot（ESP32-S3）跑通 `micro-ros-agent` 端到端**（原计划内容保留） | 三人第一次例会（定分工与节奏） | **转速跟随曲线**（题图级证据）+ `ros2 topic echo` 看到 micro-ROS 节点数据 |

> 🔀 **顺序重订（B 方案 · 2026-09-25 用户确认 · 详见 `12` §3.2）**：**W2 = S1 只清"四路 + 5 kHz"** → **W3 = S2（含寄存器版首次真写）** → **W4 = S3（SR04）** → **W5 = S6 UART（提前）** → **W6 = S5 速度环 PID** → **W7 = S7 I2C（MPU6050）→ 平衡车直立环起步**。
> **降为机动（"掉队不心疼"）**：**S4 ADC（电池电压）**、**③ 互补死区**（→ RM/无刷分支）、**④ 寄存器版**（已换成"在 S2 上写"）。
> **判据**：小车"最小可演示闭环"= **电机能控 + 速度可测 + 数据能上上位机画曲线 + 姿态能读** → 由 11 月中提前到 **10 月底~11 月初**。
> ⚠️ 互换原则不变：**每次只引入一条新知识**（`03` §4）；**主题日与钟点一字未动**（`03` §1 / §10.3 仍是权威）。

> 📅 **假期对齐（官方，2026-09-20 核）**：中秋 **09-25(五)~09-27(日)**、国庆 **10-01(四)~10-07(三) 共 7 天**、补班 **09-20(日) / 10-10(六)** 上课上班。
> **落在哪**：W1 的 **09-25~09-26**、W2 的 **09-27 + 10-01~10-03**、W3 的 **10-04~10-07** 都是假期；**W3 最后一天 10-10(六) 要上课**。
> **排期动作**：把 **S2 编码器 + 开环调速 + 平衡车 G0~G2（成品：核对 → 电压链 → 自检 → 反向拆解 → 双电机开环 → 编码器测速）** 尽量塞进 **10-01~10-07**（≈ 25~30h 可支配），W3 的周末验收顺延到 **10-11(日)**；细则见 `03_周主题日与学习指引.md` §9 与 `11_国庆平衡车项目卡.md`。
> ⚠️ **期末考风险（待确认）**：按 08-31 开学推，**第 18~19 周 ≈ 12-28 ~ 01-10** 大概率期末考 → 与 §13 的 W13~W16（F407）冲突，预案见 `03` §9.4。


> W1 若 git 学得快，可提前 1~2 天开始 W2；周末 **周六 4h + 周日 5h** 用来做"整段不被打断"的任务（如 micro-ROS 环境搭建）。

**R 线（ROS2 恢复）与上表并行**（每周 **7h**：周二 2.5h / 周四 1.5h / 周日 3h；逐日安排见 `03_周主题日与学习指引.md` §4）：

| 周 | R 线恢复目标（每次只引入一条新知识） |
|---|---|
| **W1** | 环境体检 + 命令肌肉记忆：`ros2 doctor / topic / node / service / param` 全过一遍 |
| **W2** | **编译 `uart_bridge`**（若 W1 未完成则本周补齐 → 见 `03` §4）+ 加参数 + 写 launch + 录一段 `ros2 bag` |
| **W3** | 自定义 `msg/srv` + TF 复习（`odom → base_footprint → laser`） |
| **W4** | **Gazebo + ros2_control 差速仿真回炉**（复现上次已跑通的成果，重建信心） |
| **W5+** | 转入实车对接（`micro-ros-agent` + 官方固件），ROS2 降到 **4h/周** |

---

## 15. 现有小车（鱼香ROS FishBot）情报与用法（2026-09-19 联网查证）

**查证来源**：fishros 的 GitHub 仓库列表 + 《动手学ROS2》课程 README（官网需 JS，直接查了源码侧）
**参数来源②**：官方店铺商品页"发货清单 + 三视图"（2026-09-19 用户截图）→ 已逐项入账到 **`01_FishBot硬件清点表.md`**
（雷达 **MiniV1 全新** / 轮径 **65 mm** / **370 带编码器减速电机** / 12V 2800mAh / 0.96" OLED / SR04 / 树莓派底板 / 二驱差速）

**参数来源③（最权威 = 能跑起来的固件）**：`fishros.org.cn` 社区 + 官方固件源码 → 汇总到 **`02_FishBot官方固件参数与STM32移植映射.md`**
（减速比 **40.5** / 脉冲比 **44** / 轮径 **65** / 轮距 **172.75** / PID **0.625·0.125·0** / 编码器 **4 倍频** / 电机 **双 PWM 5 kHz** /
节点 `fishbot_motion_control`、话题 `odom`(20 Hz)、`cmd_vel`、`imu`、frame `odom`→`base_footprint`）

**教程资源（正版"说明书"）**：《动手学ROS2》**第 13~16 章**就是这台车的官方实现（13 嵌入式基础 / 14 micro-ROS / 15 雷达与超声波 / 16 控制系统搭建：电机→编码器→PID→运动学→里程计）
→ **你的项目 = 把这 4 章用 STM32F407 重做一遍**（映射表见 `02_…` §7）

| 证据（官方仓库/描述） | 说明 |
|---|---|
| `fishbot_motion_control_microros` — "Fishbot运动控制程序 **MicroROS 版本**" | 下位机固件跑的就是 **micro-ROS** |
| `fishbot_motion_control_microros_four_driver(_v2)` | 有四驱/双驱两种硬件版本 |
| `esp32s3_pio_template` — "FishBot 四驱模板工程"（含 `platformio.ini`、`src/`、`lib/`、`boards/`） | 下位机主控是 **ESP32-S3**（PlatformIO 工程） |
| `MPU6050_light` — "FishBot **主控板 IMU 驱动**模块" | 主控板板载 **MPU6050** |
| `fishbot-laser-control` — "雷达转接板原理图 PCB 及嵌入式程序"；`fishbot-laser-driver` — "雷达驱动(SDK/ROS/ROS2)" | 雷达经转接板接入，有独立驱动 |
| `fishbot_camera` — "配套**无线摄像头** ROS 2 驱动" | 车上可能带无线摄像头 |
| `fishbot_nav`、`fishbot-control-ros2`、`fishbot_tool`（固件更新配置工具） | 上层 ROS2 导航/驱动/烧录工具 |
| 课程章节：13 ROS2-Control → **14 MicroROS** → 15 ROS2控制硬件实战 → **16 实体机器人硬件搭建 / 17 嵌入式控制及通信开发 / 18 上位机建图及导航** | 鱼香课程**本来就覆盖"实物小车 + micro-ROS + 建图导航"**，与你的目标完全重合 |

### 三个重要结论

1. **你的车本身就是一台 micro-ROS 真机**：下位机 ESP32-S3 跑 micro-ROS 固件 —— "MCU 接入 ROS2"的实物平台你已经有了；
2. 但 ESP32 路线是**官方已经铺好的**，你只能学"配置"；真正的硬功夫是 **把下位机移植到 STM32F407（FreeRTOS + micro-ROS）**，并自己接入编码器/IMU/雷达；
3. 这个移植恰好构成挑战杯的**创新点 + 对比实验**：
   > ESP32-S3 官方固件 vs STM32F407 自研固件 —— 对比内存占用、端到端延迟、控制实时性、可扩展性、成本，并给出数据。

### W1 清点清单（逐项拍照/记录丝印）

> **2026-09-19 更新**：商品页"发货清单 + 三视图"到手，下面一批已**确定**，
> **逐项明细不要手抄，看 `01_FishBot硬件清点表.md`**（含运动学公式、供电链、实测步骤）。

- [x] 雷达型号 = **MiniV1（全新）** ✅（发货清单 + 用户确认）
- [x] 轮径 = **65 mm**、驱动形式 = **二驱差速**、电机 = **370 带编码器减速电机** ✅
- [x] 电池 = **12V / 2800mAh**（充电器 12V1A）；还有 0.96" OLED、SR04 超声波、视觉模组 V2.0、雷达转接板、**树莓派安装底板** ✅
- [x] 电机**减速比 40.5**、编码器**脉冲比 44（= 11 线 × 4 倍频）**、**轮距 172.75 mm** ← **官方固件默认值**（见 `02_FishBot官方固件参数与STM32移植映射.md` §2.1）；⚠️ 旧版固件为 46/42/150 → **仍以实车实读为准**
- [x] 运动学常数：每输出轴一圈 **1782** 计数、里程分辨率 **0.114592 mm/count**、PID **0.625/0.125/0（±100）** ✅
- [ ] **编码器供电电压**（3.3V / 5V）→ 万用表实测（⚠️ 接错会烧编码器）
- [ ] 用 ROS2 服务读一次**本车实读参数**（`02_…` §8 有整段命令）
- [ ] 6P 电机线**线序**（哪 2 根电机 / 哪 4 根编码器）→ 万用表实测（清点表 5.B）
- [ ] 主控板丝印型号（ESP32-S3 具体模组？）、电机**驱动芯片**、板载 IMU 丝印、当前固件版本（`fishbot_tool`）
- [ ] 雷达 MiniV1 的**波特率 / 协议 / 点云格式** → 官方 `fishbot-laser-driver` 源码，或直接问店家要手册
- [ ] **别拆坏/卖掉 ESP32-S3 主控板** —— 它是"官方固件 vs 自研固件"对比实验的基准项

### 项目定位修订（重要）

| 原计划 | 修订后 |
|---|---|
| 自己从头搭一台小车 | **直接改造现有 FishBot**：底盘/雷达/电机/摄像头全保留，只把下位机换成 **STM32F407 自研固件（micro-ROS）** |
| W2 做"UART 协议桥" | 保留（**学习用**，理解底层）；项目主线改为直接上 micro-ROS |
| F407 是"想买" | **F407 升级为项目必需（P0 预算）** —— 它是这次"移植对比"的主角 |
| 雷达可能要新买 | 先确认现有雷达型号，**够用就不买**，把钱留给车载计算 |

---

## 16. 附录：自审修订 / 不做清单 / 验收标准 / 失败预案 / 归档规范 / 变更日志

> 本节是"元信息"（为什么这么改、什么不做、变更历史），平时不用看；**每两周复盘时扫一眼 §16.4 不做清单**即可。
> 16.1~16.3 是 v0.2 的修订记录（**16.3 已被 §4 取代**），16.4~16.7 是长期有效的规则。

### 16.1 本次修订项（问题 → 修正）

| # | 问题 | 修正 |
|---|---|---|
| E1 | 只写"3 月校赛"，**没写报名时间** | 很多学校报名在 **12 月~1 月**（会早于 3 月校赛）；**W4 就要摸清本校报名/材料截止流程** |
| E2 | 12 月塞了 4 件事（CAN/闭环/IMU/PID） | 拆细：CAN 预研 1 周 → 电机闭环 2 周 → IMU+PID 2~3 周 |
| E3 | ~~9000 元一次算满~~ → 改为**"钱管够 → 立刻买"**（v1.5，见 16.2） | 工具与安全件（F407/逻辑分析仪/DC-DC/电池）**本周就买**；大头（树莓派）10 月内；深度相机按触发条件 |
| E4 | 第 8 节与第 15 节冲突（已查到的事仍挂"待确认"） | 已去重，第 8 节拆成"真正待办 / 已查到" |
| E5 | 缺少"每周怎么分配 25h" | 见 16.3 |
| E6 | 缺少"不做清单" | 见 16.4（**这是你最大的风险：换方向**） |
| E7 | 缺少交付物验收标准 | 见 16.5 |
| E8 | 缺少失败预案 | 见 16.6 |
| E9 | 缺少素材归档规范 | 见 16.7 |
| E10 | "每主题双版本"在 F4 阶段失效 | 规则更新：**F1 = 标准库版 + 寄存器版 / F4 = HAL 版 + 寄存器版 / micro-ROS = 单一 CubeMX 工程** |

### 16.2 预算释放（⚠️ v1.5 改为"**钱管够 → 立刻买**"）

| 批次 | 时间 | 金额 | 买什么 | 说明 |
|---|---|---|---|---|
| **P0 立刻** | **本周** | ~1,650 | **F407VET6 板（+ F103 备用 ×2）**、**逻辑分析仪（8ch 100MHz）+ USB 电流表**、**平衡车套件**、USB-TTL ×2 + 散件包、DC-DC ≥3A ×3、电池 ×2 + 充电器 | 工具与安全件**不再等**；平衡车套件**已下单（09-25 22:32，套餐1）**，到货当天跑 `06_Kit_Check`（`11` G0） |
| **P1** | **10 月内** | ~1,300 | **树莓派 5（8G）+ 散热 + NVMe/高速卡**、USB 摄像头、电机/驱动冗余（370 电机×2 + TB6612 ×2） | "车能自己跑"的前提；树莓派装在**已有底板**上（省转接件） |
| **P2 按需** | 11~12 月 | ~400 | 结构件（3D 打印/铝件）、亚克力/OLED/SR04 备件 | 机械同学需求驱动 |
| **可选预留** | SLAM 阶段（11~12 月评估） | 2,000 | 深度相机（D435i / Astra） | **触发条件**：雷达点云不足以支撑避障/SLAM 时才买 |
| **耗材预留** | 全程 | 1,000 | 备件/耗材 | 一定会用掉，别省 |
| 合计 | — | **≈6,350** | — | 余量留作机动 |

### 16.3 每周 25h 固定分配 —— ⚠️ 已被 §4 取代（v1.11 起按 **基准 25h / 冲刺 30h**）

> 权威版本见 **§4「每周节律：主题日制度」** 与 **`03_周主题日与学习指引.md`**。下表仅留档：

| 时段 | 小时 | 内容 |
|---|---|---|
| 周一~周四晚 | 8 | 主线技术（写代码/调硬件），每天 2h |
| 周五晚 | 3 | 整理笔记 + 提交 git + 集中查文档/提问 |
| 周六 | 7 | 整段攻坚（搭环境、联调、做实验） |
| 周日 | 7 | 4h 继续攻坚 + 1h 复盘（三问）+ 更新进度表 + 2h 弹性/补课 |

### 16.4 不做清单（Stop-doing list）

1. **不学 Python**（只到"能读懂、能改参数"）；
2. **不做第二个大项目**（雷达小车是唯一主线，RM 只是同一条线的应用）；
3. **不换平台**（F103→F407 已定，别去折腾别的 MCU）；
4. **不追新技术**（强化学习控制、视觉大模型、ROS2 新发行版 —— 2027-05 前都不碰）；
5. **不连续两周没有可演示产出**；
6. **不熬夜**（**25h/周基准、30h 冲刺**，23:00 前收工；通宵会毁下周，也会毁 GPA）。
7. **不开"没约束的新知识点"、不交"没判据的交付"**（v1.27 新增，源自 09-25 RM 考核失败）：
   ① 学新外设前先有**约束来源**（车上哪个需求 / 哪张规格卡），否则**排队**不插队；
   ② **编译 `0 Error` 只是入场券**，没有"一条现象判据（Watch 数值 / 波形 / 照片）"不算完成；
   ③ 细节与整改动作见 **`09_0925考核复盘与工程精神整改.md`**（§3 六条动作 / §7 欠账台账）。

### 16.5 交付物验收标准（DoD）

| 交付物 | 通过标准 |
|---|---|
| STM32 工程 | ① Keil 编译 0 error ② 现象与 README 一致 ③ Watch 关键变量符合预期 ④ README 含接线/步骤/常见问题 |
| ROS2 包 | ① `colcon build` 无 error ② `ros2 topic echo` 看到预期数据 ③ README 命令可直接复制执行 |
| 周进度 | **`03` §6 打卡表**的当日格 + 「本周产出」行有"可演示产出"（视频/数据/截图） |
| 比赛材料 | 一页选题书 + 演示视频 + 数据表 + 实物可现场演示 |

### 16.6 失败预案

| 风险 | 触发条件 | 应对 |
|---|---|---|
| **下位机基础补课超时**（v1.5 新增） | 10 月底仍未走完 S1~S4 | **绝不压缩 S1~S4**（地基）；把 S5 速度环挪进 11 月；砍 S7（OLED 可选） |
| **F407 到货晚**（v1.5 新增） | W13 仍未到货 | 继续用 **F103 + ESP32-S3** 组合演示（micro-ROS 先跑 ESP32 侧），F407 部分顺延，**不停工** |
| 平衡车卡死 | 连续 2 周无进展 | 跳过直立环先做速度环；或退回"编码器 + 单环 PID"练手 |
| 老师不回 | W6 结束仍 0 回复 | 换 3 位老师 + 联系校团委；走"个人作品"报名 |
| 9000 未到账 | 12 月仍未到 | v1.5 起：**P0 立刻买的小额（~1,650）先自费/家里垫**；大头（树莓派/深度相机）等钱到位 |
| 校赛没过 | — | 立刻转 Plan B（大创 / 职院技能大赛 / 工创赛）+ 开源作品 |
| **RM 探路无果**（v1.24 新增） | **2026-12-31 前**仍联系不到可用战队 / 学籍口径不被接受 | **果断放弃 RM 大赛**（判据见 §16.8 v1.21 ③），回"个人作品 + 挑战杯 + micro-ROS"路线；**已投入的探路成本 ≤5h，可承受** |
| 课业爆炸 | GPA 下滑 | **保 GPA 优先**（它现在是你唯一的敲门砖），技术降速但不停止 |

### 16.7 素材归档规范（第一天就建）

```
docs/
├── media/      # 照片/视频, 命名: 20260926_pwm_init_01.jpg
├── data/       # 实验数据 csv, 命名: 20261010_pid_kp2.5.csv
├── reports/    # 周报/月报/选题书/答辩稿
└── logs/       # 调试日志: 问题 → 原因 → 解决
```
规则：**每周日复盘时把当周素材归位**；视频 1080p 且文件名带日期；截图带时间戳。

### 16.8 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-19 | 初稿：月度里程碑、前 6 周计划、预算、Plan B |
| v0.2 | 2026-09-19 | 自审修订：F103 优先（W1~W6 不含 F4）、第 8 节去重、预算分阶段释放、新增 16.3~16.7、双版本规则更新（E1~E10） |
| v0.3 | 2026-09-19 | **硬件参数入账**（新增 `成长路线\01_FishBot硬件清点表.md`）：雷达确定为 **MiniV1 全新**→采购表改为"条件触发"；P0 明确 **12V→5V DC-DC ≥3A ×2**；车载计算**树莓派优先**（顶层板已预留安装位）；§15 加参数来源②；§15 W1 清点清单改为"已确认 + 待实测"两段 |
| v0.4 | 2026-09-19 | **拿到官方固件源码参数**（新增 `成长路线\02_FishBot官方固件参数与STM32移植映射.md`）：减速比 40.5 / 脉冲比 44 / 轮径 65 / **轮距 172.75** / PID 0.625·0.125·0 / 4 倍频 / 双 PWM 5 kHz；确认 ROS2 接口（节点、`odom` 20 Hz、`cmd_vel`、frame）与雷达转接板为 ESP8266 透传；定位官方教程第 13~16 章为"官方实现说明书"；§15 加参数来源③与教程资源；W1 清点清单再打勾两项 |
| v0.5 | 2026-09-19 | **准备工作收口（不动实物）**：① 新增 `05_I2C_MPU6050_Reg`（I2C 纯寄存器版，寄存器速查表 + 超时 + 地址扫描 + 事件对照）；② 新增 `06_Kit_Check`（套件到货验收工具：一键自检 6 项 + 验收记录表）；③ 修复 `05_I2C_MPU6050` 标准库版漏包含 `stm32f10x_i2c.h`（原会编译失败）与 `mpu6050.h` 里 START/STOP/BUSY 位写错的注释；④ 在 `成长路线\README.md` 增加"**时间纪律**"表，把实测类任务排回 W1 周末/W3，避免打乱节奏 |
| **v1.0** | 2026-09-19 | **加入主题日 + 排版重构**：① 新增 **`03_周主题日与学习指引.md`**（主题日制度、每日 4 段式时间盒、**ROS2 恢复期 4 周路线**、打卡表、降级方案）；② 时间预算改为 **20h/周**（工作日 2h×5 + 周末 5h×2，按用户实际节奏）；③ 本文件加**配套文件导航 + 目录**、补上缺失的 **§1 起点与终点（一页速览）**；④ §4 重写为主题日制度；⑤ 消除重复/矛盾：§6 标"历史稿→见 §11"、§16.3 标"已被 §4 取代"、§8 待办表去掉已解决项并新增"接线前三测"；⑥ §14 增加 **R 线（ROS2 恢复）逐周表** |
| **v1.1** | 2026-09-19 晚 | **起止日改为 09-20 + 周制明确**（今晚是 09-19 晚，不计入 KPI）：① 起止 → **2026-09-20（周日）**；② 明确"**每周从周日算起、到周六结束**"（W1 = 09-20~09-26），并据此平移全部周次日期：W2 09-27~10-03、W3 10-04~10-10、W4 10-11~10-17、W5 10-18~10-24、W6 10-25~10-31；③ §9 打卡表 13 行日期全部 +1 天平移（W1-2 = 09-20~10-03 … W25-26 = 03-07~03-20）；④ `03_周主题日与学习指引.md` 同步：W1~W4 日期、打卡表、"周制"说明 |
| **v1.2** | 2026-09-19 晚 | **终审 + 去"(草案)" → 定稿**：① 文件名 `00_8个月学习计划(草案).md` → **`00_8个月学习计划.md`**（全文引用同步：README ×2、`03` ×2）；② 审核修 3 处**内部矛盾**：§3 的 2026-10 里程碑写"F103→F407 迁移"与 §16.1（E1：F407 推迟到 11 月买）冲突 → 改为**纯 F103 平台**；§14 W2 的 ROS2 任务与 `03` §4 重复规划 → 改为**指向 `03`**（避免 A/R 线抢时间）；§9 打卡表止于 W25-26(03-20) 而计划延伸到 2027-05 → 补 **W27+ 行**并注明"此阶段每月一填"；③ 新增 **§17「8 个月后我能到什么程度（实话版基线）」**（能力清单 + 不会具备的 + 三种结局概率 + 月度自查），并在目录登记；④ 标题 → v1.2 |
| **v1.3** | 2026-09-20 晚 | **⚠️ 起点更正（用户本人 09-20 晚提出"下位机计划有严重错误"）+ 电控日顺序调整**：① §0 自我盘点 STM32 行由"中上 / PWM 与输入捕获已手写双版本"更正为"**PWM 输出与输入捕获尚未完全吸收、ADC 完全没学**"，并写明 `01~05(_Reg)` 双版本工程是 **AI 备好的参考代码**（工程 README 原文："代码已经写好，Keil 工程由你新建"）**≠ 本人已掌握**；② §1「起点」同步更正；③ §3 十月里程碑改为"**先补 PWM/捕获/编码器/ADC → 再 UART/I2C/SPI/DMA**"；④ §14：**W2 改为吸收 `01`/`02` 双版本工程**，**W3 加 ADC**，**UART 顺延到 W4**（正好与"用 UART 上报 PID 数据"合并）；⑤ 同步修正 `成长路线\README.md` §5 的 W2~W4 产出列；⑥ 教训记入本节：**"工作区里有工程" ≠ "本人已掌握"**，以后 §0 盘点只认"自己动手写过并讲得出原理" |
| **v1.4** | 2026-09-20 深夜 | **全量起点核查（用户要求"重新核查所有计划"）+ 台阶式 A 线方案**：<br>① **§0 再更正两处**：STM32 行把"**用过 FreeRTOS**"改为"**仅了解/无实际工程**"（证据：`bash_history` 无 `freertos/vTask/xTaskCreate`，D 盘工程无 `heap_*.c`/`FreeRTOSConfig.h`）；ROS2 行把"**git 完全不会**"改为"**已上手**"，并补上**核查证据**（`chapt4_interfaces/srv/*.srv`、`fishbot_gazebo.urdf`+`gazebo.launch.py`、59 条 `ros2` 命令+rqt）——**ROS2 行判定为站得住**，但注明"会=照教程做过，从零自写待 W2~W4 验证"；<br>② **A 线改为"台阶式"**（每 1~2 周一个可演示台阶）：S1 PWM 输出 → S2 编码器+开环调速 → S3 输入捕获+SR04 → S4 ADC → S5 速度环 PID → S6 UART+环形缓冲 → S7 I2C(IMU+OLED) → S8 FreeRTOS 入门 → S9 F407+micro-ROS → S10 整车联调+对比实验 → S11 校赛打磨；<br>③ **三项结构性降级/后移（待用户确认后落盘 §3/§11/§13/§14）**：**CAN 与 RM 路线降级为可选**（本项目用直流电机+PWM，省 3~4 周与 ~2000 元）、**FreeRTOS 由"10 月入门"后移到 W11~W12**、**micro-ROS 主线由 12 月后移到 12 月底~1 月**（10 月仍用 ESP32-S3 官方固件先跑通 `micro-ros-agent` 端到端）；<br>④ 教训追加：**"了解" ≠ "用过" ≠ "掌握"**，§0 只分这三档，写清档位 |
| **v1.5** | 2026-09-20 深夜（第二轮） | **按"全部改成最好、最新的，钱管够"重排（用户指令）**：<br>① **§2 A 线**改为**台阶式 S1~S11**（每 1~2 周一个可演示台阶），并注明 CAN/RM 降级；<br>② **§3 月度里程碑全部重排**：10 月 = S1~S4 + S5 起步（**不碰** UART/I2C/FreeRTOS/F4）；11 月 = S5~S7 + ESP32 侧 micro-ROS 端到端；12 月 = S8 FreeRTOS + S9 起步（F407）；1 月 = S9~S10（**micro-ROS 打通 + 整车**）；2 月 = S10 收尾 + 对比实验 + 视觉；3 月 = S11 校赛；<br>③ **§11 预算改为"钱管够版 + 立刻买"**：F407（+备用 F103）+ 好一点的逻辑分析仪 + 平衡车套件提前 + USB-TTL/散件 + DC-DC×3 + 电池×2 + 树莓派 5 + 摄像头 + 电机/驱动冗余 + 结构件；**深度相机预留 2000（SLAM 阶段触发）**；**删除 M3508/CAN**；合计 ≈4,350 立刻 + 2,000 预留 + 1,000 耗材；<br>④ **§5** RM 行标为"可选/降级"；<br>⑤ **§13 micro-ROS 路线重排**（步骤 0 用 ESP32-S3 → 步骤 1 与 S6 合并 → 步骤 2 F103 FreeRTOS → 步骤 3 F407/HAL → 步骤 4 整车对比 → 步骤 5 长文），**删除原步骤 1a CAN**；<br>⑥ **§14** 的"F407 11 月才买"修订作废（钱管够），改为"顺序不变：W1~W8 用 F103 走台阶，W11 起 FreeRTOS，W13 起 F407"；**§14 W2~W6 全量重写为 S1~S6**；<br>⑦ 待同步：§12/§16.2/§16.6、`成长路线\README.md` §2/§5、`03` §1（见下条提交） |
| **v1.6** | 2026-09-20 深夜（第三轮 · **用户立规**） | **学习主权回收三规（用户本人提出，优先级最高）**：<br>① **复盘 30min → 50min**：周日 5h 重分配为 **2h ROS2 + 2h 联调 + 50min 复盘归档**（其中 **≥20min 用于读 AI 笔记并补写空白**）；§4 / §3 月度预算行 / `03` §1、§3、§6、§8 全部同步；<br>② **AI 提炼的笔记只是"草稿"** → 每篇必须留「**我的补充**」「**我的疑问**」两个空白区，**本人过一遍并补写**后才算"学过"（写入 `README` §4 第 9 条）；<br>③ **git 一律本人手敲**：AI 只给命令 + **只读**事后验证，**不代跑** `init/add/commit/push/branch/mv`（写入 `README` §4 第 10 条、`03` 铁律第 5 条）；<br>④ **起因（如实入档）**：09-20 晚用户指出"笔记都是 AI 提炼的、不是我做的"；且 Cline 在一条"已让用户停止"的克隆任务里**实际替用户完成了一次 push**（`2d97997` rename READEME→README）→ 遂立此规；<br>⑤ **教训**：用户明确表示"我自己来"之后，AI **不得执行任何写操作**；判断任务是否完成**必须有落盘证据**，不能凭一次读取就下结论 |
| **v1.8** | 2026-09-20 深夜（第五轮 · **本人自述校正**） | **起点以"本人自述"为最高优先级**（用户原话："**我没学过什么 I2C，我现在的水平你就把我当作是刚学 PWM 吧**"）：<br>① **§0 STM32 行再校正**：当前进度 = **江协科技教程刚学完第 6 章（PWM）**；**会**：GPIO/按键/OLED/单路 PWM（TIM2_CH1 1kHz 呼吸灯，示波器做过）/定时器中断+NVIC/ETR 外部计数/Keil 自建工程；**半成品**：第 5 章外部中断（`5-1` 没写完）；**未学过**：多通道 PWM、TIM1 互补死区、输入捕获、编码器接口、**ADC**、DMA、**UART**、**I2C**、SPI、FreeRTOS、HAL/CubeMX；<br>② **明确写入**：工作区 `01~06(_Reg)` 11 个工程是 **AI 参考工程，不代表本人水平**（含 I2C/UART 等都还没学）→ 用途是"学到那一章时拿来对照"，**不是压力**；<br>③ **S1 按"刚学 PWM"重排**：S1-a 多通道（TIM3 四路）→ S1-b 反推 5kHz → S1-c 真驱动电机；**TIM1 互补死区降为 S1.5**（跨度偏大，往后放）；<br>④ **教训（第 3 次同类）**：**AI 不得用"文件夹里有代码"推断"本人会"** —— 已犯 3 次（PWM → 编码器 → I2C）。**今后起点问题一律先问本人，再找证据** |

| **v1.7** | 2026-09-20 深夜（第四轮 · **用户要求"全部审查"**） | **代码审查 + 起点第三次更正（这次是逐行读真源码）**：<br>① 新增 **`成长路线\05_代码审查与能力盘点.md`**：读了用户桌面 `keil_program\` **8 个自建工程**（`led_project`/`5-1`/`6-1`×2/`6-2`/`6-3`×2/`led_project(new)`）+ AI 侧 `01~04_Reg` 全部与 `05/06` 部分；<br>② **§0 STM32 行据证据重写**：**会** Keil 自建工程 / GPIO(含模块化封装) / 定时器中断 + NVIC 优先级 / **ETR 外部计数** / **单路 PWM 1kHz（示波器验证）**；**半成品** EXTI（`5-1` 未写完）；**未做** 多通道 PWM、TIM1 互补死区、捕获、ADC、UART、I2C、SPI、DMA、FreeRTOS、HAL；<br>③ **问题清单**：用户侧 P0 真错 4 条（`GPIO_PinSource0` 误用、`key_getnum()` 缺 `return`、`5-1` 未完成、`6-3` 空壳）+ P1 规范 3 条；AI 侧 P2 **6 条**（`DIR` 只读位误写、`MSM=1`、捕获 ISR `else if`、未清初始标志、UART 缺 ORE/NE/FE 处理、`g_tim1_freq` 读错定时器）；<br>④ **§14 的 W2（S1）修正**：删掉"自建 Keil 工程 / GPIO 点灯"这类**已会内容**，改为 **TIM3 四路 5kHz → TIM1 互补+死区**，并新增验收 **"寄存器版由本人重写"**；<br>⑤ 新增建议 **S0.5「补完清单」**（修 4 个半成品，30~60min）；S2 可加速、S3 改为"先自写捕获再对照 AI 版并逐条验证疑点"；<br>⑥ 教训：**盘点能力必须读真源码**；"跟教程敲过" ≤ 真实水平，"工作区里有工程" ≠ 本人会；**AI 代码不是标准答案**，要自己审 |
| **v1.9** | 2026-09-20 深夜（第六轮 · **用户提示"注意中秋/国庆 + 明天是第 4 周开始"**） | **节假日与校历对齐（官方数据落盘 + 排期修正）**：<br>① **取到官方原文**：国务院办公厅《关于 2026 年部分节假日安排的通知》**国办发明电〔2025〕7 号**（2025-11-04）→ **中秋 9月25(五)~27(日) 3 天**、**国庆 10月1(四)~7(三) 7 天**、**补班 9月20(周日)、10月10(周六) 上班**；<br>② **校历对齐**（用户提供"第 4 周从 09-21 开始"）→ 学期第 1 周 = **08-31**，据此推出 **校历周 ↔ 计划周 ↔ 假期** 对照表（新增 **`03` §9.2**）；<br>③ **§14 表加假期标记**：W1 09-25~09-26 假、W2 09-27 + 10-01~10-03 假、**W3 10-04~10-07 假（开学后第一个 7 天连休）**、10-10(六) 上课；**排期动作**：把 **S2 编码器/开环调速/套件组装**塞进 **10-01~10-07**（≈25~30h），W3 周末验收顺延到 **10-11(日)**；<br>④ **新增风险（待确认）**：按 08-31 开学推 **期末考 ≈ 第 18~19 周（12-28 ~ 01-10）** → 与 **§13 步骤 3（W13~W16 F407 + micro-ROS）正面相撞**，建议 12 月只做 F103 侧小步、**F407/micro-ROS 挪到寒假**；<br>⑤ **发现坏文件**：`桌面\尹华振(2026-2027-1)课表.pdf` **实为教务系统报错页**（1.6 KB HTML / "出错啦！系统运行异常" / 正方 `jwglxt`）→ **需重新导出或截图**，才能把"每天 2h"落到具体钟点（新增 `03` §9.5）；<br>⑥ `README` §3 导航补 `06` 任务单与 `03` §9 指针；`06` 任务单末尾加 **"接下来 4 周窗口速览"**（09-21 ~ 10-11 逐段怎么用）；**§5 关键时间节点**加 4 行（补班/中秋/国庆/期末+寒假）；<br>⑦ 教训：**排期必须叠加"学校日历"** —— 之前所有周次都假设"每周结构相同"，实际被假期/补班/考试周切碎 |
| **v1.10** | 2026-09-20 深夜（第七轮 · **课表解密**） | **课表从教务 PDF 解出 → 主题日改为"按周次段动态落点"**：<br>① 桌面课表 PDF（真 PDF · iText · 2 页 · **学号 202520256013** · 班级 **楚怡机器人2501** · 打印时间 2026-09-20）正文为 `UniGB-UCS2-H` 编码的 `(...)` 字面串 → 按 **UCS-2BE 逐字符解出**（含 `(节次)周次` 前缀）；<br>② **关键发现**：**PLC控制系统设计实训（一~五、含 9-12 节晚课）仅第 2-3 周** → **09-20 就是最后一天**，**第 4 周起课表骤轻**；**第 4-13 周周五全天无课** ⭐⭐；**第 7 周起周六上午有课**；**第 8-13 周周三晚有课**；**第 14-19 周周五下午有课**；**周日全天无课**；<br>③ **新增 `03` §10**（课表原文 + 三条结论 + "可用时段表" + 每日 2h 落点：周一/二/四晚 19:00-21:00、周三 15:00-17:00、**周五第 4-13 周 14:00-17:00**、周六 14:00-17:00+19:00-21:00、周日 09:00-11:00+14:00-16:00+50min 复盘）；<br>④ **期末考估算修正**：课表排到 **第 19 周（2027-01-04~01-10）** → 期末考 ≈ **第 20 周（01-11~01-17）**（§5 / §13 / `03` §9.4 同步）；<br>⑤ 桌面原 PDF 已被删除（副本留在 `%TEMP%\kb.pdf`）；**教训**：排期必须按"**分段开课**"的真实课表，不能假设每周结构相同 |
| **v1.11** | 2026-09-20 深夜（第八轮 · **用户反馈"2h 太短、工作日也有空档"**） | **时间预算按真实课表上调：20h → 基准 25h / 冲刺 30h**：<br>① 依据 `03` §10 的课表空档重排落点：**周一晚 19:00-21:40（2.5h）**、**周二晚 19:00-21:45（2.5h）**、**周三下午 14:00-17:00（3h，稳定档期）**、**周四下午 14:30-17:30（3h）**、**周五 08:30-11:30 + 14:00-16:00（5h，第 4-13 周全天空）**、**周六 14:00-18:00（4h）**、**周日 09:00-11:40 + 14:00-16:30（5h）+ 50min 复盘**；<br>② 分配：STM32 **13h** / ROS2 **7h** / 归档 **2h** / 联调 **3h**（+ 50min 复盘）；8 个月总量 650h → **≈ 800~1000h**；<br>③ **假期另算**：中秋 3 天 ≈ 12h、国庆 7 天 ≈ 35h → 只做台阶加速，不占 KPI；<br>④ 新增三条纪律：**23:00 前收工** / **每周留半天完全休息** / **连续两周实际 <16h → 主动降回 20h 档**（附降档数字）；时间盒改为 **2.5h / 3h / 4h / 5h 四档**，明确"**加时 = 加完整段，不是拉长一段**"；<br>⑤ **自行更正**：§10.2 中"周三电控日②必须挪"作废 —— 电控日② 固定在**周三下午**，与第 8-13 周周三**晚课**（19:00-21:40）不冲突；<br>⑥ 同步文件：`03`（标题/§1/§2/§3/§4/§9.2/§10.2/§10.3）、`00`（§1/§4/§12/§14/§16.3/§16.4）、`README` §1、`04` §1.1/§3、`06` |
| **v1.12** | 2026-09-21 晚（**电控日 ①**） | **`5-1` 跳过 + 第一个"三路 PWM"工程审读**：<br>① **`5-1`（外部中断）已在别的笔记本完成** → `05` §5 的 S0.5 清单第 ② 项勾掉；<br>② **今天实际产出**：**`stm32_study_myself\01_pwm_out_project_keil`**（自建 Keil 工程 + 标准库；**TIM2 CH2/CH3/CH4 → PA1/PA2/PA3 三路 PWM**）；<br>③ **审读结论（新增 `05` §8）**：**正面 7 条**（引脚↔通道↔API 三者一致；`TIM_OCStructInit` 放赋值前；Init 顺序标准；**你自己读库源码推出的两条正确结论**：`TimeBaseInit` 会写 `EGR.UG` 产生更新事件、`GPIO_Init` 的 ODR 写入只对 IPD/IPU 有意义）；**P0 2 条**（**`PSC/ARR` 没减 1 → 实际 988.7 Hz 而非 1 kHz**；`delay_ms` 空循环且无 `volatile`）；**P1 4 条**（`TIM_ClockDivision` ≠ 死区、`GPIO_Speed` 对复用输出**有效**、应 `TimeBaseStructInit`、`(void)` 风格）；**虚惊 2 条**（C99 已开 ✅、-O0 不会删延时 ✅）；<br>④ **S1-a 验收改为**：CH3 固定 25% + CH4 固定 75% + 示波器两路同屏 + 工程 README 答"三问"（1kHz 怎么算/CCR 定占空比/为何减 1）；<br>⑤ 评价：**目前最好的一个工程**（结构、注释、主动查源码的意识都到位）<br>⑥ ⚠️ **教训（AI 侧，记档）**：AI 用"几分钟前的 `git status` 快照"推断"**用户没提交**"→ **错**。事实：用户 **22:46 已写 README（WPS 文档，含截图）**、**22:56 已提交并推送 `6039ee9`**。**判据修正：判断"做没做"必须看"最新提交 / 最新文件时间戳"，不得用过期快照** —— 这与"用文件夹里的代码推断本人会不会"属**同一类错误**（拿间接/过期证据下结论） |
| **v1.13** | 2026-09-21 深夜（用户追补：**"我的 README 你还没回答我里面的内容"**） | **`README.doc` 内容补读 + 示波器照片实证（新增 `05` §8.7）**：<br>① 09-21 只登记了"README 已写、含截图"**却没读内容** → 本次用 **OLE 复合文件解析**补读：正文在 **`WordDocument` 流（4142 B）**、截图在 **`Data` 流（81904 B）**（⚠️ **裸字节扫描 = 截断坏图**，必须按 `FAT`/目录表/`MiniFAT` **逐扇区重组流**再抽图）；<br>② **截图内容**：手机拍的便携示波器 —— `Freq 999Hz` / **`Cyc1 1.01ms`** / `Up-p 3.45V` → **`1.01ms` ⇒ 990 Hz，实测印证 `05` §8.2「PSC/ARR 未减 1 → 实际 988.7 Hz」**；**09-23 验收判据定为：改 `719/99` 后 `Cyc1 ≈ 1.00 ms`**；同屏 `999Hz` 与 `1.01ms` 自相矛盾 → **便携示波器以"周期/光标"读数为准**；<br>③ **你的两条自评都成立**：**1.5 h 全手写** ✅；**CCR 影子寄存器**机制理解正确 ✅ 且**代码事实上已是"无预装载"**（证据：本库**没有 `TIM_OCPreload` 成员**、`TIM_OC2/3/4Init` **从不写 `OCxPE`** → 复位值 0 = 写入立即生效）→ 补半句：**"跟手（关预装载）↔ 无毛刺（开预装载，电机/舵机该开）"按用途二选一**；<br>④ 回答"**是否需要再写一遍寄存器**"：**不必重写工程**，改练**双向翻译**（API→位 写注释 / 位→现象 反推 / `F12` 看库实现 / Watch 单步看位变化）；<br>⑤ **README 缺工程记录三要素**：① 引脚↔通道映射 ② 频率计算式 ③ 实测证据注解（另：**三路共用同一结构体 → CH2/CH3/CH4 现在全是 30%**）；<br>⑥ ⚠️ **教训（AI 侧第 5 次同类错误）**：**"文件存在" ≠ "读过内容"**，与 v1.12 ⑥ 同源（拿间接/过期证据下结论）→ **新规则：文档类交付物的验收必须"读一遍内容"再登记**；<br>⑦ 建议（待你点头）：`.doc` 是二进制（git 无差异、GitHub 不能预览）→ 工程 README **同时存一份 `.md`**，确认后写进 `03` 规范 |
| **v1.14** | 2026-09-21 23:15（用户追问：**这个打卡表你为什么一直没有更新**） | **打卡表修复 + 规则收口（"进度表"也纳入单一权威源）**：<br>① **问题**：你在 `03` §6 看到的 W1 只有 **09-20** 一格 —— **09-21 电控日① 的产出（自建 Keil 工程 + 三路 PWM + README + 提交 `6039ee9`）我写进了 `00` §9 双周表、`00` §0、`05` §8、`06`/`07` 任务单，却没回填 `03` §6 这张"日/周粒度"的表** → 表看起来"一直没更新"；<br>② **根因（文档设计缺陷，不是你的错）**：**三处规则互相指向不同的表** —— `03` §0 铁律 2 与 §5 卡片、§8 第 2 条都写"更新 **`00`** 打卡表"，而 `00` §9 自己写着"**每两周填一行**" → 日粒度的那张表**没有责任人**，于是"谁都没填"；同时 §6 只写了"周日复盘时填本周产出"，**没有"当天打勾"的义务**；<br>③ **修复**：**W1 的周一格补 ✅ + 补 09-21 产出（含证据 hash）**；`03` §6 新增「**维护规则**」4 条（① 当天本人 30 秒打勾 + AI 审读后必须回填"当日产出"行；② 一行 = 做了啥 + 证据；③ 周日复盘压成"一句能给外人看的话"；④ **唯一权威表**：日/周 = `03` §6，双周 = `00` §9，`README` §5 只留本周一句话）；把 `03` §0/§5/§8 的指向从"更新 `00` 打卡表"改为"更新 §6 + 每两周抄进 `00` §9"；`00` §9 标题标注粒度、§16.5 DoD 的"周进度"指明是哪张表；`README` §5 补 09-21 一行；<br>④ ⚠️ **教训（AI 侧）**：**"写进某个文档" ≠ "写进规定的那张表"** —— 交付物要落到**规则指定的位置**，否则等于没做（与 v1.11「数字只维护一处」同源：**每类信息只能有一个权威落点**）；<br>⑤ 顺带更正：v1.13 的日期原写 2026-09-22，实际是 **09-21 深夜（系统时间 23:18）** 完成的；<br>⑥ **提交回执**：本批文档修改已由本人提交为 **`88b12d4`**（**2026-09-21 23:20:42**，含 `00`/`03`/`05`/`06`/`README` + **新增 `07`**，共 **6 文件**），并于 **23:20:47 推送成功**（`origin/main` = `88b12d4`，工作区 clean）——本行即该提交之后补记的回执 |
| **v1.15** | 2026-09-23 凌晨 05:00（用户："**看看 git 提交没**" → 顺手做**全仓**核查，不只看 `成长路线`） | **提交回执 + 09-23 凌晨进度回填（含编译器实证）**：<br>① **提交回执（v1.14 那批）**：**`88b12d4`** 于 **09-21 23:20:42** 提交、**23:20:47 推送成功**，含 `00`/`03`/`05`/`06`/`README` + 新增 `07`（**6 文件**）→ `HEAD` = `origin/main`、工作区 clean（已写入 v1.14 第⑥条；数据源：`.git/logs/HEAD` 与 `.git/logs/refs/remotes/origin/main`，**不依赖终端回显**；⚠️ **但这两个文件当时读到的是陈旧缓存快照 → 该结论有误，已在 v1.16 更正**）；<br>② ⚠️ **但发现新工作未提交**：`01_pwm_out_project_keil` 的 `pwm.c`/`pwm.h`/`main.c`/`o1_project.uvprojx` = **M**，`System\`（`Delay.c`/`Delay.h`）= **未跟踪** → **必须 `git add -A`**（只 `add` 修改文件会漏掉新文件夹）；<br>③ ✅ **09-23 凌晨（04:42-04:54）实际进度**：`delay_ms` 空循环**已从 pwm.c/pwm.h 删除**，换 **`System\Delay.c`（SysTick · us/ms/s 三级）**；**工程集成正确**（`.\System\Delay.c` 进编译组、**`.\System` 进 `IncludePath` 第 339 行**）；`main.c` 改为 **CH2 呼吸灯**；<br>④ ⚠️ **编译实证（`Objects\o1_project.build_log.htm`）**：**1 Error / 2 Warning / Target not created** —— error = **`Usher\main.c(3): cannot open source input file "Delay.h"`**（**发生在把 `.\System` 加进 `IncludePath` 之前**，加完即应消失 → **必须在 Keil 里 Rebuild 复验**）；2 条 warning 实为同一条 **`pwm.h(8): #1295-D: Deprecated declaration pwm_init - give arg types`** → **`void pwm_init(void)` 一改即归零**（**编译器替我印证了 `05` §8 的 P1 结论**）；<br>⑤ ⏳ **P0 #1 仍未做**：`pwm.c` 第 32-33 行 `TIM_Period=100` / `TIM_Prescaler=720`（→ 988.7 Hz）；09-23 改 **`99 / 719`**；<br>⑥ 🚨 **纪律告警**：核查时系统时间为 **2026-09-23 04:55**（文件改动集中在 04:42-04:54）→ **触犯铁律 3「23:00 前收工」**，~~已当场劝停~~ ⚠️ **此告警已在 v1.18 撤回**：那批"04:42-04:54"实为 **09-22 当晚 20:42-20:54**（双系统把 Windows 时钟改了 +8h）→ **并没有熬夜**；<br>⑦ 📌 教训（AI 侧 · 第 6 次同类）：**最后一次 `git status` 是按 `成长路线` 过滤的** → 差点把"工作区 clean"当成全仓结论；**改规则：报"干净/提交完"必须用全仓（不加 pathspec）状态** |
| **v1.16** | 2026-09-23 13:02（用户："**可能是我时间不对…你把全部仓库都看一遍，就有答案了**"） | **全仓核查 + 时钟校准 + 更正 v1.15 的错误结论（重要）**：<br>① ❌ **我上次的错（AI 侧第 6 次同类，已记档）**：我引用 `.git/logs/HEAD` 与 `logs/refs/remotes/origin/main` 得出"**本地 = 远端 = `88b12d4`、你 09-22 没有提交**" —— 那两个文件我读到的是**陈旧缓存快照**（同批读 `refs/heads/main` 时工具已明示 outdated）。✅ **真相：09-22 20:44:16 你提交并推送了 `1ef1a14`**（C/C++ 笔记 `12_STL容器_map与unordered_map_哈希表.md` + 「语言补丁队列」+ `03` §4「C++ 语言补丁穿插」规则），**+222 行 / 0 删除**，本地 = 远端 = **`1ef1a14`**；<br>② 🕐 **时钟校准（你怀疑自己时间不对）→ 结论：你的时钟是对的**。三路独立验证：**(a)** GitHub 服务器侧推送 `2026-09-21T15:20:47Z` ↔ 本地 reflog `23:20:47`（秒级吻合）；**(b)** `2026-09-22T12:44:21Z` ↔ 本地 `20:44:20`（秒级吻合）；**(c)** 公网授时 `timeapi.io` = **2026-09-23 13:02:16（周三）** ≈ 本机 `Get-Date` `12:59:36`。→ **"现在 = 2026-09-23（周三）下午"成立**，`pwm.c`/`main.c`/`uvprojx` 的写入时间**显示为** "09-23 04:42-04:54" —— ⚠️ **但这个解释已在 v1.18 更正**（实为 **09-22 当晚 20:42-20:54**，双系统时钟 +8h）→ **"凌晨熬夜"告警已撤回，铁律 3 未被违反**；<br>③ 📚 **全仓清单（本机）**：**只有 1 个仓库** `D:\STM32F103C8_Workspace\.git`；**`D:\STM32F103C8_MotorSpeed` 完全没进版本管理**（含 `HARDWARE/motor_speed.c` = PWM 输入捕获测速/编码器接口/采样定时器三套配置、`USER/main.c`、`Libraries/`，还夹着 `_ls_check.txt`、`_W1_Ubuntu清单.txt` 等杂物）→ ⚠️ **风险：这块工程只存在于这一块硬盘上**；按 README §3.1 的仓库分工，它应是第三仓「**作品库**（私有）」，**建议尽快 `git init` 入库**；<br>④ ☁️ **全仓清单（GitHub 公开）**：`stm32_study`（默认分支 **main**，pushed **09-22 20:44:20**）、**`ros2_study`（**公开**、默认分支是 **master** 不是 main、pushed **09-22 20:43:48**、内容是**鱼香ROS 教程仓镜像** size≈3569，创建于 09-19 13:58Z）**、`stm32-`（09-06 建的**空仓**，从未使用）。✅ **已查明（本行 13:20 更正）：你推的是自己的分支 `my-notes`** —— commit **`f8091f6`**（作者 09-22 **20:33:33** / 提交 20:43:44 / **推送 20:43:48**，**+554 / −3**），内容 = **手写 `w1_service_demo`（service server + client）** + **学习笔记 `03`（publish 实体四层地址链）/ `04`（服务的三个"为什么" + 自我推理逐条判对错）** + **「双层笔记工作流」**（自己写原始层 / AI 整理成品层 / **复述才算认领**）；该分支还含 **`88b13014`（09-20 21:48：`w1_topic_demo` pub/sub + 笔记 01/02 首版）**。默认分支 `master` 只是上游鱼香ROS 镜像，所以**第一眼"看不到"你的提交** → 这也正是我把 09-22 记成"只有读源码"的原因，**已更正**；<br>⑤ 🔧 **工作区现状（`1ef1a14` 之上未提交）**：`pwm.c`/`pwm.h`/`main.c`/`uvprojx`（M）+ **`System/`（未跟踪）** + `成长路线/00`、`03`（本次文档改动）→ 今天收尾用 **`git add -A`** 一次提交；<br>⑥ 🔒 **新规则（防复发，写进 `03` §1）**：**"提交了没/工作区干不干净"这类结论不许只凭 `.git` 内部文件** —— 必须（i）用 `git log/status/rev-parse` 命令、（ii）与**服务器侧时间**（`api.github.com` 的 `pushed_at` / `events`）交叉验证；**同一会话内二次读取同一文件必须警惕缓存陈旧**（本次就是栽在这里）；<br>⑦ 🧭 **对 W1 的收口**：`03` §6 的 **09-22 格补 ✅**（依据 = `1ef1a14` 提交 + `ros2_study` 推送，非空勾），并把当晚产出写进产出行；**`07` 的 ROS2 验收（`colcon build` + `/mcu/frame`）无证据 → 未达成**，顺延到 09-24（周四 · ROS2 动手日）或今天下午 |
| **v1.17** | 2026-09-23 13:30（用户看 `00` §9 直接问：**"为什么这里没有 9-22 日的"**） | **双周表补 09-22/09-23 + 新增"跨表同步"规则**：<br>① **现象**：`03` §6 已补 09-22，但 **`00` §9 的 W1-2 行还停在 09-21**（我 09-21 晚写的快照，之后没再动）→ 你一眼看出；<br>② **根因（同类第 7 次：落错位置/漏同步，v1.14 的翻版）**：我按"日粒度只进 `03` §6"机械执行，忘了 §9 这一行是 **W1-2 的汇总**，09-22/09-23 本来就属于它；<br>③ **修复**：W1-2 行补 **09-22**（手写 `w1_service_demo` service demo + 笔记 `03`/`04` + 双层笔记工作流 + 笔记 `12`/语言补丁 + `f8091f6`/`1ef1a14` 两次推送）与 **09-23**（SysTick 延时接入 + 呼吸灯 + 1 Error 待 Rebuild + 待办）；"卡住的问题"列改写为**真卡点**（读 `rclcpp` 被 STL 容器挡住 → 语言补丁队列）；"下一步"列改为 **09-23 下午：Rebuild + `719/99` + S1-a 示波器验收 + 提交** / **09-24：编译 `uart_bridge` + `/mcu/frame`**；<br>④ **新规则**：**跨表同步**（每天同时写 `03` §6 与本表，滚动维护）—— 已写进 §9 标题下 |
| **v1.18** | 2026-09-23 13:40（用户指出：**"我的双系统会让 Windows 时间变成别的国家的时间，没同步时你看到的时间就不对"**） | **撤回"凌晨熬夜"误判 + 新增"时间证据分级"规则**：<br>① ❌ **误判**：我把 `pwm.c`/`pwm.h`/`main.c`/编译产物的 mtime **"09-23 04:42-04:54"** 当成"凌晨还在改代码"，据此在 **v1.15 ⑥** 发了"违反铁律 3（23:00 收工）"的告警 —— **错**。真相：**那是 09-22 当晚 20:42-20:54**（同一台机器接着 20:44 那次提交做的）；<br>② 🔎 **根因（双系统时间机制）**：**Ubuntu 默认把硬件时钟 RTC 当作 UTC 写回，Windows 默认把 RTC 当"本地时间"读** → 从 Linux 切回 Windows 后系统时间**偏 +8 小时**；用户若未自动/手动同步，就一直偏着 → **所有文件 mtime、编译日志时间整体偏移 8h**（也解释了为什么 git 提交时间反而是准的：`1ef1a14` 作者时间 20:44:16、服务端记录 20:44:20）；<br>③ ✅ **已对过的锚点（可复算）**：GitHub 服务端 `2026-09-22T12:44:21Z` ↔ 本地 reflog `20:44:20`（秒级吻合）；公网授时 `timeapi.io` `2026-09-23 13:02:16` ↔ 本机 `Get-Date` `12:59:36`；<br>④ 🔧 **治本（二选一，建议做①）**：① **Linux 侧** `timedatectl set-local-rtc 1 --adjust-system-clock`（让 RTC 存本地时间 → Windows 不再跳）；② **Windows 侧** 把 `RealTimeIsUniversal`（`HKLM\SYSTEM\CurrentControlSet\Control\TimeZoneInformation`）设为 `1`（让 Windows 也按 UTC 读）。临时办法：Windows 时间设置里"立即同步" 或 `w32tm /resync`；<br>⑤ 📌 **新规则（已写进 `03` §1）**：**"什么时候做的"证据分级 = ① git 提交时间（服务端可验证）> ② 文档按事件写的日期 > ③ 文件 mtime / 编译日志**；**mtime 与 git 冲突时以 git 为准**，并记录冲突量（本次 = +8h）；<br>⑥ 影响面：`03` §6 与 `00` §9 的"09-23 凌晨"条目**改归属为"09-22 当晚续"**；`05` §8.8.1 补了时间口径说明 |
| **v1.19** | 2026-09-23 20:10（用户三问：**ROS2 与下位机是否断层 / C++ 与算法短板 / 蓝桥杯要不要参加**） | **诊断记档 + 两个"待你点头"的动作**：<br>① 🔍 **断层：真实存在，但是计划内的**。现状证据：ROS2 侧**从未收到过 STM32 的任何一个字节**（`uart_bridge` 至今未编译、`/mcu/frame` 从未出现），STM32 侧**从未写过 UART**（到目前为止只有 PWM / 输入捕获）。**计划里本就定了两个汇合点**：**R 线 W2**（编译 `uart_bridge` + 看到 `/mcu/frame` 话题）+ **`00` §13 步骤 1（W6~W8，10 月底~11 月中）**（串口协议桥：MCU 打包 → 上位机解析 → ROS2 话题）。→ **结论：现在断层不是问题，"汇合日被推迟"才是问题**（你上次断 ROS2 就是这个模式）；<br>② 🟢 **建议（已在 v1.23 修正）**：**(a)** **R 线 W2 里加一次"PC 侧假数据合流"**：用虚拟串口（`socat`）喂一行文本 → `uart_bridge` → `ros2 topic echo /mcu/frame`（5 分钟、**纯 PC 侧**，不引入 STM32 新外设，先证明"桥"是通的）；**(b) ❌ 撤回**：原提"国庆塞 S6-min（MCU 发 UART 帧）提前合流" —— **违反 A 线台阶顺序**（编码器 / ADC 都还没学），见 **v1.23**；<br>③ 🧩 **C++/算法短板：证据成立**（自述"冒泡排序能说不能写"；09-22 卡在 `unordered_map`）。**诊断：不是数学问题，是"算法 → 代码"的翻译肌肉没练过**（循环不变式 / 边界 / 下标）。处方：**每天 10 分钟手写 1 题**（已加进 `03` §4 的「**算法补丁队列**」，第 1 题＝冒泡排序；验收＝**空白文件手写 + 编译通过 + 3 组边界自测**，不许抄不许查）；<br>④ 🏁 ~~**蓝桥杯：可以参加，但必须封顶**~~ **❌ 已作废（v1.24：用户决定不搞蓝桥杯）**：(i) **只报"单片机设计与开发"**（与主线同向：外设/中断/定时器/ADC/UART，备赛即练主线）；**软件赛道不报名**，只用其真题当"手写题集"；(ii) **备赛窗口只有两个**：**寒假整段（≈5 周 × 1.5h ≈ 50h）** + 每周 20 分钟补丁；**学期内不给它加时**（25h/周主线不动）；(iii) **硬护栏**：**月度自查若主线完成率 < 80% → 蓝桥杯降级为"只报名、不备赛"**；与 **3-4 月校赛/省赛冲突时以车为主线**；(iv) **别急着买板**：先白看 2 套真题（官网/社区有），11~12 月报名前再决定是否买 CT117E 类板（≈200~300 元） |
| **v1.20** | 2026-09-23 20:30（用户澄清：**"不是单片机的蓝桥杯，是 C/C++ 方向的"**，目标 **省三**） | ❌ **已作废（v1.24：蓝桥杯整体放弃 —— 集中到 RM + micro-ROS 小车）**，以下保留作历史回溯：~~**软件类 C/C++ 组：省三难度评估 + "保三"方案**~~（替代先前"只报单片机赛道"的建议）：<br>① **比例（以当年章程为准）**：省赛一/二/三等奖 ≈ **10% / 20% / 30%** → **获奖率 ≈ 60%，省三＝前 60%**；软件类报名人数最多、强手多，**但只需"填空题 + 前 1~2 道编程题"通常就落在省三档**；<br>② ⭐ **先确认组别（难度差一截）**：**B 组（普通本科）/ C 组（高职高专）** —— 由**学籍口径**决定（`楚怡计划 / 学籍农大` 这条线怎么认），**C 组竞争明显更小**，报名前必须先问清；<br>③ **省三只需要前 4 组能力**：**① I/O 与格式（多组数据 / EOF / 大数）② 暴力枚举 + 模拟 ③ 排序与二分（`qsort`/`sort` + 二分答案）④ 简单数论与日期（gcd / 素数筛 / 进制 / 日期推算）**；**明确放弃**（时间不够就不碰）：图论、复杂 DP、线段树、字符串算法（KMP/哈希）、并查集；<br>④ **投入**：**20~40h**（20h = 保三有希望；40h = 比较稳）；**量少而精：3~5 套真题 + 30 道基础题**（不做 10 套）；<br>⑤ ⚠️ **与主线的真实冲突**：**寒假主线已很重**（计划把 W13~W16 的 F407 + micro-ROS 整段挪到寒假）→ 蓝桥杯**上限 30h**，且**优先用"每周 20 分钟算法补丁"先把前 4 组练熟**，寒假只做真题冲刺；<br>⑥ **工具前置**：PC 端 C++ 环境（g++ / MinGW + VS Code）+ 在线题库（洛谷 / 牛客 / 蓝桥官方练习系统）；<br>⑦ **报名**：多在 **11~12 月**（个人可报，费用以当年通知为准）；**省赛一般 3~4 月**，与 **3 月中校赛**基本错开；**不贪国赛**（5~6 月撞期末 + 冲刺段） |
| **v1.21** | 2026-09-23 20:45（用户提问：**"或者不参加比赛，全心投入 RM 你觉得哪个更好"**） | **三条路的取舍分析（结论：不做二选一，但"现在全心 RM"是错的）**：<br>① ❌ **"现在全心投入 RM 对抗赛"不可行**：RM 超级对抗赛需要 **编制队伍（常见 5~10 人）+ 数万元级预算 + 全年投入（备赛期每周 20~40h）**，而你当前是"单人/小团队 + 无实验室 + 9000 预算 + 25h/周"→ 硬上会**同时毁掉比赛与主线**；<br>② ✅ **正确姿势 = "把 RM 技术栈当主线的最优参照 + 探路战队"，三档走**：**(a) 现在~11 月**：不组队不参赛；**⚠️ 学习顺序不变**（严格按 `S1→S11` 走，见 v1.23），只在 **W6+ 的自然节点**上把 RM 需要的能力（**CAN / F407+HAL / PID / FreeRTOS / 串口协议**，本就是你计划 W11~W16 的内容）纳入；**(b) 12~1 月**：花 **3~5h** 打听农大 / 长沙高校的 RM 战队是否招电控（招新多在秋季，但冬季常补招），用你的作品（GitHub + 示波器数据 + PWM/编码器/PID）报名；**(c) 2027-03 起**：若进队 → **把主线"改装"到 RM 载体上**（CAN 电机/云台/裁判系统），micro-ROS 小车退为个人作品线；<br>③ 🎯 **判据（防赌）**：**若 2026-12-31 前联系不到可用战队，或职院/外校学籍不被接受 → 果断放弃 RM**，回到"个人作品 + 挑战杯 + micro-ROS"路线；<br>④ 📊 **三条路对比（与主线重合度 / 门槛 / 时间成本 / 能换到的资源）**：**RM 电控**＝五★/四★/五★/五★（实验室+导师+团队+经费+简历）；**蓝桥杯软件**＝一★/二★/二★/二★（一个证书）；**原计划（自研小车+挑战杯）**＝五★/二★/三★/三★；<br>⑤ 🔧 **计划联动**：**若决定探路 RM，则把 §13 的"CAN 已降级为可选"改回"必学"**（RM 电机全程 CAN）—— 按你的决定执行 |
| **v1.22** | 2026-09-23 21:00（用户问：**"我现在的学习进度怎么样，学习质量如何"**） | **首次"进度 + 质量"证据评估（全文写入 `05` §9）**：① **进度**：W1 第 4 天，计划应投 ≈13h、实际按证据估 **11~14h** → **达标**；**广度超纲**（R 线已到 service/接口内容，A 线摸到 S1/S2），但 **W1 的两个硬验收未落地**（`uart_bridge` 编译 + `/mcu/frame`；S1 的完整规格）→ 结论 **"做得比计划多，但做完的证据比计划少"**；② **质量三档**：**A 档**（主动查源码 / 测量闭环 / 工程规范 / 制度自建 / 独立判断 4 次纠正 AI）｜**B 档**（外设概念 / 文档 / 工具链）｜**C 档**（算法"能说不能写" / **读-写一致性自检缺失** / 共享资源意识弱 / **未验证就交付** / UART 零经验）；③ **三件事**：**验证变条件反射**（每个工程收尾必须有一条现象证据）→ **（R 线 · PC 侧）先把 `uart_bridge` 编译起来**（**A 线 UART 仍按计划 W6**；国庆按计划做 S2 编码器 —— 见 v1.23）→ **每天 10 分钟手写题 + 每工程一张"意图↔配置对照表"** |
| **v1.23** | 2026-09-23 19:45（用户纠正：**"ADC 都还没学、编码器都还没学，你就给我跳到串口通讯了"**） | **撤回"提前合流"提案 + 新增「A 线顺序不可跳步」纪律**：<br>① ❌ **我的错**：为缓解"ROS2 ↔ 下位机断层"，我提议在**国庆**塞一个"S6-min（MCU 发 UART 帧）"—— **违反台阶顺序**（实际顺序：S1 PWM ✅ → **S2 编码器** → S3 输入捕获 → **S4 ADC** → S5 PID → **S6 UART**；UART 在第 11 章，排在 ADC/DMA 之后）→ **该提案撤回**；<br>② ✅ **只保留"PC 侧"那半**：R 线 W2 的"**假数据合流**"（`socat` 虚拟串口 → `uart_bridge` → `/mcu/frame`）**纯 PC 侧、不引入 STM32 新外设** → 不构成跳步；<br>③ ✅ **明天（09-24 周四 · ROS2 日）的 `uart_bridge` 编译 + 看到 `/mcu/frame` 不变** —— 它是**计划里本排在本周的 R 线任务**（`03` §4 W1 周四/周日），与 A 线进度无关；<br>④ ✅ **国庆 10-01~10-07 按原计划**：**S2 编码器接口 + 开环调速 + 平衡车套件组装 / `06_Kit_Check` 验收**（`00` §14 原文），**不塞 UART**；<br>⑤ 🔒 **新纪律**：**A 线台阶顺序（S1→S11）不得为"合流焦虑"跳步**；R 线的合流预演**只能在 PC 侧做**（不引入 STM32 新外设）；真要提前合流，**只允许在计划规定的窗口内（W6+）**。 |
| **v1.24** | 2026-09-23 21:20（用户决定：**"蓝桥杯不搞了，全心全意投入 RM 和自己的 micro-ROS 小车计划"**） | **砍支线、聚焦两线（决定记档 + 全部联动改动）**：<br>① ✅ **决定**：**放弃蓝桥杯**（含软件类 C/C++ 组的省三目标、寒假真题冲刺、11~12 月报名）→ 时间集中到 **RM 探路 + 自研 micro-ROS 小车主线**；<br>② 🔓 **释放出的资源（如实入账，不虚报）**：**(a)** 寒假备赛段 **≈30h**（原 v1.20 上限）→ **整段归寒假主线**（F407 + HAL + micro-ROS，本来最紧）；**(b)** 每周"算法补丁"窗口**保留但改口径**（从"刷比赛真题"→"补 C++ 短板"，触发场景＝读 `rclcpp`/写 ROS2 节点卡住，见 `03` §4）；**(c)** 报名费 / 竞赛板 **未发生**（未报名、未买 CT117E ✅ 按 v1.20 ④ 的建议执行）→ **零沉没成本**；<br>③ ⚠️ **但"全心投入 RM"≠ 提前学 CAN/F407**：**台阶顺序 `S1→S11` 不变**（v1.23 纪律照旧）；RM 电控要的 **CAN / F407+HAL / FreeRTOS / PID / 串口协议** **恰好全在本计划 W6/W11/W13 的既有位置上** —— **不新增、不提前**；<br>④ ✅ **"全力 RM"现在真正能做的三件事（合计 ≤5h，不占主线时间）**：**(a)** 10 月内花 **≤2h** 看 2026 赛季**规则 + 1~2 个开源电控仓**（把"RM 到底要什么"看清）；**(b)** 11 月花 **≤1h** 问清**学籍/参赛资格口径**（楚怡计划 + 湖南机电职院在校生能否报名、按哪组）；**(c)** 12~1 月花 **3~5h** 联系农大 / 长沙高校战队（招新/补招）—— **判据仍按 v1.21 ③：2026-12-31 前无战队 → 放弃 RM 大赛，回个人作品线**；<br>⑤ 📌 **不再做的事（防复活）**：不进算法/竞赛备赛群、不刷真题、不买竞赛板；**省下的注意力全押在"每两周一个能演示的台阶产出"**（这是 RM 招新与挑战杯**共用**的硬通货）；<br>⑥ 🔧 **本次联动改动清单（已同步）**：`00` **§2** 两条线说明（CAN/RM 状态）、**§3** 2027-05 里程碑行、**§13** CAN 注（"若探路 RM"→"已启动探路"）、**§16.6** 新增"RM 探路无果"失败预案、**§16.8** v1.19 ④ / v1.20 标 ✅❌作废、`03` **§4** 算法补丁队列口径、`03` **§9.4** 寒假 +30h 说明、`成长路线\README.md` §1 目标新增第 4 条 |
| **v1.25** | 2026-09-23 21:40（用户质疑：**"可是我是双系统啊"** —— 对"socat 假数据合流"的可行性发问） | **双系统串口归属澄清 + 假数据合流精确步骤落档 + 一处计划口径修正**：<br>① ✅ **澄清（结论）**：**串口永远属于"正在跑 ROS2 的那个系统"= Ubuntu**；`socat` 是 Linux 工具（Windows 没有）→ 这个测试**本来就只在 Ubuntu 里做**（不需要 STM32 / Keil / Windows）→ **双系统不是障碍**；<br>② 📌 **双系统的两个真实约束（写入 `04` §3.1）**：**(a)** 设备名每系统一套（Ubuntu `/dev/ttyUSB0` + `dialout` 组 ；Windows `COMx`）→ `port` 参数只填 Ubuntu 的名字；**(b)** **真合流（W6）时 MCU 必须插在 Ubuntu**，而烧写在 Windows → **每次真合流要重启一次** → 对策：**固件做成"上电即自动发帧"**，流程＝"Windows 烧好 → 重启进 Ubuntu 收数据"；<br>③ 🔧 **`04` 新增 §3.1**：`socat` 四终端步骤 + **合法帧字节示例**（`AA 55 02 00 07 09` → `/mcu/frame`=`[0,7]`、`/mcu/counter`=7）+ **反证**（改错 SUM → 校验错+1）+ 两个坑（socat 终端不能关 / `openPort()` 在构造时调用→**先 socat 后起桥**）；<br>④ 📦 **`ros2_ws_src\uart_bridge\README.md` 同步新增 §2.1**（"没有硬件也能测"）；<br>⑤ ⚠️ **口径修正（读源码才发现）**：`03` §4 W2 原写"加 3 个参数（串口名/波特率/**发布周期**）"，但 `uart_bridge_node.cpp` 实际声明的是 **`port` / `baud` / `publish_counter`** → **真正待做的是 `publish_period`**，`03` §4 已改；**同一轮还核实了 `launch\uart_bridge.launch.py`（已存在；`port`/`baud` 已声明为启动参数；`publish_counter` 暂硬编码 `True`）与 `CMakeLists.txt`（`install(DIRECTORY launch ...)` 已就位）→ W2 的 launch 任务是"续写"而不是"从零写"**；<br>⑥ 📌 **AI 侧动作纪律（本次做对了）**：**给任何命令前先读源码/文件清单，确认参数名与真实行为** —— 本次先读了 `uart_bridge_node.cpp`（确认 `port`/`baud`/`publish_counter` 的声明、`/mcu/frame` 类型、帧格式与校验和、`openPort()` 调用时机）、`launch\uart_bridge.launch.py`（确认 `port`/`baud` 是**已声明的启动参数**）、`CMakeLists.txt`（确认 `launch/` 已安装）与包内 `README.md` §3/§4（确认话题表与参数表），**逐项核对后**才写出 `-p port:=`、launch 备用写法、那 6 个帧字节、以及 `data: [0,7]` / `data: 7` 这两条预期输出。 |
| **v1.26** | 2026-09-25 下午（**证据驱动**：`工具与环境\03/04` 时间戳 **09-24 14:09**、IC 工程文件时间戳 **09-25 14:33~15:49**、build log **`Project File Date: 09/25/2026`**） | **两天实况补记（09-24 R 线闭环节 + 09-25 输入捕获双定时器重写）+ 两处口径修正**：<br>① ✅ **09-24（R 线 · ~3h）实况**（我此前只知"计划"，实际已做完）：`~/repos` clone → `~/ros2_ws` 软链 `uart_bridge` → **`colcon build` 0 failed** → **`socat` 假数据灌帧 → `/mcu/frame` = `data: [0, 7]`、`/mcu/counter` = 7**（**全程不需要硬件**）⇒ **W1 的 R 线硬验收闭环**；<br>② 📝 **09-24 的 3 个现象已成笔记**：`工具与环境\03_socat虚拟串口_pts编号每次都会变.md`（实测 **21/22**，照抄示例 `5/6` 卡了 10 分钟）、`工具与环境\04_pgrep查不到进程_comm只留15字符.md`（`uart_bridge_nod`）、以及 **`0x55` 写错对象时终端会飘出一串 `U`**（已回填 `04` §3.1 症状速查）；<br>③ ⚠️ **口径修正（双重证据）**：`03` §4 W2 的参数任务改为 **"补 `publish_period` + 注册参数回调"** —— 依据 = 我读源码（`uart_bridge_node.cpp` L42-44 只 `declare_parameter`、`openPort()` 只在构造里调用、**无 `add_on_set_parameters_callback`**）＋ **用户 09-24 实测 `ros2 param set port` 不生效**；同时确认 **`launch\uart_bridge.launch.py` 已存在且 `port`/`baud` 已声明为启动参数**（W2 的 launch 是"续写"而非"从零写"）、**`socat` 假数据自测已在 09-24 完成**；<br>④ ✅ **09-25（A 线 · 输入捕获）双定时器重写完成**：`w1_ic_测周法_project_keil` 拆成 **` -正确版`**（TIM2_CH2/**PA1** 出 1 kHz → **跳线 PA1↔PA6** → **TIM3_CH1** 捕获 + 从模式复位；**LED 判据 900~1100 Hz**；`0E/0W`、`Code=2728`；**2 张照片已核内容**：示波器 `1.00 kHz / Cyc1 1000 µs / 占空比≈20%`＋ 面包板上外部 LED 点亮）**+ `-错误版`**（原单定时器版原样留档 = 错题本）→ 审读写入 `05` **§8.10**（3 P0 / 2 P1 / 5 P2 + 两处"待你自己解释"的技术点）；<br>⑤ 🧭 **叙事同步**：`05` §8.9 加**归档说明**、§9.2 加 **09-25 更新**（C 档 ③已消 / ④大幅改善 / ②本轮通过）与 **09-24 R 线更新**（"验收闭环 ★★☆☆☆"应上调）、`00` 进度表 W1-2 行补 09-24/09-25、`03` §4 W2 行与验证/判据列、`04` §3.1 症状速查；<br>⑥ 📌 **AI 侧纪律（本轮再执行，并承认一次失误）**：**不用"计划文字"代替事实** —— 先读源码 + build log + 文件时间戳 + 笔记时间戳再下结论；**上一轮我差点把"09-24 是计划日"当成"09-24 未做"**，本轮按 `03` 行内 09-24 记录纠正；<br>⑦ ⏳ **仍未闭合（3 项，都是小收尾）**：` -正确版\README.md` 三要素、**一次 Watch 数值**（`TIM3->CCR1` / `IC_GetFreq()`）、**W2 三件**（`publish_period` / 参数回调 / bag）；✅ **两个 IC 目录 + 本轮全部文档已提交并推送：`b20084d2`**（09-25 晚；`git status` 干净）。 |
| **v1.27** | 2026-09-25 深夜（用户自省："我缺的是**项目工程精神**，不是语法"） | **新增 `09_0925考核复盘与工程精神整改.md`（把"工程精神"写成六条动作 + 台账）+ 三条不做清单**：<br>① **缘由**：09-25 晚 **RM 第一次考核失败**（`stm32_study_myself\rm战队第一次考核\流水灯控制`：原版 `0E/23W` **能编译却运行时卡死在波浪灯**；审读 = `05` §8.11 的 4 坑 / 23 warning / 重构版 / 未闭合 3 项）；<br>② **自诊 × 证据核对（`09` §2 六条）**：**"缺语法" ❌ 不成立**（4 坑 0 条语法错）｜**"看懂一次＝学会" ✅**（＝ `05` §9.2 C④「未验证就交付」＋ S1 四项欠账）｜**"数学与代码结合弱" ✅ 且是**最短的一块**（09-21 `PSC/ARR` 未减 1 → 988.7 Hz、09-23 `ARR=65536-1`、09-25 `sin` 隐式声明 + 相位"超前"，**四处同源 = 数没落地就下结论**）｜**"问题分析弱" ⚠️ 只改措辞**（定位套路与判据都有，缺的是**收敛口径 ＝ 规格卡**）｜**"怕被追上→疯狂学新的" ✅ 且早已量化**（`05` §9.4 风险①「广度超前 + 规格欠账」）；<br>③ **`09` §3 六条动作**（规格卡先行 / 意图↔配置对照表 / 一条现象判据 / 数据级自检 / **非阻塞+初始化职责+状态机** / 一次只改一个变量）＋ **§4 换燃料指标**（KPI 达标率 · 欠账净额 · 有判据的交付数）；<br>④ **联动改动**：`00` **§16.4 不做清单新增第 7 条**（不开"没约束的新知识点"、不交"没判据的交付"）、`README` **§3 文件表 + §4 第 11 条（工程精神五条）**、`README` §5 补 09-25 实况；<br>⑤ ⏳ **本轮未闭合（都在 `09` 里自记）**：`09` §7 空区块（**本人手写**：§7.1 四坑→固定动作台账 / §7.2 疑问）、`05` §8.11.6 的三个 `[ ]`（**实物照片 / 一次 Watch 数值 / git 提交**）；<br>⑥ 🧭 **下一步（`09` §5）**：09-26 上午**闭卷重做**（照 `_复写计划_20260927.md`）→ 下午补实物证据 + 提交 → 09-27（W2 第 1 天）定 W2 唯一目标 = **S1 四项欠账清零**（规格卡 `05` §9.5）。 |
| **v1.28** | 2026-09-25 深夜（用户三条要求：**① 不许改主题日计划 ② 平衡车新购（国庆任务）③ 以后每天学完都要"出一个项目"给我做、笔记由 AI 写全、我只验收**） | **新增「每日项目制」+「国庆平衡车 P 卡」（主题日零改动）**：<br>① **新增 `成长路线\10_每日项目制.md`**：**三档卡**（`D` 日卡 30~90min / `W` 周卡＝规格卡 / `P` 窗口卡）＋ **项目卡模板 9 字段（不附实现代码）** ＋ **在途 ≤ 1** ＋ **收工前 10 分钟四步**（写含计算式的结论 → 跑一条判据 → 改台账 → 写"明天第一件事"）＋ **反模式 7 条** ＋ **8 条建议**；<br>② **新增 `成长路线\11_国庆平衡车项目卡.md`**：`套餐1（含遥控器）` **9 项到货核对表**（实测列留空）＋ **里程碑 G0~G5 与 S 台阶闸门** —— **✅ G0 组装+`06_Kit_Check`自检 / G1 双电机开环(S2) / G2 编码器测速(S2)**；**⛔ G3 ADC(S4) / G4 直立环(S5+S7) / G5 蓝牙(HC-04=UART=S6)+NRF24L01(SPI 不在 S1~S11 内) 国庆一律不做** ＋ **7 天时间盒**（每天 5h，照 `03` §9.3 含 1 天 ROS2 Gazebo + 1 天机动）＋ **G1/G2 规格卡** ＋ **安全红线 6 条** ＋ 台账；<br>③ **口径修订（按用户要求）**：`README` §4 **第 9 条**由"AI 留空白 + 本人补写"改为 **"AI 写全文 + 你必做验收三动作"**（复述 / 指出≥1 处 / 真用过一次），**但保留 3 个手写口**（一句话复述 / 我的坑 / 我的疑问）—— 分工可换，"看懂一次 ≠ 学会"（`09` §2 #2）这条防线不换；<br>④ **新增 `README` §4 第 12 条（每日项目制）**＋ §3 文件表 2 行＋ §5 W1 行补 09-25 实况；<br>⑤ 📌 **零改动声明**：**`03` §1 主题日表与 `00` §4 的时长/钟点一字未动** —— 本机制**只在"收工 10 分钟"与"交付验收"两个位置加动作**；<br>⑥ ⏳ **待用户确认的两条口径冲突**（写在 `11` §7，未擅自改）：**(a)** `README` §5 的 **W3/W4 行**写"平衡车验收 / 直立环起步"，与台阶定义（**IMU = I2C = S7**、**速度环 = S5**）矛盾 → 建议按 **v1.23 纪律**把直立环后移到 S5+S7 之后；**(b)** **NRF24L01 = SPI**（S1~S11 无 SPI）、**HC-04 = UART**（＝S6）→ 遥控/蓝牙属"新知识点"，按 **§16.4 第 7 条**排队。 |
| **v1.29** | 2026-09-25 深夜（**用户指出：`README` §5 的 W3/W4 行与台阶定义（ADC=S4 / I2C=S7 / UART=S6）矛盾 —— "我早就发现了，让你反复检查你也没查出来"**） | **计划一致性审计 + 9 处口径修复（含 3 个参考工程 README）+ 顺序重订提案 + RM 护栏**：<br>① **新增 `成长路线\12_计划一致性审计与顺序修订.md`**：**§0 认账**（我错在"只复述、不逐格对账"＋"只加注、不改行"的**半修复**）＋ **§1 七项检查清单 C1~C7（可复跑）** ＋ **§2 14 条问题**（#1 `README` §5 三行是 09-20 前的旧内容；#2 ADC/I2C/UART/直立环四处错位；#3 **三个参考工程 README 都写"W3 直立环"**；#4 S1 截止日两套口径；#7 套件到货排两周；#13 **RM 无时间盒条款**…）＋ **§3 按实际进度的 A/B/C 三套顺序（B 推荐：S1 只清"四路 + 5 kHz" → 国庆做 S2 → **S6 UART 提前到 W5** → S5 速度环 → S7 I2C → 直立环；**S4 ADC 降为机动**）** ＋ **§4 RM vs 小车护栏**（小车 P0；RM ≤5h/周、不占电控日、月度<80% 降级、12-31 判据）＋ **§5 四个待拍板开关**；<br>② ✅ **已修 9 条**：`README` §5 **W2/W3/W4 三行重写**（对齐 `00` §14：W2=S1 / W3=S2 / W4=S3）＋ §5"到 W3 直接用 / 覆盖到 W4"两处改写 ＋ §2 平衡车行；`03` §4 算法队列 ⑧（W2 → **S6/W6**）；**三个参考工程 README**（`05_I2C_MPU6050`、`…_Reg`、`06_Kit_Check` 的"W3 直立环"→"**S5+S7 之后（约 11 月）**"）；`00` §14 W2/W3 行的"套件到货"重复口径合并为"**已下单 09-25 + 到货当天跑 `06_Kit_Check`（`11` G0）**"＋ §16.2 同步；<br>③ 🧠 **新纪律**：`10` §8 新增 **反模式 #8**（"改了计划只加说明、不改行"）；**`12` §1 的七项检查在每次改顺序/日期时必跑**；<br>④ ⏳ **待你拍板（`12` §5）**：① S1 的③互补死区+④寄存器版是否改性质 ② **S6 UART 提前到 W5** ③ S4 ADC 降为机动 ④ RM 护栏写入 `README` §1 / `00` §16.1 —— **确认后出 v1.30 并同步 `00` §13/§14、`03` §4、`05` §9.5、`11`**；<br>⑤ 🔎 **顺带查出仓库级问题（`12` §2 #15 / §7）**：根目录 **11 个参考工程（733 文件）已移入 `cline 的示例代码/` 但从未入库**（`git status` 显示 **733 × ` D `**，已验证是"纯改名"：`cline 的示例代码` 递归文件数 = **733**）＋ `stm32_study_myself\rm战队第一次考核\` 与 `成长路线\09~12` **未跟踪** → **`12` §7 给出提交策略**（分两条提交：`refactor:` 搬家 / `docs:` 本轮文档；**提交后 `git status --porcelain` 行数应 ≤5** 作为新验收判据）。 |
| **v1.30** | 2026-09-25 深夜（用户确认：**"B 方案，①②③④全同意"**；并告知 **平衡车是成品、不需组装**） | **顺序重订正式落地（B 方案）+ RM 护栏入档 + 平衡车"成品"口径**：<br>① **A 线顺序（§14 已重写 W2~W6）**：**W2 = S1 只清 ①②（四路 + 5 kHz）** → **W3 = S2（`03_Encoder` 标准库 + **本人寄存器版再写一遍** + 国庆窗口 `11` G0~G2）** → **W4 = S3（SR04）** → **W5 = S6 UART + 环形缓冲（⭐ 提前，复用算法补丁 ⑧）** → **W6 = S5 速度环 PID（必须有上位机曲线）** → **W7 = S7 I2C → 平衡车直立环起步**；**降为机动**：**S4 ADC**、**③ 互补死区（→ RM/无刷分支）**、**④ 寄存器版（换场，不取消）**；<br>② **§13 步骤 1 时间改 W5~W8**（因 S6 提前）；<br>③ 🛡 **RM 护栏入档**（`README` §1 第 4 条）：**小车 = P0 唯一作品载体 / RM = P1 探路**，**RM ≤5h/周（含例会与考核）、禁止占用周一/周三电控日**，**任一月主线完成率 <80% → RM 降为"只参会"**，**2026-12-31 判据不变**；<br>④ 🛴 **平衡车"成品"口径（`11` 新增 §1.1）**：**到货即成品 → 取消"组装"环节**，G0 改为 **核对+量电压链+`06_Kit_Check`+「反向拆解」画出引脚↔外设对照表**（成品车最容易"只敢用教程"，所以这条是**必做**）；新增**红线 ④：禁止拿出厂固件/套件 demo 当成果**（先备份出厂固件，再从自己写的空工程开始）；新增核对项：**电机是否带编码器、编码器有没有引出到排针**（若无 → G2 改用 FishBot 的 **370 电机**，平衡车退为"IMU + 直立环载体"）；**电池每月补电一次**（唯一"买早了"的真实风险是 18650 长期亏电）。 |
| **v1.31** | 2026-09-25 深夜（用户提供**平衡车下单页技术参数截图**；并问 3 个问题：**"我学得真的不扎实吗 / 明天不学 ROS 吗 / 学得快是因为赶进度吗"**） | **平衡车官方参数入账 + 3 个问题给"证据版"答复（不喊口号）**：<br>① 📋 **`11` §1 新增「官方技术参数」表（14 行，下单页原文）**：主控 `F103C8T6` ＋ **`TB6612FNG`** ＋ `MPU6050` ＋ `NRF24L01+` ＋ **`HC-04`** ＋ **`AMS1117`(3.3V)** ＋ 电机 **`25GA370`／减速比 `9.27666`／6V620RPM** ＋ **编码器 AB 正交 `11PPR`** ＋ 电池 **2×18650 1500mAh** ＋ 遥控器（`F103` ＋ `NRF24L01+`/AT2401C）；<br>② 🧮 **由参数表先算出 3 个数（G0 只核对、不许现场推）**：**编码器一圈计数 = `11×9.27666` = 102.04（1 倍频）／`11×4×9.27666` = 408.2（4 倍频）**、额定 **620 RPM**、电池**串联 ≈7.4V／并联 ≈3.7V 必须实测**；并把"**磁铁在电机轴还是输出轴**"列为 G0 必分辨项（**它决定 G2 的换算式**）；<br>③ ✅ **原"④ 是否带编码器"结案一半**：参数表明写**有编码器** → `11` §1.1 的"**无编码器 → 改 FishBot 370**"**降级为备用预案**（不预设启用）；G0 只余两件核对（**实物是否有 / A、B 引到 MCU 哪两个脚**）；联动改 **`11` §2 闸门表 G0·G2 判据**、**§7 ④**、**`README` §2 硬件行**；<br>④ 🧭 **两处口径补丁**：**(a)** `11` §7 ② 补 **`HC-04` = S6 的实物载体** → **S6 提前到 W5（10-18~10-24）后它就有了"约束来源"，届时可合法开**（遥控器 `NRF24L01` = SPI，**仍排队**）；**(b)** `11` §3 补 ROS2 口径 —— **国庆 10-05 那一天 `替代` W2 的"周四 ROS2 动手日（10-01）"**（10-01 归 G0），**W2 的 09-29 周二仍在，净额没丢**；<br>⑤ 📊 **3 个问题的答复（全部引文件）**：**(a) "扎实吗"** → `05` §9.2 **C 档 5 条里 ③ 已消 / ④ 大幅改善 / ② 本轮通过**，"不扎实"应精确改写为"**可演示级 ≠ 可交付级**"；**(b) "明天（09-26 周六）不学 ROS2 吗"** → **是主题日制度**（周六 = ⚒️ 攻坚日），**R 线固定在周二/周四/周日（7h/周）**，09-26 那格给**欠账清偿**（`09` §5 的 D-0926-01/02），**W2 的 ROS2 任务已排在 `03` §4**；**(c) "学得快是因为赶进度吗"** → **不是**：可用 **≈890h vs 需求 235~360h（余量 2.5~3.8 倍）**、**W1 KPI 16h/16h 达标**；"快"来自**广度超前 + 时间余量大**，慢的是**规格 / 判据**（`05` §9.4 风险①）⇒ 结论 = **不是赶进度，是"计量单位"用错了**（`09` §4 换燃料：KPI 达标率 / 规格欠账净额 / 有判据的交付数）。 |
| **v1.32** | 2026-09-26（W1 最后一天 · 周六攻坚日；用户四条裁决：**① 归属 = 同一件事 ② 相位不改（"我觉得完美了"）③ 上板 3 项都做了 ④ 提交 git**） | **`流水灯第二次尝试` 审读落档 + D-0926-01 结"已交（部分）" + 本文件零计划改动**：<br>① 📕 **`05` 新增 §8.12**（8 子节）：5 处修复复核（**只替换 `Bit_SET/Bit_RESET` 就 `0E/0W`，`Code=4044`**；`Code 4044→4104`）/ **AI 本轮认错 4 次**（同一个病：拿旧快照 + 不重读文件 + 没算数）/ `break;` 机器码对拍（`0x08000eec NOP → BL PWM_Init` 的"填空执行"消失）/ ⭐ **"要多按一会"完整因果**（判定窗 = **2 × 主循环周期**：改前 **640 ms** → 非阻塞后 **40 ms**）/ **新查真问题：波浪亮峰 3→1→2、间距 180/90/90** / 待修表 P1-1~P2-4 / 判据清单 / 与 §8.11·D-0926-01 的关系；<br>② 🧭 **两条裁决落档**：**`10` §7 D-0926-01 = "已交（部分）"**（走法①；**只差呼吸灯**，2/3 效果）＋ **相位偏差 = 已知偏差**（`05` §8.12.5 末条 + 8.12.6 P1-1：**本人决定保留，不再计待修**，且写明"想反证我只需 30 秒 Watch"）；<br>③ ✅ **上板 3 条现象勾闭**（按键即时 / 流水灯 100 ms 一盏 / `g_loop` 两模式 ≈50 Hz）—— **本人自述口径**（`05` §8.12.7 标【你测】，**照片/录屏仍缺**）；<br>④ 🔎 **顺手查实两条**：4 个源文件 = **UTF-8 无 BOM + CRLF**（Keil 需 `Encoding=UTF-8` 否则中文注释乱码，**编译不受影响**：UTF-8 字节全 ≥ `0x80`，拼不出假 `\`）＋ `git status` 证明**本轮只动 `成长路线\` 文档、未碰工程源文件**；<br>⑤ 🚦 **零计划改动（`12` §1 的 C1~C7 因此无需重跑）**：**W1 三口径（A/R/B）全绿**（仓库+参数入账 / `uart_bridge` 09-24 socat 闭环 / 制度上线）＋ **W1 KPI 16h/16h 达标**；**W2 第 1 天（09-27）目标不变** = `W-0927-01`（S1 欠账 ①② 清零）；<br>⑥ 📊 **W1 收尾裁定（用户问"今天是否达标"）**：**W1 层面达标**｜**09-26 当日"日卡闭环"= 部分达标**（缺：工程 `README` 一行结论 / `09` §7.2 三个数 / D-0926-02 的照片+Watch / 呼吸灯）＋ ⚠️ **台账超发**（`10` §2 铁律，见 `10` §7 后注的处置建议）；<br>⑦ 🧾 **本次提交 = `docs:` 一条**（`05`/`10`/`03`/`00`/`README` + 未跟踪的 `13_成果转化路线`/`推免规划`）；**`rm战队第一次考核\` 故意不入库**（公开仓库 + 含 `_参考答案_20260925\` ⇒ 按 `12` §7"**由你确认可公开**"条款留给你本人）。 ⚠️ **0926 14:08 更正（见 v1.37）**：本句**只对 `流水灯第二次尝试\` 成立** —— `流水灯控制\`（**含 `_参考答案_20260925\`**）**已于 09-25 15:18 由本人提交并推送到公开仓库（`3871faa`，83 个文件）**，原文此句是**事实错误**。 |
| **v1.33** | 2026-09-26（用户反问：**“呼吸灯不是有吗”** —— 直接质疑 `05` §8.12.6 P2-3（缺呼吸灯）与 §8.12.7 第 4 个 `[ ]`） | **口径修正：把“缺呼吸灯”限定到正确对象（不是计划改动）**：<br>① 🔎 **重读源码取证（本轮没再犯“拿旧快照”的病）**：`流水灯第二次尝试\Usher\main.c` **全 44 行** = `if(mode>1) mode=0;` + `switch(mode)`，**只有 `case 0`（流水）/ `case 1`（PWM）**；`Hardware\PWM.c` 三路 `φ = 270/180/90`（相邻 90°）⇒ **依次明暗 = 波浪**；该工程目录递归文件里**只有 1 份 `main.c`**（不存在第二个工程）；对照 `流水灯控制\_复写计划_20260927.md` §0 **D 步**“正弦表 + **呼吸灯（先三灯同步）**｜三灯一起渐亮渐灭”；<br>② ✅ **用户是对的，错在我没说清“哪个工程”**：**呼吸灯在 09-25 的 `流水灯控制\` 里就有**（`Effect_Breathe_Init/Step`：三路**取同一个**正弦值 = 三灯同步；`README` §3 ②“3 个灯一起渐亮→渐灭，一个来回 2.4 s”）—— **09-26 上板看到的“按一下=呼吸”就是它**（证据归 **D-0926-02**）；而 **`05` §8.12 整节的对象是 09-26 新建的第二个工程**（`led_test.uvprojx`），它只有 **2/3**；<br>③ 🔧 **落档（3 处 + 本节）**：`05` §8.12.6 **P2-3** 行加“**仅指本工程**”限定并写明旧工程有；`05` §8.12.7 呼吸灯那条加 **📌 反纠正注** + **30 秒自判法**（`mode 1` 下三灯“一起”明暗 = 呼吸 / “依次”明暗 = 波浪）；`10` §7 **D-0926-01** 状态与表后注同步改写；<br>④ ⚖️ **裁定不变的部分**：`D-0926-01` 仍记 **“已交（部分）”**（该卡要求的是**闭卷重做后的新工程**出 3 个效果）；`05` §8.12.5 相位 = **已知偏差**不变；`D-0926-02`（旧工程证据）仍需照片 / Watch / 入库；<br>⑤ 🧭 **新纪律（防复发）**：**同一个词在两份文档里指两个工程 ⇒ 必然争吵** —— 此后凡写“有 / 缺某效果”，必须写全 `流水灯控制\`（09-25）还是 `流水灯第二次尝试\`（09-26）。 |
| **v1.34** | 2026-09-26（用户二次澄清：**"流水灯里面 case1 不就是呼吸灯吗"** / **"PWM 不就是用来做波浪和呼吸的吗"** / **"09-25 那一版都是错的，你看他干什么"**） | **第二次口径修正（v1.33 的方向错了，本轮改回）：呼吸灯按"PWM 亮度渐变"口径认定 ⇒ `D-0926-01` 结"已交"**：<br>① ❌ **我 v1.33 的错**：把"呼吸灯"窄化成"**三灯同步**渐亮渐灭"，于是把 `mode 1`（三路错相 90°）判成"波浪、缺呼吸"；**更错的是拿 09-25 `流水灯控制\` 当"呼吸灯存在"的依据** —— 那是**本人判定的失败版**，**不具引用资格**（`05` 里该工程此后只作**对照 / 错题本**，**不作任何"已完成"证据**）；<br>② ✅ **正确口径（本人 0926）**：**呼吸灯与波浪灯 = 同一个 PWM 底子**（都是"改 CCR 让亮度随时间变"），**差别只在三路 duty 的关系**：**同值 = 三灯同步呼吸** / **各差一个相位 = 波浪** ⇒ 本工程 `mode 1` 三灯都在渐亮渐灭 ⇒ **呼吸灯 = 已实现**；错相 `270/180/90` = 已结案的**已知偏差**（裁决②）；<br>③ 🔧 **落档（5 处）**：`05` §8.12.6 **P2-3 划掉**（并写明我 v1.33 那条依据无效）；`05` §8.12.7 呼吸灯 `[ ]` → `[x]` + 📌 注改写；`05` §8.12.8 归属按裁决①收敛（不再讨论归属）；`10` §7 **D-0926-01 状态 "已交（部分）" → "已交"** ＋ **判据列**改为 2 模式口径（流水 / PWM 呼吸）；本文件本行；<br>④ 🧭 **顺带一条建议（等本人点头）**：**`D-0926-02` 建议作废**（对象 = 已判失败版，补证据无意义），实物证据改挂到**新工程**；<br>⑤ 📌 **纪律（同类错误已三次，写死）**：**"缺不缺某个效果"的判据，只能来自本人对"效果"的定义，不能来自我按名字想象的定义**；拿不准就**先问定义、再下结论**（`05` §8.12.2 的"先跑一次 / 读一次 / 算出数"之外，再加一条"先问一次"）。 |
| **v1.35** | 2026-09-26 下午（用户示波器实测：**"我看呼吸灯的频率会有一瞬间跳到 1.2k"**） | **P1-2 严重性上调（文件零计划改动）：`PWM_Init()` 每 20 ms 重跑 = 每 20 ms 砍断一个 PWM 周期**：<br>① 🔎 **根因（读码+算数，不靠猜）**：`main.c` 的 `case 1` 里 `PWM_Init()` **每轮循环都跑**（无 `last_mode` 守卫，≈50 次/秒）⇒ `TIM_TimeBaseInit` 末尾写 `EGR.UG=1`（`TIM_PSCReloadMode_Immediate`）**把 `CNT` 清零** = 当前周期被从中间砍断；`TIM_OCxInit` 的 `TIM_Pulse=0` 又把三路 `CCR` 先清 0；<br>② 🧮 **数字对得上**：正常 `72 MHz/72/1000 = 1.000 ms`；被砍的周期 = 调用瞬间的 `CNT`（0~1000 µs）⇒ 833 µs = **1.2 kHz**；且"循环 20 ms = 正好 20 个 PWM 周期"⇒ 切口**相位锁定**、**每 20 ms 稳定一刀**（这正是"一瞬间"的来源）；<br>③ ✅ **修法 = 4 行**（`last_mode` 守卫：只在模式变化时 `xxx_Init()` 一次，循环里只留 `Run`/`PWM_LED` + `Delay_ms(20)`）；已核对 `LED_CurrentRun` 的 `s_step/s_tick` 是函数内 `static` ⇒ 挪出 `Init()` **不改 100 ms/盏节奏**；**源文件仍由本人手敲**（一个字节没动）；<br>④ 📕 **落档**：`05` §8.12.6 **P1-2 行重写**（🔴 上调 + 写明我上午"µs 级毛刺"的判断是**第 4 次同类错**：没算数就下"无害"结论）；`05` §8.12.7 新增判据 **"波形干净度"**（`Period` 稳定 1.000 ms、无 20 ms 周期的变形脉冲）；`10` §7 后注同款补记；<br>⑤ ⚖️ **不改任何裁定**：`D-0926-01` 仍 **"已交"**（判据"有 PWM 亮度渐变"成立）；P1-1 相位仍是**已知偏差**；**建议 09-27 开工前先修 P1-2**（~5 min，同一个病搬到 S2 电机 PWM 上就是真抖）。 |
| **v1.36** | 2026-09-26 下午 14:03（用户两条话：**"陪练模式 开"** ＋ **"我好累，这个 bug 我不想修了，你也不要修了，我已经从早上 8:00 坐到现在了"**） | **W1 收工：陪练模式生效 + P1-2 顺延（文件零计划改动）**：<br>① 📕 **`05` 新增 §8.12.9「这个项目有多基础」**（回答"是不是很基础"）：**题目基础 / 判据不基础** —— 自写代码 **≈208 行**（`main 47 / PWM 68 / LED 50 / KEY 43`，`Library\` 是厂商代码不算水平）＋ 知识点全在入门教程前 ~20 集 ＋ 判据四层（能亮 → **不阻塞** → **波形干净** → **能讲清寄存器**，本人已摸到第三层）＋ **答辩自测 6 问**（`PSC/ARR` 推导 · `CCR=1000` · `500±499` · `AF_PP` vs `Out_PP` · 为何不用 `while+Delay` 消抖 · `EGR.UG`）；<br>② 📕 **`05` 新增 §8.12.10「AI 依赖度自评」**（回答"不都是你教我的吗"）：把"我教的"拆成 **① 知识（我给）／② 判断（你给：上探头看到 1.2 kHz 并当真、提出"init 在循环里"的假设、两次纠正我并赢、裁决相位不改）／③ 手感（只有你能给）** ＋ **我本轮抢跑的 3 个动作**（读库源码找 `CNT`← 该用 **Find All References**；算 `833 µs` ← 该先算差数看"像哪种错"；分"频率/亮度"两条线 ← 该画"谁管什么"表）＋ **三步自问** ＋ **每周 30 min 闭卷复现（W2 起）**；结论：**"第 7 天靠 AI"正常，要防的是"第 30 天还只靠 AI"**；<br>③ 🛠 **陪练模式正式生效（本人裁决①）**：此后我说"**去看哪一行 / 拿哪条判据算**"，**不贴代码**，最后 10% 由本人走完；**第一次实战 = 09-27 的 P1-2 修复**（我先给判据：`Period` 稳 `1.000 ms` + 来回切 10 次仍亮 + 流水灯仍 100 ms/盏）；<br>④ 🛑 **P1-2 顺延（本人裁决② = 疲劳裁决）**：**今天不修、AI 不许动源文件** ⇒ 顺延 **09-27 开工前 5 min**；`05` §8.12.6 **P1-2 行加收工裁定**（并写明**不阻塞任何结案**：`D-0926-01` 仍"已交"、P1-1 仍"已知偏差"）；<br>⑤ 🧾 **本轮边界**：只改 `成长路线\` 文档（`05`/`00`）⇒ **工程源文件零改动**，`流水灯第二次尝试\` 仍是 `??`（**等本人手敲 `git add`**）；**零计划改动** ⇒ `12` §1 的 C1~C7 无需重跑；**W1 到此收工**，**W2 第 1 天（09-27）= `W-0927-01`（S1 欠账①②清零）不变，顺序：先 P1-2 后 S1**；<br>⑥ ⏱ **工时诚实记录（14:14 更正，见 v1.38）**：本人 **08:00 起连续作业至 14:10 ≈ 6.2 h**（午/晚饭与休息未计）—— **高于 W1 日均**；⚠️ **提醒**：午后疲劳已出现（本人自述"好累"）—— **09-27 只做两件事：修 P1-2 + S1 四路 5 kHz**，其余（README / `09` §7.2 三个数 / D-0926-02 处置）**排到复盘那一格，别提前**。~~（原文误写"连续作业至深夜 / 连续 12 h+"，系未量时钟的臆测）~~ |
| **v1.37** | 2026-09-26 下午 14:08（用户问：**"你给我 git 了吗"**） | **git 事实核查（`git status -uall` + `git ls-files` + 远端页面实测）+ 更正 v1.32 的一句事实错误**：<br>① ✅ **我本轮 6 次提交全部已推送**：`aca4f4f`（P1-2 拦坑）/ `42781c1`（P1-2 查实 v1.35）/ `3300298`（P1-2 两种写法）/ `79246de`（§8.12.9）/ `cceb1de`（§8.12.10）/ `3dbc3a2`（W1 收工）—— **每次只含 `成长路线\` 的文档**（`--name-only` 逐个对过）；`git status -sb` = `## main...origin/main`（**无 ahead**）；<br>② 📦 **未入库 = 仍 `??`（等本人手敲 `git add`）**：**(a)** `…\rm战队第一次考核\流水灯第二次尝试\` —— `-uall` 展开 **65 个文件**（`Usher\main.c` + `Hardware\*.c/h` + `Library\`（StdPeriph 全量 40+ 文件）+ `Start\` + `System\Delay.c/h` + `led_test.uvprojx`）；**`Objects\` / `Listings\` / `DebugConfig\` / `*.uvoptx` / `*.axf` 已被 `.gitignore` 挡住**（不会把编译垃圾带上）⇒ 提交体积 ≈ 库文件 1.5 MB 级；**(b)** `成长路线\推免规划_尹华振_2029届.md`（含真名 + 推免策略 ⇒ 按 `12` §7 留本人拍板）；<br>③ 🔴 **纠错（我 v1.32 ⑦ 那句"`rm战队第一次考核\` 故意不入库"只对一半）**：`流水灯控制\`（**含 `_参考答案_20260925\`**）**早在 `3871faa`（2026-09-25 15:18:58，改动 83 个文件）就由本人提交**，且 `git merge-base --is-ancestor` 退出码 = **0**（**已在 `origin/main` 上**，只落后当前 9 个提交）；远端 = **`git@github.com:HzEgw/stm32_study.git`**，抓页面实测 = **Public**（页面有 `Public` 标签、47 commits）⇒ **"参考答案已公开可见"是既成事实**，不是本轮造成的；<br>④ 🧭 **处置选项（✅ 本人 0926 14:09 裁决 = (a) 不动**，原话一个字"a"）：普通**不做任何改动** —— 理由：**考核 09-25 已交 ⇒ 对自己无成绩风险**，只是别人能看；且与 `13` 成果转化路线"**公开可见的成长记录**"口径一致（这是有意保留的展示面）。若日后反悔，(b)/(c) 成本不变、随时可做：**(a) 不动** ★已选／**(b) 只从最新版本移除**：`git rm -r --cached "stm32_study_myself/rm战队第一次考核/流水灯控制/_参考答案_20260925"` + commit（**历史里仍可翻到**，仓库首页看不到）／**(c) 仓库转 Private**（最彻底，代价 = 失去公开展示面）／**(d) 抹历史（**不推荐**：需 filter-repo + 强推，风险高收益低）；<br>⑤ 🖐 **动作边界（再次确认）**：本轮**仍一个字节都没碰工程源文件**；`流水灯第二次尝试\` 的 `git add/commit` 由本人手敲（`README` §4 第 10 条）。 |
| **v1.38** | 2026-09-26 14:20（用户问：**"怎么又是深夜了，现在才几点啊"**） | **时钟口径核查 + 更正我"深夜 / 连续 12 h+"的臆测（时间标签勘误）**：<br>① 🕒 **实测三头对（本机 ↔ 网络 ↔ 提交）**：本机 `Get-Date` = **`2026-09-26 14:10:09 +08:00`（星期六）**、时区 = `(UTC+08:00) 北京，重庆，香港特别行政区，乌鲁木齐`（**正确**）、`Get-Date` UTC = `06:10:09`；网络授时（timeapi.io `/Time/current/zone?timeZone=Asia/Shanghai`）= **`2026-09-26 14:10:08`** ⇒ **与本机秒级吻合** ⇒ **本机时钟准，无双系统 +8h 偏移（`00` v1.18 那个隐患当前不存在）**；我上一条提交 `4b23a64` 的时间戳 `ad=cd=2026-09-26 14:09:17 +0800` 与两者一致（**服务端可验证**）；<br>② 🔴 **我的错（本轮第 5 次同类错：不量就下结论）**：我从"本人说累 + 早上 8:00 坐到现在"**推**出"深夜"，并把 **≈6.2 h 写成"连续 12 h+"**，还据此给了"该睡觉/关机"的作息建议 ⇒ **全部更正**：`00` v1.35/v1.36/v1.37 **行头**由"晚/深夜"改为 **"下午 + 精确时刻"（v1.36 = 14:03 / v1.37 = 14:08）**、v1.32 ⑦ 更正注由"0926 深夜更正"改为 **"0926 14:08 更正"**、**v1.36 ⑥ 工时**由"至深夜 / 连续 12 h+"改为 **"08:00 → 14:10 ≈ 6.2 h"**（原文划删除线留痕）；`05` §8.12.6 **P1-2 行**"0926 **晚**"改为 **"0926 下午"**、"连续作业**到深夜**"改为 **"至 14:10 ≈ 6.2 h"**；`05` §8.12.7 **判据行**"0926 **晚**新增"改为 **"0926 **下午**新增"**；<br>③ 🧭 **作息结论随之改（重要：别按更正前的原文执行）**：**不是"深夜该睡了"，现在是周六下午 14:10**；本人 **08:00 起已连续 6.2 h**（自述"好累"属实，**疲劳裁决照旧有效**）⇒ 可选 = **(a) 先休息 30~60 min 再定** ／ **(b) 今天就此收工**（09-27 干 P1-2 + S1）／ **(c) 只做不烧脑的轻活**（工程 `README` 补三要素 = `P2-2`，≈10 min，纯打字）—— **由本人裁决；AI 不催、不替选**；<br>④ 📌 **新口径规则（并入 `v1.18` 证据分级）**：**凡写"几点 / 几小时"必须先量时钟**（`Get-Date` + 网络授时 + `git log --date=iso` **三头对**），**没量的时段一律不许写进文档**（本轮教训：一句"深夜"把 6.2 h 夸成 12 h+，连带作息结论都错）；<br>⑤ 🧾 **边界**：本轮仍**只改 `成长路线\` 文档**，**工程源文件零改动**。 |
| **v1.39** | 2026-09-26 14:25（用户：**"顺便把产出也更新一下"**） | **W1 产出清单回填（4 处）+ 1 处过期产出更正 + 我的两处错当场改**：<br>① 📕 **`03` §6（日/周粒度产出行 · W1 行）**：**更正 1 处过期产出** —— 原文 09-26 格写"❌ 未闭合：呼吸灯（2/3 效果）"，**已被本人同一天澄清推翻**（呼吸灯在 09-25 的 `流水灯控制` 里本来就有；第二个工程 `case 1` 给的是**三路相位差 270/180/90 ⇒ 看着像波浪**）⇒ 改写为"**`D-0926-01` 改判「已交」= 2/2 效果齐**"＋**追加 09-26 下午段**（P1-2 抓取 + 4 行修法 + "09-27 开工前 5 min"裁定 / 陪练模式生效 / 仍缺 4 项 / 提交实测数）；<br>② 📕 **`00` §9（双周粒度打卡表）W1-2 行**：追加 **09-26 段**（上板 3 现象 / `D-0926-01` 已交 / P1-2 / 陪练模式 / 未闭合 4 项 / 下一步 = **先 P1-2 后 `W-0927-01`**）；<br>③ 📕 **`README` §5（进度表）W1 行**：**状态 `进行中` → `✅ 已收工（W1 达标）`** ＋ 追加 09-26 段（与 ② 同口径）；<br>④ 📕 **`00` §14（逐周计划）W1 行「周末验收产出」格**：旧值一句 `GitHub 仓库 + 清点表已更新` → 改写为 **W1 实况 6 条**（仓库 + 6 次提交 / 三路 PWM（示波器 `997 µs`）/ `w1_ic` 双定时器（**S3 提前 ≈80%**）/ RM 考核两工程 + `D-0926-01` / R 线 `uart_bridge` 假数据闭环 / 制度 5 件套 + **KPI 16h/16h**）＋ **未闭合 5 项**（含 P1-2）；<br>⑤ 🔴 **我的两处错（当场发现、当场改）**：**(a)** §14 那格**首版漏了列分隔符（竖线）** ⇒ 会把「主线 B」与「产出」并成一格、**整张表由 4 列变 3 列**（已即时修回）；**(b)** 我写"本轮 **8 次** `docs:` 提交"——**又是没量就写数**（同 v1.38 的病）；`git log --date=iso` 实测 = **09-26 当天 12 次**（`0c77d0c` 13:27 → `ab2c777` 14:11）⇒ 文档三处已改为 **12 次**；<br>⑥ ⏸ **`09` §7.2「每周三个数」是本人手写区**（`README` §4 第 9 条 **AI 不代填**）⇒ 本轮**一字未动**，只在对话里给**候选数**（**KPI 16h/16h = 达标率 100%**／**欠账净额**：新增 P1-2 待修 + 顺延项，清偿见 `09` §5／**有判据的交付数候选 = 6**：git 仓库 · 三路 PWM · `w1_ic` · `uart_bridge` · `流水灯控制` · `流水灯第二次尝试`），**由本人周日（09-27）填**；<br>⑦ 🧾 **本轮边界**：只改 `成长路线\` 的 **`00` / `03` / `README` 三个文档**（`git status --porcelain` 实测 = 这 3 个 ` M ` + 原有 2 个 `??`），**工程源文件零改动**；**零计划改动** ⇒ `12` §1 的 C1~C7 无需重跑。 |
| **v1.40** | 2026-09-26 14:19（用户再问：**"按照现在的进度，我的计划能完成吗"** —— 与 09-25 是**同一个问题**，原测算在 `05` §9.4） | **`05` 新增 `§9.4.1`「W1 收工后的复算」**（**不改 §9.4 原测算，只做复算**，便于对照）：<br>① 🕒 **先量时钟再写字**（v1.38 规则）：`Get-Date` 实测 = **2026-09-26 14:19:17 +08:00（星期六）**；日期账同样实测 —— **2026-09-27 → 2027-05-31 = 246 天 = 35.1 周**；<br>② 🧮 **补上原版缺的"日历账"维度**（原 §9.4 只有工时账）：剩余 **35.1 周** vs 计划自带 **W2~W36 = 35 周** ⇒ **里程碑序号维度的结构缓冲 ≈ 0**；但每周任务只需 **13~16h / 可用 25h ⇒ ≈40% 富余** ⇒ **缓冲不在"周"上，在"每周多出来的 9~12h"上**；<br>③ 📊 **W1 折算 = 净超前 ≈1.2 周**（三路 PWM +0.5 ／ 输入捕获 ≈80% +0.8 ／ R 线预做 +0.25 ／ 扣除未闭合 4 项与 P1-2 −0.3）；<br>④ 🚦 **三个先行指标更新到 09-26 收工**：**KPI 1/1（16h/16h）🟢** ｜ **规格欠账仍 2 项（四路 / 5 kHz）🟡**，W2 卡口已定、10-03 前清零 ｜ **B 线外部动作 = 0 🔴（唯一零进展）**；<br>⑤ 🎯 **诚实结论**：**A 线技术目标 = 能**（工时余量 2.5~3.8 倍）；**"校赛过→省赛+大创" = 技术侧够，一半在 B 线**，而 B 线 = 0（`13` §7 **R1**：无导师 ⇒ 四条通道全锁死，窗口 2026-10~11）；`00` §17.4 定位 = **乐观线起跑位，样本仅 1 周**；<br>⑥ 📌 **一句话**：**"时间不会让你完不成计划；一直不去找老师 + 每周只交演示版会"** ⇒ **09-27 起三件事 = ① P1-2（5 min）② W2 卡口（TIM3 四路 @5 kHz）③ B 线第一封邮件（1h，别等 W4）**；<br>⑦ 🧾 **边界**：只改 **`05`（新增 §9.4.1）+ `00`（本行）** 两个文档；**`09` §7.2 三个数仍留你手写**（`README` §4 第 9 条）；**工程源文件零改动**。 |











---

## 17. 8 个月后我能到什么程度（定稿基线 · 实话版）

> 写在 **2026-09-19 定稿**当天。规则：**每月初回看一次**——如果发现"早已超过"或"还差很远"，
> **改的是计划，不是这句话**。它的作用只有一个：**当镜子，防止自我欺骗**。

### 17.1 先校准：20h/周 × 34 周 ≈ 650h 到底意味着什么
- 650h ≈ **一个认真做的本科毕设 + 一个省赛项目的总投入**。但"投入时间"不等于"有效成长"。
- 按经验打折：**有效 ≈ 55~65%**（≈ 350~420h 真正进脑子），其余消耗在配环境、查错、走弯路、看教程。
- 结论一句话：**你能"从零做出一个完整机器人系统"，但不会"在任何一个方向上成为高手"**。
  这正是"集成型 / 全栈下位机"的定位——**深度靠赛道窄，宽度靠系统完整**。

### 17.2 到期（2027-05）**大概率能稳定做到**（现实线）
| 能力 | 到期水平 | 客观证据（面试/答辩可展示） |
|---|---|---|
| 裸机 STM32 | 独立写外设驱动：GPIO/TIM/PWM/输入捕获/编码器/UART(中断+DMA)/I2C/SPI/ADC（**CAN 可选，v1.5 起不做硬要求**）；**标准库与寄存器两套都会** | 工作区 10+ 个双版本工程 + 逐行寄存器注释 |
| 调试 | 会用示波器/万用表/逻辑分析仪 + Keil Watch + 串口日志，能区分"硬件 / 时序 / 逻辑"三类问题 | `docs/logs` 里几十条"现象 → 原因 → 解法" |
| RTOS | 能用 FreeRTOS 拆任务、队列/信号量、优先级不踩坑 | 下位机固件稳定跑 3~4 个任务 |
| 控制 | 速度环/直立环 PID 会调；懂编码器 → 速度 → 里程计的换算与标定 | PID 阶跃响应对比图（数据说话） |
| ROS2 | **比现在高一档**：会写节点 / 自定义 msg / launch / 参数 / TF；能读懂并改 `fishros`、Nav2 的代码；能讲清 TF、QoS、executor | 自研功能包 + `uart_bridge` + micro-ROS 端到端 |
| micro-ROS | 能独立把 STM32 接成 ROS2 节点（串口/WiFi），知道 XRCE-DDS 在链路里干了什么 | 一块 F407 上的自研节点（非抄例程） |
| 工程化 | 会定协议、会分层（MCU / 桥 / 上位机）、会留调试口；会用 git 管版本与实验记录 | 仓库 commit 历史 + 可复现 README |
| 表达 | 能写复现文档、能拍 3 分钟演示、能答辩（讲清"我做了什么、为什么难、凭什么可信"） | 作品包 + PPT + 视频 |

### 17.3 到期**不会**具备（别自我欺骗）
- ❌ **不是** SLAM / 导航算法的人：默认会用 Nav2 跑通，但讲不清 cartographer 内部，写不出自己的前端后端。
- ❌ **不是** 深度学习的人：视觉部分是"调用 + 微调"，不是"设计网络"。
- ❌ **不是** 硬件 / PCB 的人：不会画板、不懂 EMC；焊接是"能补"，不是"能设计"。
- ❌ **作品 ≠ 名次**：校赛能否过，一半在手、一半在选题与评委。
- ❌ 不保证 **GPA / 导师 / 队友**：这三件是"计划之外变量"，只能持续降风险（§16.2），不能承诺。

### 17.4 三种结局（每月对号入座，别在最后才知道自己走哪条）
| 线 | 触发条件 | 2027-05 的样子 | 我的估计 |
|---|---|---|---|
| **乐观** | 34 周里 ≥28 周完成 KPI ＋ 找到指导老师 ＋ 队友稳 | 校赛过→省赛；F407 自研 micro-ROS 车完整可复现；投暑期实习能拿到面试 | 25% |
| **现实** | 完成 ≥22 周 KPI，中间断 1~2 次（每次 1~2 周） | 校赛过；作品能演示、能讲清；攒下一份"拿得出手"的简历资产 | **50%** |
| **困难** | 断线 ≥3 次 / 导师与场地始终无解 / 赛前赶工 | 只有 F103 若干工程 + 半成品车；但裸机能力、git 与文档习惯仍然留下 | 25% |

> **关键结论**：即使走"困难线"也不会白学；但**只有"现实线以上"才配得上挑战杯与实习**。
> 所以目标定在**现实线**，用 `03` 的主题日制度去压住那 50% 的波动。

### 17.5 每月一行自查（写在 §9 打卡表旁边）
```
月度自查：KPI 完成 ___/4 周 ｜ 本月新增能力 ____________ ｜ 最大空白 ____________ ｜ 下月首要修正 ____________
```

