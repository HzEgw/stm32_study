# 07 · 09-22（周二）ROS2 日任务单（**Linux 侧** · 19:00-21:45）

> **为什么是今天**：按 `03` §1 主题日 —— **周二 = 🐢 ROS2 回炉日**（R 线，基准 2.5h）。
> **本日唯一目标（一句话可验证）**：**在 Ubuntu 里 `colcon build` 通过，并让 `uart_bridge` 跑起来、出现 `/mcu/frame` 话题。**
> **原则**：**只做 R 线，不碰 STM32**（防分心）；命令随手复制进 `~/ros2_ws/notes/commands.md`（你的"外置记忆"）。
> 📖 全程照 **`04_Ubuntu侧开工清单.md`** 敲；**报错先查 `04` §5 报错表**，查不到就把报错贴我。

---

## ⏱ 时间盒（2.5h）

| 时段 | 做什么 | 参考 |
|---|---|---|
| **Windows 侧 · 重启前 5~10min** | ⚠️ **必做**：`git status` → `git add -A` → `git commit -m "..."` → `git push`（现在有 **5 个文件**待提交）—— **别带着未提交改动重启** | `04` §0.3③ / §6 |
| 同上 | 写 **Linux 待办清单**（3~5 条，抄下表的第 3~7 行即可） | `04` §0.3② |
| 19:00-19:10 | 进 Ubuntu → `source /opt/ros/humble/setup.bash`（**写进 `~/.bashrc`** 一劳永逸）→ `ros2 doctor` | `04` §4① |
| 19:10-19:30 | 若 `~/repos` 不存在 → **配 SSH 钥 + `git clone git@github.com:HzEgw/stm32_study.git`**；随后 `cd ~/repos/stm32_study && git pull` | `04` §1.2 / §1.3 |
| 19:30-20:00 | `mkdir -p ~/ros2_ws/src` → **软链接** `uart_bridge` → `rosdepc install -i --from-path src --rosdistro humble -y` → `colcon build --symlink-install --packages-select uart_bridge` | `04` §3 ①②③ |
| 20:00-20:10 | 休息（离开椅子） | — |
| 20:10-20:40 | `source install/setup.bash` → `ros2 run uart_bridge uart_bridge_node` → `ros2 topic list \| grep mcu`（没有串口也应打印**清晰**的"打开失败"） | `04` §3④ / §4③④ |
| 20:40-21:20 | **通过后**（W2 内容）：① 节点加**参数**（串口号/波特率）② 写 `launch/` ③ `ros2 bag record` 录 10s；**没通过**就查 `04` §5 | `00` §14 R 线 W2 |
| 21:20-21:45 | **收工**：命令写进 `~/ros2_ws/notes/commands.md` → Linux 侧 `git add/commit/push` → **回 Windows 后把 `03` §6 打卡表 09-22 那格打 ✅（30 秒，顺手补一行产出）** | `04` §6 |

---

## ✅ 验收（DoD）

- [ ] `ros2 doctor` **无 error**（warning 可放过）
- [ ] `colcon build` **0 failed**；`uart_bridge` 能 `ros2 run` 起来
- [ ] `ros2 topic list | grep mcu` **看到 `/mcu/frame`**
- [ ] `~/ros2_ws/notes/commands.md` 里有今天**所有**命令
- [ ] Linux 侧 `git push` 成功（改动上云）
- [ ] **三问**（写进 notes，别查资料先自己讲）：
      ① `colcon build` 到底在干什么？（对比 Keil 的"编译→链接→下载"）
      ② 为什么每次都要 `source install/setup.bash`？
      ③ 为什么 `colcon build` **必须在 ext4**（`~/ros2_ws`）而不能在 `/mnt/d`？

## ⚠️ 三个必踩的坑（先记住）

1. **别在 NTFS 上 build** → 所以工作空间是 `~/ros2_ws`（Ubuntu 的 ext4），**不是** `/mnt/d/...`
2. **装依赖用 `rosdepc`**（官方 `rosdep` 拉不到索引，`04` §2.3）
3. **每个新终端都要 `source`**（`/opt/ros/humble/setup.bash` + `~/ros2_ws/install/setup.bash`），否则报 `Could not find package 'uart_bridge'`

---

## 📌 后天（09-23 周三 · 电控日②）：PWM **精度与验证**（不是"补作业"，是"优化项"）

> 你 09-21 已经交了完整的东西：**代码 + README（含截图）+ git 提交 `6039ee9`** ✅
> 后天只是把"**1 kHz 到底准不准**"这件事做扎实：

`stm32_study_myself\01_pwm_out_project_keil`：
1. `pwm.c` 第 32-33 行 → **`TIM_Prescaler = 719`、`TIM_Period = 99`**（目标 **精确 1 kHz**）
2. `delay_ms()` → 换 **SysTick 版**（现在是空循环，-O0 能用但升优化会失效）
3. 然后做 **S1-a 验收**：CH3 = 25% + CH4 = 75%，**示波器看 PA1/PA2 两路同屏**
4. 工程 README 答"三问" + 落盘 —— 细则见 `05_代码审查与能力盘点.md` §8 与 `06` 任务单
