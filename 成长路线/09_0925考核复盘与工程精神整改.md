# 09 · 2026-09-25 RM 考核失败复盘：把"项目工程精神"拆成可核查的动作

> **为什么单开一个文件**：09-25 晚的考核失败**不是**一条新 bug，而是把一条**老毛病**第一次暴露在"别人出题、别人验收"的场合 ——
> `05` §9.2 **C 档 ④「未验证就交付」** ＋ `05` §9.4 **风险 ①「广度超前 + 规格欠账」**。
> 它值得单独成文，因为它是**关于"我怎么学"的结论**，不属于任何单个工程的技术细节。
>
> **怎么用**：① 现在通读一遍（10 min）；② 把 **§7 的空区块亲手填掉**（这是你的资产，AI 不代填）；
> ③ 每周日复盘用 **§8 的 4 个勾**走一遍；④ 每次开新工程前先看 **§3 的 6 条动作**。
>
> ⚠️ **本文件的边界**：正文由 AI 按**文件证据**起草；§2 的"判定"是**基于证据的推断**，不是对你的定性。
> **不同意哪一条，就改它**（这是你的复盘，不是我的报告）。

---

## 0. 一句话结论

**这次不是"语法不会"，是"没有验收口径"**：
原版能编译（`0 Error`）、能下载，但**运行时卡死在波浪灯** —— 交出去的是"**我以为**跑通了"，
而不是"**有判据证明**它符合规格"。所谓"缺项目工程精神"，精确翻译过来就是这一句。

> ⚠️ **不要把结论写成"我不够自律 / 学习能力差"** —— 这两条有反证：W1 KPI **16h/16h 达标**，
> 两条主线的硬验收都跑出了现象证据（`05` §9.1、§9.2 的 09-24 / 09-25 更新）。
> **问题不在"投入"，在"口径"。**

---

## 1. 事实基线（全部有文件证据，不用靠回忆）

| 事项 | 证据位置 |
|---|---|
| 题目与要求对照 | `流水灯控制\README.md` §1 |
| 失败版留档（6 文件） | `流水灯控制\_原版备份_20260925\`（`Usher\main.c` + `Hardware\{LED,KEY,PWM}.c|h`） |
| 4 个真坑 | `流水灯控制\README.md` §4 ＝ `05` §8.11.1 |
| 23 个 warning | `05` §8.11.2（`#188-D`×20 / `#223-D`×1 / `#550-D`×1 ＋ `key_mdoe` 命名坑） |
| 重构版（已封存） | `_参考答案_20260925\`（**开工前不要打开**） |
| 闭卷重做计划 | `_复写计划_20260927.md`（时间盒 A~G ＋ 6 个必答题 ＋ 4 个预防针） |
| **尚未闭合的 3 项** | `05` §8.11.6 的三个 `[ ]`：**实物照片 / Watch 数值 / git 提交** |

**四个坑（一句话版；全部属于"结构 / 数值"，没有一个是语法）**

| # | 症状 | 根因 |
|---|---|---|
| 1 | 进波浪灯后**按什么都没反应** | 效果函数里 `while(1)` 吃掉主循环 → `key_run()` 再也不跑（**没有"非阻塞"这一层概念**） |
| 2 | 切到 PWM 后**流水灯不受控** | 引脚被配成 `AF_PP` 后电平由 CCR 决定；切回来**没人重配引脚**（**初始化职责没归属**） |
| 3 | 波浪灯**亮度乱跳 + 台阶粗** | `sin()` 没 `#include <math.h>` → 隐式声明、返回值按 `int` 解释 = 垃圾数；`ARR=99` 只 100 级亮度（**数值没落地**） |
| 4 | 按一下**跳两个**、等松手时灯停 | `Delay_ms + while(读==0);` 阻塞消抖（**同上**） |

---

## 2. 你的自诊 × 证据核对（**这一节才是"我到底缺什么"的答案**）

| # | 你的原话 | 判定 | 证据 | **建议的精确措辞（把"感觉"换成"可核查"）** |
|---|---|---|---|---|
| 1 | "我最大的短板不是语法" | ✅ **成立** | 4 个坑里 **0 个**是语法错；你 8 个自建 Keil 工程里的两处问题（`5-1` 半成品 / `6-3` 空壳，`05` §1）是**没写完**，不是语法不会 | 语法不是瓶颈：瓶颈在 **① 运行结构（阻塞 / 初始化归属）② 数值（±1 / 相位 / 量级）③ 判据（什么算完成）** |
| 2 | "学完一个知识点就做个小示例，看懂一次＝学会" | ✅ **成立，而且早在账上** | `05` §9.2 C④「未验证就交付」（IC 版编译 0E 就以为完成）；**IC 工程做了两遍**（错误版→重写版）；S1 规格 **4 项欠账**（§9.5） | 成长单位从「**看懂一次**」换成「**关掉一张规格卡**」（通道 / 频率 / 引脚 / 判据全打勾） |
| 3 | "数学和代码结合能力差" | ✅ **成立，且是你最短的一块** | 四个同源实例：**09-21** `PSC=720/ARR=100` 没减 1 → 实际 `988.7 Hz`；**09-23** `ARR=65536-1`（把捕获满量程抄进 PWM）；**09-25** `sin` 隐式声明 ＋ 相位写成"超前"（靠数据仿真才抓到，§8.11.4）；`05` §9.2 **C①「算法能说不能写」**（冒泡排序） | **数必须算出来才能下结论**：`PSC/ARR`、占空比、相位下标、量程 —— 一律先写式子 / 先仿真，再写代码 |
| 4 | "问题分析能力和代码没法局限（收敛）起来" | ⚠️ **方向对，措辞要改** | 你**已经有**定位套路（`_复写计划` §5 的六步"灯不亮"排查顺序）＋ **已经会做判据**（双定时器版 `IC_GetFreq()∈(900,1100) Hz` → LED 亮，§8.10）→ **不是"不会分析"** | 你缺的是**收敛口径**：开工前没定义"做到什么程度算完"（**规格卡**）→ 于是"能跑"自动变成终点 |
| 5 | "怕被追上 → 疯狂学新东西（像鸦片）" | ✅ **成立，而且它已经在账上** | `05` §9.4 风险①「**广度超前 + 规格欠账**」：S1 只到 1 kHz 三路、输入捕获提前做但只是演示版、IC 做了两遍 | 换燃料：每周只看 **3 个数** —— **KPI 达标率 / 规格欠账净额 / 有判据的交付数**（§9.4 先行指标），而不是"这周学了几个新外设" |
| 6 | "我不再纠结 AI 时代还要不要写代码" | ✅ **结论正确，这次的证据很硬** | 题目全程只用**教科书级外设**（GPIO×2 + TIM PWM），且 AI 就在旁边 —— 失败点却全在**规格 / 结构 / 数值 / 验证** | **AI 能替我把代码敲出来，替不了我"定义规格 + 判断是否达标"**；这正是"集成型 / 全栈"定位的核心资产（`00` §17.2「工程化」「调试」两行） |

---

## 3. "项目工程精神"＝6 条动作（**每条都能被别人检查**）

| # | 动作 | 你最近的反例（有据） | 检查方式（我 / 队友 / 评委都能查） |
|---|---|---|---|
| 1 | **开工前写规格卡**：通道数 / 频率 / 引脚 / 判据 / 证据清单 | 拿到题直接写效果函数，"能亮"就成了目标 | 工程 `README.md` 顶部有一张 5 列表；**模板直接用 `05` §9.5** |
| 2 | **意图 ↔ 配置对照表**：一句意图 → 对应哪一行配置 / 寄存器 | 说"别用 CH1"却写 `TIM_Channel_1`（`05` §8.9.8）；重写时丢了 `TIM_ClearFlag`（§8.10.3 P1-1） | 表里每一行都能 `F12` 指到代码 → **专治"改对的地方会退回去"** |
| 3 | **一条现象判据**（判据 ＞ 数值 ＞ 印象） | 原版 `0E / 23W` 编译通过就当作完成 | `README` 里有"Watch 变量 → 期望值"表 ＋ 照片 / 波形；`05` §8.11.6 那种勾 |
| 4 | **数据级自检**：先算 / 先仿真，再判断 | `sin` 隐式声明、相位"超前" —— 都是"看着像对" | 每个结论旁附一行计算（如 `72MHz÷72÷1000 = 1kHz`）或仿真输出 |
| 5 | **结构三件套：非阻塞 / 初始化归属 / 状态机** | `while(1)` 吃掉主循环；切效果不重配引脚；`key_mode` 混用 | Review 只问三句：**主循环会卡住吗？谁负责初始化？状态在哪个变量里？** |
| 6 | **一次只改一个变量** | 调灯时同时动"参数 ＋ 引脚模式 ＋ 效果结构" | 每次 commit / 记录**只含一处变更** ＋ 一句预期（`_复写计划` §5 已写，要真的做） |

> ⚠️ **第 5 条不是"考核技巧"，是上 RTOS 与电机控制的地基**：S2（开环调速）之后所有代码都要求"主循环永不阻塞"；
> 现在养成，W11 学 FreeRTOS 时才不用重学一遍"任务"的概念。

---

## 4. 焦虑的对策：**换燃料指标**（不是靠意志力）

| | 旧燃料（多巴胺快、欠账不减） | 新燃料（反馈慢、可复利） |
|---|---|---|
| 计量单位 | "今天学了几个新外设 / 看懂了哪段源码" | "**关掉了几张规格卡 / 欠账净额 / 有判据的交付数**" |
| 立刻的爽点 | 打开新章节、摸新外设（**当天就有反馈**） | **很少**（所以只能按"周"兑现，不能按"天"） |
| 落点 | —— | 每周日只填 `05` §9.4 的 **3 个先行指标** ＋ 本文件 **§7.1 台账** |
| 硬纪律 | —— | **新知识点必须带一个"约束"才能学**（例：不是"会多通道"，而是"车上两个电机要 4 路 5 kHz 双 PWM" → `05` §9.5） |

> 一句话：**焦虑压不住，只能"换计量单位"** —— 焦虑盯的是"别人又学了什么"，台账盯的是"我这周关掉了什么"。
> 后者每周都给出一个可核查的数；前者永远不给你终点。

---

## 5. 立刻要做的 4 件事（**不改主线**，全部落在 09-26 / 09-27）

> ⚠️ 前提：`08_0924ROS2日②任务单.md` 原把"`w1_ic` **双定时器重写**"排在 **09-26**，
> 但 `00` v1.26 记录该重写**已在 09-25 完成** → **09-26 那一格空出来了**。
> 下面把闭卷重做放到 09-26 上午只是**建议**：你想放在 09-27 也行，**改不改由你定（改了告诉我，我同步日期）**。

| 时间 | 动作 | 完成标志 |
|---|---|---|
| **09-26 上午**（原 IC 重写格，已空出） | **闭卷重做** `流水灯控制`：照 `_复写计划_20260927.md` 的 **A~F**，**90 分钟到点就停**；期间**不许打开** `_参考答案_20260925\` | 交卷后与参考答案 `diff` → **只记差异，不抄代码**；差异写进 `09` **§7.1** |
| **09-26 下午** | ① **实物证据**：上电=流水灯 → 按一下=呼吸灯 → 再按=波浪灯 → 再按回流水灯（**照片/录屏，写明每张拍的是什么**）；② `git add` + 提交（**本人手敲**，见 `README` §4 第 10 条） | `05` §8.11.6 的 3 个 `[ ]` → `[x]`（Watch 那一格按 `流水灯控制\README.md` §5 的表看 `g_mode` / `g_led1~3_duty`） |
| **09-27（W2 第 1 天，5h）** | W1 复盘 50min（`03` §6 → `00` §9）＋ 定 W2 **唯一目标** = **S1 四项欠账清零**（规格卡照抄 `05` §9.5） | W2 目标一行写进 `03` §6 ＋ `09` **§7.1** |
| **本周内（10 min）** | 给 09-25 的 4 个坑**各写一条"下次的固定动作"** | `09` **§7.1** 五列表里不留"（待填）" |

> ⚠️ **本周不再开任何新外设**（`00` §16.4 第 7 条）：把 S1 那张卡关掉，比多学一个 ADC 值钱得多。

---

## 6. 我的一句话整改口号（**本人手写**）

> 参考句式："**先定判据，再写代码**。" ／  "**能跑不算完，有判据才算完**。" ／ "**每个台阶只做一遍到位**。"
>
> ✍️ **我的**：____________________________

---

## 7. 我的手写区（**AI 不代填** —— 这是 `README` §4 第 9 条的规矩）

### 7.1 欠账台账：4 个坑 → 我的**固定动作**（建议 09-26 前写完）

| 坑（09-25 原版） | 下次遇到同类情况，我的**固定动作**（一条、可执行） | 会在哪张卡 / 哪个文件里被检查 |
|---|---|---|
| ① `while(1)` 吃掉主循环 | （待填） | |
| ② 切效果不重配引脚 | （待填） | |
| ③ `sin` 隐式声明 / 相位写成"超前" | （待填） | |
| ④ `Delay_ms + while(读==0)` 阻塞消抖 | （待填） | |
| ＋ `key_mdoe` 命名坑（`05` §8.11.2） | （待填） | |

### 7.2 每周三个数（周日填，**只填数，不写感想**）

| 周 | KPI 达标（h） | 规格欠账净额（新欠 − 清偿） | 有判据的交付数（个） | 一句话 |
|---|---|---|---|---|
| W1 | | | | |
| W2 | | | | |
| W3 | | | | |

### 7.3 我的疑问（写下来 → 周五集中查 / 提问，**别当场陷进去**）

1. （待填）
2. （待填）

---

## 8. 周日复核（5 分钟，只走这 4 个勾）

- [ ] `09` §7.1 的台账**有没有新增动作**（没有就写"无新坑"，也是一种结论）
- [ ] 本周的交付**是否都带判据**（照片 / Watch / 波形）—— 缺的列进下周清单
- [ ] `05` §9.4 的 **3 个先行指标**已填（在 `09` §7.2）
- [ ] 下一周**只开一个新知识点**，且它**带约束来源**（`00` §16.4 第 7 条）

---

## 9. 与其他文件的接口（谁引用谁）

| 文件 | 关系 |
|---|---|
| `05` §8.11 | 本次考核的**技术审读**（4 坑 / 23 warning / 重构版 / 未闭合 3 项）—— 本文件只负责"**学习方式**"这一层 |
| `05` §9.2 **C 档** | ①算法"能说不能写"（**最短的板**）②读写一致性 ③共享资源 ④未验证就交付 ⑤UART 零经验 → 本文件 §3 是它们的**动作化** |
| `05` §9.4 / §9.5 | 3 个先行指标 / **S1 规格卡**（＝本文件 §3 动作 1 的模板） |
| `00` §16.4 第 7 条 | 本文件结论进"不做清单"的那一条 |
| `README` §4 第 11 条 | 本文件的**一页压缩版**（写代码时按那 5 条执行） |
| `_复写计划_20260927.md` | 09-26 / 09-27 的**执行脚本**（时间盒 + 6 个必答题 + 4 个预防针） |

> 📌 **最后一句（写在 09-25 深夜）**：这次失败其实是"好消息" ——
> 它发生在**中秋假的一次队内考核**里，代价只有一次脸红；
> 同样的毛病若留到 **2027-03 校赛答辩**那天才暴露，代价就是名次。
> 把它修在这里，是这笔账**最便宜的一次清偿**。
